# Future TODOs

最終更新日: 2026年7月7日

この資料は、第11.5段階以降で出た改善案を、現在の実装範囲から分離して記録するためのTODOである。第12段階または第13段階で対応済みになった項目は、その状態を明記して残す。

## 1. アウフタクト対応

- 「聖者の行進」など、曲頭が弱起で始まる曲へ対応する。
- 拍線・小節線と合わせて設計する。

## 2. ピアノ表示・スコア表示の拍線

- 状態: 第12段階で実装済み。
- ピアノ表示にも拍を表す薄い線を出す。
- スコア表示にも拍の把握を助ける線を出す。
- 将来、利用者確認で表示が邪魔になる場合は、表示ON/OFF設定を検討する。

## 3. 小節線

- 状態: 第12段階で実装済み。
- ピアノ表示・スコア表示の両方に、小節線を少し太めに表示する。
- 五線への移行を助ける教育的補助として扱う。
- アウフタクト、拍番号、小節番号は未実装のまま残す。

## 4. スコア表示の音名・指番号ラベル左寄せ

- 状態: 第13段階で実装済み。
- スコア表示では、音符ブロック中央寄せではなく左端寄せで音名・指番号を表示する。
- 表示順は `音名 / 指番号` を基本とする。
- 右手・左手は既存の色分けと凡例で区別する。文字ラベルとして手情報を追加するかは、必要性が出た場合に再検討する。

## 5. 広い音域、加線、低音域の扱い

- 喜びの歌などで下第2加線のラが出る場合がある。
- スマートフォンでの実鍵盤合わせと、スコア表示の見やすさを両立する方針を検討する。

## 6. 移調版や応用教材の運用方針

- 例: カエルの合唱（ニ長調）など。
- 基本はJSONファイル読み込みで扱い、よく使うものだけ内蔵サンプルへ昇格する。

## 7. 臨時記号付き音符の自動強調

- 五線譜で臨時記号にマーカーを付ける教育実践がある。
- `sharp` / `flat` の音符ブロックを自動で色付けする案を検討する。
- 音符ブロック全体は無彩色寄りにし、臨時記号だけが目立つ配色も候補にする。
