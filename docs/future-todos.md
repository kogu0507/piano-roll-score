# Future TODOs

最終更新日: 2026年7月8日

この資料は、第11.5段階以降で出た改善案を、現在の実装範囲から分離して記録するためのTODOである。第12段階から第14.5段階で対応済みになった項目は、その状態を明記して残す。

## 1. アウフタクト対応

- 「聖者の行進」など、曲頭が弱起で始まる曲へ対応する。
- 拍線・小節線と合わせて設計する。

## 2. ピアノ表示・スコア表示の拍線

- 状態: 第12段階で実装済み。
- ピアノ表示にも拍を表す薄い線を出す。
- スコア表示にも拍の把握を助ける線を出す。
- 将来、利用者確認で表示が邪魔になる場合は、表示ON/OFF設定を検討する。

## 3. 小節線

- 状態: 第12段階で実装済み。
- ピアノ表示・スコア表示の両方に、小節線を少し太めに表示する。
- 五線への移行を助ける教育的補助として扱う。
- アウフタクト、拍番号、小節番号は未実装のまま残す。

## 4. スコア表示の音名・指番号ラベル左寄せ

- 状態: 第13段階で実装済み。
- スコア表示では、音符ブロック中央寄せではなく左端寄せで音名・指番号を表示する。
- 表示順は `音名 / 指番号` を基本とする。
- 右手・左手・指定なしは、第14.5段階で追加した小さな補助バッジと凡例で区別する。文字情報をさらに増やすかは、必要性が出た場合に再検討する。

## 5. 広い音域、加線、低音域の扱い

- 喜びの歌などで下第2加線のラが出る場合がある。
- スマートフォンでの実鍵盤合わせと、スコア表示の見やすさを両立する方針を検討する。

## 6. 移調版や応用教材の運用方針

- 例: カエルの合唱（ニ長調）など。
- 基本はJSONファイル読み込みで扱い、よく使うものだけ内蔵サンプルへ昇格する。

## 7. 臨時記号付き音符の自動強調

- 状態: 第14段階で実装済み。第14.5段階で音符ブロック本体、手の補助表示、臨時記号強調の視覚上の役割を整理済み。
- 五線譜で臨時記号にマーカーを付ける教育実践に合わせ、`sharp` / `flat` の音符ブロックを自動強調する。
- 音符ブロック全体を派手に塗り分けるのではなく、細い左端帯と少し濃い枠線で軽く強調する。音名表示OFF時だけ、小さな `♯` / `♭` マーカー文字も表示する。
- 第14段階補正で色の意味づけを見直し、`sharp` はオレンジ/アンバー系の暖色、`flat` は青/青紫系の寒色で区別する。
- 将来、利用者確認で表示が邪魔になる場合は、表示ON/OFF設定を検討する。

## 8. ダブルシャープ、ダブルフラットの扱い

- 状態: 将来検討。
- 現行スキーマの `spelling.accidental` は `natural`、`sharp`、`flat` のみ対応する。
- ダブルシャープ、ダブルフラット、ナチュラル記号の明示表示、調号との関係は、楽曲データ形式と五線譜教育上の扱いを合わせて設計する。
