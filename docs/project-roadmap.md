# プロジェクト進行表

## 1. この資料の目的

この資料は、`piano-roll-score` の全体進行をユーザーとCodexが共有するための一覧である。

次の疑問に答えることを目的とする。

- 今どの段階にいるか
- 次に何を実装するか
- 実装スレッドへ何を依頼するか
- 実装完了後にユーザーが何をすればよいか
- どの時点で設計判断や手動確認が必要か

詳細な製品仕様は `spec.md`、現在の実装段階の詳しい作業内容は `docs/implementation-plan.md` を参照する。

## 2. 資料の使い分け

| 資料 | 役割 |
|---|---|
| `spec.md` | 何を作るか、なぜ作るかを定める |
| `AGENTS.md` | Codexが守る作業ルールを定める |
| `docs/project-roadmap.md` | 全体の順序、進捗、ユーザーの次の行動を示す |
| `docs/implementation-plan.md` | 現在の実装段階の詳細と完了条件を示す |
| `README.md` | プロジェクトへの入口と開発方法を示す |

## 3. 進行管理の方法

各段階は次の順序で進める。

1. 設計スレッドが、次の段階の範囲と実装指示を作る。
2. ユーザーが、その指示を実装スレッドへ渡す。
3. 実装スレッドが、実装、テスト、ビルドを行う。
4. ユーザーが、実装スレッドの完了報告を設計スレッドへ共有する。
5. 設計スレッドが、結果、未決事項、次の段階を確認する。
6. 必要な場合だけ、ユーザーが実機またはブラウザで手動確認する。
7. 設計スレッドが資料を更新し、次の実装指示を作る。

ユーザーは、技術的な判断やコマンド実行を自分だけで行う必要はない。必要な操作がある場合は、設計スレッドが目的、手順、正常な結果、問題が起きた場合の対応を具体的に案内する。

## 4. 現在の状態

最終更新日: 2026年7月2日

現在の段階: **第10段階 教室での手動検証とMVP仕上げ**

状態: **実装開始待ち**

ユーザーが現在行うこと:

1. 設計スレッドが提示する第10段階の実装指示を、実装スレッドへ渡す。
2. 実装スレッドの完了報告を、この設計スレッドへ共有する。
3. その後、`docs/manual-checklist-stage10.md` に沿ってMVP仕上げ後の確認を行う。

ユーザー自身がファイルを編集する必要はない。開発サーバーの起動や確認URLは設計スレッド側で案内する。

## 5. 段階一覧

| 段階 | 内容 | 状態 | 主なユーザー確認 |
|---|---|---|---|
| 0 | 仕様と共有資料の準備 | 完了 | 仕様の目的と優先事項 |
| 1 | 開発基盤 | 完了 | 設計スレッドで再検証済み |
| 2 | 楽曲スキーマと内蔵データ読み込み | 完了 | コミット・検証済み |
| 3 | ロード画面とJSON入出力 | 完了 | 手動確認6項目すべて合格 |
| 4 | 縦表示の静的描画 | 完了 | PC・Android・Canvas左右端確認すべて合格 |
| 5 | 横表示の静的描画 | 完了 | 五線風表示と異名同音、音符ブロック高さ |
| 5.5 | 横表示の表示調整 | 完了 | 五線間隔と譜面縦位置 |
| 6 | 共通タイムラインと再生 | 完了 | 流れる速度と操作感 |
| 7 | メトロノームとプリカウント | 完了 | 音量、音質、タイミング |
| 7.5 | 再生ガイドと助走表示 | 完了 | 音ゲー的な違和感を減らす |
| 8 | 端末内保存と設定保存 | 完了 | 保存と削除の分かりやすさ |
| 9 | レスポンシブ対応と操作性調整 | 実装済み | スマートフォン、タブレット、PC |
| 9.5 | 練習画面レイアウト再設計 | 完了 | Canvasと主要操作の優先配置 |
| 10 | 教室での手動検証とMVP仕上げ | 実装開始待ち | 教育的価値と改善点 |

## 6. 各段階の予定

### 第0段階: 仕様と共有資料

状態: 完了

成果物:

- `spec.md`
- `AGENTS.md`
- `README.md`
- `docs/project-roadmap.md`
- `docs/implementation-plan.md`

### 第1段階: 開発基盤

状態: 完了

主な内容:

- Vite + TypeScript
- TypeScript strict
- Vitest
- Playwright
- npmスクリプト
- `/app/` サブパス対応
- 最小ロード画面
- 単体テストとE2Eスモークテスト

2026年6月22日に、設計スレッドで主要ファイルと次の検証を再確認した。

- 型検査: 成功
- 単体テスト: 1件成功
- E2Eテスト: PC幅とスマートフォン幅の2件成功
- ビルド: 成功
- `/app/` サブパス: 正常

### 第2段階: 楽曲スキーマと内蔵データ読み込み

状態: 完了

主な内容:

- ローカルGitの初期化と第1段階の基準点保存
- 楽曲データのTypeScript型
- 実行時スキーマ検証
- MIDI番号と音名表記の整合性
- ド♯とレ♭の扱い
- 内蔵サンプルJSON
- 内蔵サンプル一覧
- `?id=001` による読み込み

完了コミット:

```text
8e793b8c179e6c722b10cc0261e384a693363a53
```

設計スレッドで型検査、37件の単体テスト、8件のE2Eテスト、ビルドを再確認した。

### 第3段階: ロード画面とJSON入出力

状態: 完了

主な内容:

- 内蔵サンプル選択
- JSON貼り付けと編集
- JSON検証エラー
- JSONファイルの読み込み
- JSONファイルの書き出し

想定するユーザー確認:

- 初心者でも操作順が分かるか
- ボタン名と説明文が分かりやすいか
- エラーからやり直せるか

ユーザー手動確認結果:

- 最初の画面: 問題なし
- サンプル読み込み: 問題なし
- JSON編集とエラー: 問題なし
- 破棄確認: 問題なし
- JSON書き出し: 問題なし
- JSON再読み込み: 問題なし
- 分かりにくい箇所: なし
- 改善要望: なし

### 第4段階: 縦表示の静的描画

状態: 完了

主な内容:

- Canvas描画
- 白鍵と黒鍵の位置計算
- 課題音域だけの表示
- 白鍵幅調整
- 横位置調整
- 判定ライン
- 音名と指番号

この段階は重要な手動確認を必要とする。

ユーザーが確認する予定の内容:

1. スマートフォンでアプリを開く。
2. 実際のピアノ鍵盤付近へ置く。
3. 白鍵幅を調整する。
4. 横位置を調整する。
5. 画面上の位置と実鍵盤が合うか確認する。
6. 不要な左右余白がないか確認する。

実装コミット:

```text
f62cc6b2a02959837d2e7aed168b8335d4394a49
```

2026年6月23日に設計スレッドで次を再確認した。

- 型検査: 成功
- 単体テスト: 78件成功
- E2Eテスト: 32件成功
- ビルド: 成功
- `git diff --check`: 成功
- 作業ツリー: クリーン
- 自然音サンプルのCanvas描画: 目視確認済み
- ド♯とレ♭が同じ横位置で異なる音名になること: 目視確認済み

ユーザーによるPC確認結果:

- 縦表示の最初の見え方: 問題なし
- 白鍵幅の調整: 問題なし
- 横位置の調整: 問題なし
- Canvasのドラッグ: 問題なし
- ロード画面へ戻る: 問題なし
- ド♯とレ♭の表示: 問題なし
- 分かりにくい箇所: なし
- 改善要望: なし

ユーザーによるAndroid Chrome実機確認結果:

- スマートフォンで開けた: はい
- 横向き表示: 問題なし
- タッチで白鍵幅を調整: 問題なし
- タッチで横位置を調整: 問題なし
- Canvasの指ドラッグ: 問題なし
- 実鍵盤と白鍵幅を合わせられた: はい
- 実鍵盤と横位置を合わせられた: はい
- 不要な左右余白: 今回の課題では問題なし
- 端末回転後の表示: 問題なし
- 合わせやすかった音域: 5度
- 分かりにくい箇所: なし

実機確認から得た改善事項:

- 通常のアプリとしては自然な左右余白がCanvasにも存在する。
- 実鍵盤へ合わせる用途では、Canvasだけは表示可能領域の左右端まで使える方が教育上の利点が大きい。
- スマートフォンでは物理的な画面幅により、おおむね6度程度が実鍵盤へ合わせられる上限と考えられる。
- タブレット横向きでは、1オクターブと完全5度程度まで合わせられる可能性がある。

設計判断:

- 第9段階へ延期せず、第4段階の小規模補正として扱う。
- ヘッダー、説明、調整UIの読みやすい余白は維持する。
- 鍵盤との位置合わせに使うCanvasだけ、端末の表示可能領域の左右端まで広げる。
- ノッチなどの安全領域は侵さず、ページ全体の横スクロールは発生させない。

Canvas左右余白の補正コミット:

```text
9f792b60a000898994b1ecd7e5916d5a96d9e5ba
```

設計スレッドで再確認済み:

- 型検査: 成功
- 単体テスト: 78件成功
- E2Eテスト: 34件成功
- ビルド: 成功
- `git diff --check`: 成功
- 844×390および回転後の全幅E2E: 成功
- 実ブラウザのCanvas左余白: 1px（境界線分）
- 実ブラウザのCanvas右余白: 1px（境界線分）
- ヘッダーと調整UIの左余白: 12px
- ページ全体の横スクロール: なし

ユーザーによる補正後のAndroid Chrome確認結果:

- Canvasが左右端まで広がった: はい
- ヘッダーと操作欄の余白: 問題なし
- 画面全体の横スクロール: なし
- 気になったところ: なし

第4段階は完了とする。

### 第5段階: 横表示の静的描画

状態: 完了

主な内容:

- 太い5本線
- 控えめな補助グリッド
- 五線外の補助線
- 音名表記に基づく縦位置
- ド♯とレ♭の表示差

実装コミット:

```text
cffab259cee89f31de00599278a67792a63cd1c7
```

2026年6月24日に設計スレッドで次を再確認した。

- 型検査: 成功
- 単体テスト: 89件成功
- E2Eテスト: 44件成功
- ビルド: 成功
- `git diff --check`: 成功
- 作業ツリー: クリーン
- 仕様上の未決事項: なし

ユーザー手動確認結果:

- 横表示の最初の見え方: 問題なし
- 五線5本線の見やすさ: 問題なし
- 補助グリッドの濃さ: 問題なし
- 縦表示と同じ課題だと分かるか: はい
- ド♯とレ♭の違い: 問題なし
- ロード画面へ戻る: 問題なし
- 画面サイズ変更後の表示: 問題なし
- スマートフォン表示: 問題なし
- 使用スマートフォン: Android
- 使用ブラウザ: Chrome
- 分かりにくい箇所: なし

改善要望:

- PC、スマートフォン共通で、横表示の音符ブロックの縦幅が少し大きい。
- 五線に寄せるなら、音符ブロックの高さを五線の線と線の間隔に合わせるとよい。
- 現状はブロックが五線の間からほんの少しはみ出して見える。

設計判断:

- 五線への移行を支援する見え方に関係するため、第6段階へ進む前の第5段階小修正として扱う。
- 五線座標、異名同音、時間軸、再生機能には触れず、横表示の音符ブロック高さだけを調整する。

音符ブロック高さ補正コミット:

```text
b47a902bdf3a5ae1978bd0940ddb02ac2ae2c249
```

設計スレッドで再確認済み:

- 型検査: 成功
- 単体テスト: 90件成功
- E2Eテスト: 44件成功
- ビルド: 成功
- `git diff --check`: 成功
- 作業ツリー: クリーン
- PC幅で横表示画面を開けること: 確認済み
- Canvas表示、ロード画面へ戻る、縦表示切替: 確認済み
- ページ全体の横スクロール: なし

現在の残作業:

- なし。

補正後のユーザー再確認結果:

- PCとスマートフォンの両方で確認済み。
- 横表示の音符ブロック高さ: ちょうどよい
- 五線らしさ: よくなった
- ド♯とレ♭の違い: 問題なし
- ロード画面へ戻る: 問題なし

追加で得た改善提案:

- 横表示でも、縦表示の白鍵幅・横位置調整に相当する表示調整があると見やすい。
- 五線の1間、つまり線間隔を調整できると、音域の狭い課題や初心者、高齢者、子供に有効である。
- 譜面全体の縦位置を調整できると、右手だけの練習や将来の両手課題で役立つ。
- 低音部譜表部分を下に表示する案もあるが、MVPでは本格的な大譜表を対象外としているため、現時点では将来検討として記録する。

設計判断:

- 横表示の五線間隔調整と譜面縦位置調整は、第6段階の再生に入る前に第5.5段階として実装する。
- 再生中の見え方にも影響するため、先に横表示の座標モデルを調整可能にしておく。
- 低音部譜表または大譜表風背景は、第5.5段階では実装しない。

手動確認手順は `docs/manual-checklist-stage5.md` に定める。

### 第5.5段階: 横表示の表示調整

状態: 完了

主な内容:

- 横表示の五線間隔を調整する
- 横表示の譜面全体の縦位置を調整する
- 「画面高に合わせる」または同等の初期調整を行う
- 「中央に戻す」で縦位置を戻す
- 調整値をCanvasへ即時反映する
- 調整値はこの段階では永続保存しない

想定するユーザー確認:

- 五線間隔を広げると見やすくなるか
- 譜面縦位置を動かして、必要な音域を見やすい場所へ置けるか
- スマートフォンでも操作しにくくないか
- 縦表示やロード画面が壊れていないか

手動確認手順は `docs/manual-checklist-stage5-5.md` に定める。

実装コミット:

```text
ef03ee915a3490a5296990cdcb665a5b28e98bf0
```

2026年6月24日に設計スレッドで次を再確認した。

- 型検査: 成功
- 単体テスト: 93件成功
- E2Eテスト: 46件成功
- ビルド: 成功
- `git diff --check`: 成功
- 作業ツリー: クリーン
- 仕様上の未決事項: なし

ユーザー手動確認結果:

- 五線間隔の調整: 問題なし
- 五線間隔を広げた見やすさ: よくなった
- 譜面縦位置の調整: 問題なし
- 中央に戻す操作: 問題なし
- ド♯とレ♭の違い: 問題なし
- 縦表示との切り替え: 問題なし
- ロード画面へ戻る: 問題なし
- スマートフォン表示: 問題なし
- 使用スマートフォン: Android
- 使用ブラウザ: Chrome
- 分かりにくい箇所: なし
- 改善要望: なし

### 第6段階: 共通タイムラインと再生

状態: 完了

主な内容:

- スタート
- 一時停止
- 先頭に戻す
- シーク
- 再生速度
- 縦横表示での再生位置共有
- 縦表示で音符ブロックが上から下へ流れる
- 横表示で音符ブロックが右から左へ流れる

この段階では、メトロノーム、プリカウント、音源再生、演奏判定は実装しない。これらは第7段階以降で扱う。

想定するユーザー確認:

- 音符の流れる速さが見やすいか
- 停止と再開が自然か
- シークと鍵盤幅調整を混同しないか
- 縦表示と横表示を切り替えても同じ再生位置が維持されるか

実装コミット:

```text
170ac5a5d91737e6df4a75c9e785acb9697e00c1
```

設計スレッドで再確認済み:

- 型検査: 成功
- 単体テスト: 103件成功
- E2Eテスト: 50件成功
- ビルド: 成功
- `git diff --check`: 成功
- 作業ツリー: クリーン
- 仕様上の未決事項: なし

未実装として維持したもの:

- メトロノーム
- プリカウント
- 音源再生
- 演奏判定
- 設定永続保存

ユーザー手動確認結果:

- 縦表示のスタート: 問題なし
- 縦表示の一時停止・再開: 問題なし
- 先頭に戻す: 問題なし
- シーク: 問題なし
- 再生速度変更: 問題なし
- 横表示への切り替えと現在位置維持: 問題なし
- 横表示の再生: 問題なし
- ロード画面へ戻る: 問題なし
- スマートフォン表示: 問題なし
- 使用スマートフォン: Android
- 使用ブラウザ: Chrome
- 分かりにくい箇所: なし
- 改善要望: なし

実装範囲と完了条件は実装時点の `docs/implementation-plan.md`、手動確認手順は `docs/manual-checklist-stage6.md` に定めた。

### 第7段階: メトロノームとプリカウント

状態: 完了

主な内容:

- Web Audio API
- メトロノーム
- 拍のアクセント
- なし、1小節、2小節のプリカウント
- メトロノームON/OFF
- メトロノーム音量調整

想定するユーザー確認:

- 音量が適切か
- 高い音と低い音を区別しやすいか
- 画面と音のタイミングが自然か

この段階では、音符に対応するピアノ音源再生、演奏判定、設定保存は実装しない。

実装コミット:

```text
a54efd484a1584df8ae2f7e206187f51f11b1d41
```

設計スレッドで再確認済み:

- 型検査: 成功
- 単体テスト: 111件成功
- E2Eテスト: 52件成功
- ビルド: 成功
- `git diff --check`: 成功
- 作業ツリー: クリーン
- 仕様上の未決事項: なし

手動確認で重視すること:

- メトロノーム音量が適切か
- 1拍目とその他の拍の違いが分かるか
- プリカウントが入る準備として分かりやすいか
- 画面の動きとメトロノーム音のタイミングが自然か

ユーザー手動確認結果:

- メトロノームONで音が鳴る: 問題なし
- 1拍目とその他の拍の違い: 問題なし
- メトロノームOFF: 未確定。回答欄に「問題なし / 問題あり」が残っているため、補正後に再確認する。
- 音量調整: 問題なし
- 再生速度変更への追従: PCで問題あり。再生速度を上げると、画面のスクロールが少し遅れる時がある。毎回ではない。
- 1小節プリカウント: 問題なし
- 2小節プリカウント: 問題なし
- 横表示でのメトロノームとプリカウント: PCで問題あり。再生速度変更への追従で同様の現象。
- ロード画面へ戻る: 問題なし
- スマートフォン表示と音: 問題なし
- 使用スマートフォン: Android
- 使用ブラウザ: Chrome
- スマートフォンでは、メトロノームと譜面移動のズレは確認されなかった。
- 音量について気になったこと: なし
- 分かりにくい箇所: なし
- 改善要望: なし

設計判断:

- 基本利用はスマートフォンとタブレット想定であり、Android Chromeでは問題が出ていないため重大ブロッカーではない。
- ただし、メトロノームと画面の同期は第7段階の中核であるため、第8段階へ進む前に小補正として扱う。
- 補正の目的は、再生速度変更時に画面描画とメトロノーム予約の基準時刻をそろえ、PCでもズレを起こしにくくすることである。
- 音源再生、演奏判定、設定保存などの新機能は、この補正に含めない。

補正コミット:

```text
c11b7b557a469dba513899a255b3f76da29294c1
```

補正内容:

- `PlaybackController.setPlaybackRate()` で、`tick`、`anchorBeat`、`anchorTimeMs`、`precountAnchorTimeMs`、メトロノーム再スケジュールが同じ `nowMs` を使うよう補正
- `start`、`pause`、`resetToStart`、`seek` も単一基準時刻へ整理
- メトロノームON/OFF・音量変更時も、再生中なら現在時刻まで進めてから再スケジュール
- 速度変更時の `currentBeat` とメトロノーム次拍計算が新速度で揃う単体テストを追加
- 縦表示・横表示のE2Eで、メトロノームON状態の再生中に速度を上げても `currentBeat` が進み続けることを確認

設計スレッドで再確認済み:

- 型検査: 成功
- 単体テスト: 112件成功
- E2Eテスト: 52件成功
- ビルド: 成功
- `git diff --check`: 成功
- 作業ツリー: クリーン
- 仕様上の未決事項: なし

補正後のユーザー手動確認結果:

- メトロノームOFF: 問題なし
- 縦表示で、再生中に速度を上げた時の画面とメトロノーム: 問題あり
- 横表示で、再生中に速度を上げた時の画面とメトロノーム: 問題あり
- 1小節プリカウント後の再生: 問題なし
- ロード画面へ戻る: 問題なし
- 使用PCブラウザ: WindowsのChrome
- PCで、少し動き出しが遅れるように感じることがある。
- メトロノームは「カコココカ」と「カコココカコ」まで行く二種類があるように感じられた。

設計判断:

- 追加の同期補正を続けるより、音楽ゲーム的な「判定線にぴったり合わせる」見え方を避ける方が、MVPの教育目的に合う。
- 赤い判定線は、正誤判定や完全同期を期待させるため、再生ガイドラインまたは再生ガイド帯へ弱める。
- プリカウント中も表示用の助走位置で譜面を流し、1拍目から急に画面が動き出す違和感を減らす。
- 実際の `currentBeat`、シーク、保存対象の現在位置は壊さず、表示用の `displayBeat` または同等の値を分離する。
- この方針は、将来のアウフタクト対応にもつながる。

### 第7.5段階: 再生ガイドと助走表示

状態: 完了

主な内容:

- 赤い判定線を、より弱い再生ガイドラインまたは再生ガイド帯へ変更する
- 「判定ライン」という文言を「再生ガイド」へ変更する
- プリカウント中も表示用の助走位置で譜面を流す
- 実際の `currentBeat` と表示用の `displayBeat` を混同しない
- 縦表示と横表示の両方で同じ考え方を使う

実装コミット:

```text
a90280d962282d51c4942757397c1d9f154229f2
```

実装済みの内容:

- UI文言を「判定ライン」から「再生ガイド」へ変更
- 縦表示・横表示のヘルプ文言を再生ガイドの説明へ変更
- Canvas上の強い赤い線を、薄い青緑系の再生ガイド帯と細線へ変更
- `currentBeat` とは別に `displayBeat` を導入
- プリカウント中は `currentBeat` を進めず、`displayBeat` を負の助走位置から0へ近づける
- 縦表示・横表示の両方で `displayBeat` による助走描画に対応
- Canvas dataset に `data-display-beat` と `data-playback-guide-x` を追加
- 単体テストとE2Eで、助走表示、実 `currentBeat` 維持、UI文言を検証

実装スレッドでの検証:

- 型検査: 成功
- 単体テスト: 115件成功
- E2Eテスト: 54件成功
- ビルド: 成功
- `git diff --check`: 成功
- `git diff --cached --check`: 成功
- コミット後の作業ツリー: クリーン

想定するユーザー確認:

- 音楽ゲームの判定線のような違和感が減るか
- プリカウント中に譜面が流れても自然か
- 速度変更時の多少のズレが気になりにくくなるか
- 縦表示と横表示で同じ感覚になっているか

ユーザー手動確認結果:

- 縦表示の再生ガイドの見え方: 問題なし
- 「判定ライン」ではなく「再生ガイド」として理解できるか: はい
- 1小節プリカウント中の助走表示: 問題なし
- プリカウント後の再生開始: 問題なし
- 再生速度変更時の違和感: 問題なし
- 横表示の再生ガイドと助走表示: 問題なし
- ロード画面へ戻る: 問題なし
- スマートフォン表示: 問題なし
- 使用PCブラウザ: Chrome
- 使用スマートフォン: Android
- 使用スマートフォンブラウザ: Chrome
- まだ気になるズレ: なし
- 再生ガイドについて気になったこと: なし
- プリカウント中の動きについて気になったこと: なし
- その他気になったところ: なし

実装範囲と完了条件は `docs/implementation-plan.md`、手動確認手順は `docs/manual-checklist-stage7-5.md` に記録済み。

### 第8段階: 端末内保存と設定保存

状態: 完了

主な内容:

- IndexedDBへの楽曲保存
- 保存一覧
- 読み込み
- 削除
- `localStorage` への設定保存
- JSONエクスポートの案内
- 端末内だけの保存であり、クラウド同期されないことの明示
- ブラウザのデータ削除で消える可能性の説明

実装コミット:

```text
cf8fd81405f98979c519049bd9d0fdebf66a87ec
```

実装済みの内容:

- IndexedDBによる明示的な楽曲保存、一覧表示、読み込み、削除
- 削除確認とキャンセル対応
- 端末内・同一ブラウザ限定保存である旨とJSON書き出し案内の表示
- `localStorage` による縦表示、横表示、再生関連設定の復元
- 壊れた設定値の初期値復帰、範囲外値の正規化
- 保存一覧と設定復元の単体テスト、E2Eテスト

実装スレッドでの検証:

- 型検査: 成功
- 単体テスト: 122件成功
- E2Eテスト: 60件成功
- ビルド: 成功
- `git diff --check`: 成功
- `git diff --cached --check`: 成功
- コミット後の作業ツリー: クリーン

設計判断:

- `song.id` がある楽曲は同じ保存IDとして再保存時に更新扱いにする。
- `song.id` がない楽曲は初回保存時に保存IDを発行する。
- 同じ曲から複数の別バージョンを保存する「複製保存」は、MVPでは扱わず将来検討とする。

想定するユーザー確認:

- 端末内だけの保存だと理解できるか
- 保存、読み込み、削除を迷わず行えるか
- 誤って消しにくいか
- 表示・再生設定が次回起動時に復元されるか

ユーザー手動確認結果:

- 楽曲を端末内へ保存できた: はい
- 保存後に曲名と保存日時が分かる: はい
- 端末内だけの保存だと理解できる: はい
- 保存一覧から読み込めた: はい
- ページ再読み込み後も保存曲が残った: はい
- 削除確認と削除操作: 問題なし
- 縦表示の白鍵幅・横位置設定の復元: 問題なし
- 横表示の五線間隔・譜面縦位置設定の復元: 問題なし
- 再生速度・メトロノーム・プリカウント設定の復元: 問題なし
- JSON書き出し案内の分かりやすさ: 問題なし
- スマートフォン表示: 問題なし
- 使用PCブラウザ: Windows Chrome
- 使用スマートフォン: Android
- 使用スマートフォンブラウザ: Chrome
- 分かりにくかったところ: なし
- 改善してほしいところ: なし

### 第9段階: レスポンシブ対応と操作性調整

状態: 実装済み

主な内容:

- ロード画面、縦表示、横表示、保存一覧、再生操作欄のレスポンシブ調整
- スマートフォン、タブレット、PC相当の代表画面幅での操作性確認
- 端末回転、ウィンドウサイズ変更、表示方向切り替え後の再レイアウト確認
- タッチ領域、ボタン間隔、折り返し、ラベルの見やすさの調整
- ページ全体の横スクロール防止
- セーフエリアと高DPI Canvasの既存対応確認
- 基本的なアクセシビリティ改善

実装コミット:

```text
3f480b2669e34379a807fe620e5010a472110107
```

実装済みの内容:

- スマートフォン幅向けにロード画面、保存一覧、JSON欄、ボタン列の余白と折り返しを調整
- 主要ボタン、range、file input、checkbox、select のタッチ領域を44 CSS px相当以上へ調整
- 縦表示・横表示の操作パネルを狭い画面で縦積みしやすく調整
- スマートフォン横向きなど高さが短い画面でCanvas高さを調整
- フォーカス表示対象を入力、セレクトにも拡張
- ページ全体の横スクロール抑制を追加
- 第9段階用E2Eを追加し、スマートフォン縦横、タブレット縦横、PC相当で確認

実装スレッドでの検証:

- 型検査: 成功
- 単体テスト: 122件成功
- E2Eテスト: 66件成功
- ビルド: 成功
- `git diff --check`: 成功
- `git diff --cached --check`: 成功

設計スレッドでの再検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、122件
- `npm.cmd run test:e2e`: 成功、66件
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

想定するユーザー確認:

- 実際に利用予定の端末で操作できるか
- ボタンが小さすぎないか
- 画面回転後も使えるか
- 保存、再生、表示調整などの既存機能が狭い画面でも使いやすいか

設計スレッドでの追加判断:

- 第9段階の実装により、スマートフォン、タブレット、PC相当の画面幅で横スクロールとタッチ領域は改善された。
- ただし、練習中の主役である譜面Canvasと再生・表示調整操作が画面の主役になっていないため、このままMVPにするには弱い。
- スマートフォンやタブレットを鍵盤付近へ置いて使う場合、実際の鍵盤を見ながら白鍵幅や横位置を調整できる必要がある。
- そのため、第10段階へ進む前に、第9.5段階として練習画面レイアウトを再設計する。

### 第9.5段階: 練習画面レイアウト再設計

状態: 完了

主な内容:

- 練習画面ではCanvasと再生プレビューを画面の主役にする。
- 練習中によく使う操作を常時見える主要操作バーへ集約する。
- 縦表示では、白鍵1鍵の幅と譜面の横位置を実鍵盤を見ながら調整しやすくする。
- 横表示では、五線の1間の幅と譜面の縦位置を調整しやすくする。
- ロード画面へ戻る、保存、JSON書き出し、説明などは、ハンバーガーメニューまたは折りたたみメニューへ移す。
- 再生速度、メトロノーム、プリカウントなどは、練習画面内の設定パネルへ整理する。
- スマートフォン、タブレット、PCで、譜面Canvasと主要操作が同時に扱えることを確認する。

実装コミット:

```text
bf3e630e2ed30d6df23dece711561db600c054ef
```

実装済みの内容:

- 練習画面に主要操作パネルを追加し、再生操作、シーク、表示調整をCanvas近くへ集約
- 「ロード画面へ戻る」を練習メニュー内へ移動
- 端末内保存、保存一覧、JSON書き出しはロード画面で操作する導線としてメニュー内に明示
- 再生速度、メトロノーム、音量、プリカウントを「再生補助設定」の折りたたみに整理
- 縦表示、横表示の切り替えを主要操作として目立つ位置に維持
- 第9.5段階用E2Eを追加し、メニュー開閉、主要操作、横スクロールなしを確認

設計スレッドでの再判断:

- 直近実装は、主要操作をCanvas近くへ追加した点では前進している。
- しかし、意図していた固定構造である「上部: 曲名 + 縦/横切替 + メニュー」「中央: Canvasを最大限広く表示」「下部: 再生・表示調整バー」にはなっていない。
- 練習中の主役であるCanvasが中央で最大化されておらず、主要操作バーも下部バーとして固定されていない。
- そのため、この状態をMVP候補とはせず、第9.5補正として実装をやり直す。

補正で必ず実現する構造:

```text
練習画面
├─ 上部: 曲名 + 縦/横切替 + メニュー
├─ 中央: Canvasを最大限広く表示
├─ 下部: よく使う再生・表示調整バー
│  ├─ 再生/一時停止
│  ├─ 先頭
│  ├─ 曲の現在位置
│  ├─ 白鍵幅 / 五線間隔
│  └─ 横位置 / 縦位置
└─ メニュー内
   ├─ 再生速度
   ├─ メトロノーム
   ├─ プリカウント
   ├─ 保存
   ├─ JSON書き出し
   └─ ロード画面へ戻る
```

実装スレッドでの検証:

- 型検査: 成功
- 単体テスト: 122件成功
- E2Eテスト: 70件成功
- ビルド: 成功
- `git diff --check`: 成功

設計スレッドでの再検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、122件
- `npm.cmd run test:e2e`: 成功、70件
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

補正コミット:

```text
f4360d3c46149c6e94bb21d079f0f6383d0025f8
```

補正実装済みの内容:

- 練習画面を `practice-screen`、`practice-topbar`、`practice-canvas-region`、`practice-bottom-bar`、`practice-menu` の固定構造へ変更
- 上部バーを、曲名、縦/横切り替え、練習メニューに限定
- 中央Canvas領域を最大化
- 下部バーへ、スタート/一時停止、先頭、曲の現在位置、白鍵幅/五線間隔、譜面横位置/縦位置を集約
- メニュー内へ、再生速度、メトロノーム、音量、プリカウント、端末内保存、JSON書き出し、保存一覧、ロード画面へ戻るを移動
- 練習画面から保存した後、ロード画面へ戻った際に保存一覧を再読み込みするよう補正
- 第9.5段階用E2Eを固定構造検証へ更新

補正後の実装スレッドでの検証:

- 型検査: 成功
- 単体テスト: 122件成功
- E2Eテスト: 72件成功
- ビルド: 成功
- `git diff --check`: 成功

補正後の設計スレッドでの再検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、122件
- `npm.cmd run test:e2e`: 成功、72件
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

想定するユーザー確認:

- 実鍵盤を見ながら白鍵幅と横位置を調整できるか
- Canvasと主要操作が画面内で近く、練習中に使いやすいか
- ロード画面へ戻る操作が邪魔にならず、誤操作しにくいか
- 縦表示と横表示の切り替えが練習中に使いやすいか
- メニュー内に移した操作が見つけにくすぎないか

補正後のユーザー手動確認結果:

- PC縦表示でCanvasが主役になっている: はい
- PC縦表示で白鍵幅・譜面横位置を調整しやすい: はい
- PC横表示でCanvasが主役になっている: はい
- PC横表示で五線間隔・譜面縦位置を調整しやすい: はい
- PCでメニュー内の操作が分かりやすい: いいえ
- スマートフォン縦表示でCanvasと主要操作を同時に扱いやすい: いいえ
- スマートフォン縦表示で実鍵盤を見ながら調整できそう: いいえ
- スマートフォン横表示でCanvasと主要操作を同時に扱いやすい: いいえ
- スマートフォンでロード画面へ戻る誤操作が起きにくい: はい
- スマートフォンでメニューを開閉しやすい: はい
- スマートフォンで横スクロールがない: はい
- スマートフォンの端末回転後の表示: 問題あり
- タブレット表示: 未確認

再補正方針:

- 第10段階へ進まず、第9.5再補正として扱う。
- PCとスマートフォンで配置方針を分けない。PCを小型ディスプレイとしてピアノ上へ置く運用も想定し、同じ操作体系にする。
- 通常練習モードでは、Canvasをピアノ鍵盤に近い画面下側へ大きく配置する。
- 通常練習モードで常時表示する操作は、スタート/一時停止、先頭、再生速度、練習メニュー、曲の現在位置シークに絞る。
- 曲名と縦/横切り替えは練習メニュー内へ移し、通常練習画面のCanvas表示を圧迫しない。
- 白鍵幅、譜面横位置、五線間隔、譜面縦位置は、常時表示せず、練習メニューから入る表示調整モードで扱う。
- 練習メニューは深い階層にせず、再生設定、表示設定、その他の順に並べる。
- 再生速度は使用頻度が高いため、通常練習画面へ常時表示する。

再補正コミット:

```text
89de9d69e2be5ae6d829e0f0101715990538ef04
```

再補正実装済みの内容:

- 通常練習モードを「上部の最小操作列 + シークバー + 下側Canvas」に再構成
- 曲名、縦横切替、表示調整、保存/書き出し系を練習メニュー内へ移動
- 表示調整モードを追加し、縦表示/横表示それぞれで調整UIとCanvasを表示
- 再生速度を通常画面とメニュー内の両方に配置し、即時同期
- 練習メニューを「再生設定」「表示設定」「その他」の順に整理
- スマートフォン横向きなど短い画面でCanvas高さを確保するため、上部速度入力とシークバーを調整
- 関連E2Eを新構造へ更新

再補正後の実装スレッドでの検証:

- 型検査: 成功
- 単体テスト: 122件成功
- E2Eテスト: 76件成功
- ビルド: 成功
- `git diff --check`: 成功

再補正後の設計スレッドでの再検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、122件
- `npm.cmd run test:e2e`: 成功、76件
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

再補正後のユーザー手動確認結果:

- PC縦表示の通常練習モードでCanvasが鍵盤側に大きく表示される: はい
- PC縦表示で表示調整モードを開閉し、白鍵幅・譜面横位置を調整しやすい: はい
- PC横表示の通常練習モードでCanvasが鍵盤側に大きく表示される: はい
- PC横表示で表示調整モードを開閉し、五線間隔・譜面縦位置を調整しやすい: はい
- PCで練習メニューが再生設定、表示設定、その他の順で分かりやすい: はい
- スマートフォン縦表示の通常練習モードでCanvasと最小操作を扱いやすい: いいえ
- スマートフォン縦表示で実鍵盤を見ながら表示調整できそう: テスト不可
- スマートフォン横表示の通常練習モードでCanvasと最小操作を扱いやすい: テスト不可
- スマートフォン横表示で表示調整モードを使いやすい: テスト不可
- スマートフォンで再生速度を常時操作しやすい: テスト不可
- スマートフォンでロード画面へ戻る誤操作が起きにくい: テスト不可
- スマートフォンでメニューを開閉しやすい: テスト不可
- スマートフォンで横スクロールがない: テスト不可
- スマートフォンの端末回転後の表示: テスト不可
- タブレット表示: 未確認

再々補正方針:

- 第10段階へ進まず、第9.5再々補正として扱う。
- PCは合格しているが、スマートフォンで実用不能なため、スマートフォン実機を最優先にする。
- PCとスマートフォンで、通常練習画面の基本構造と操作優先順位はそろえる。
- 再生、一時停止、先頭、練習メニューは、文字ボタンではなくアイコン中心の1列にする。
- 再生速度は大きなrangeではなく、`2倍`、`1.75倍`、`1.5倍`、`1.25倍`、`1倍`、`0.75倍`、`0.5倍` のプルダウンまたは同等のコンパクトUIにする。
- 曲の現在位置シークバーは、上下余白を削った低背の1行UIにする。
- 練習メニューは画面外に展開されないよう、固定オーバーレイ、ドロワー、またはダイアログとして画面内に収める。
- 練習画面では、画面移動時にブラウザのプルダウン更新が発生しにくいよう、ページ全体の縦スクロールとoverscrollを抑制する。
- 表示調整モードとCanvas左右端利用、保存、JSON書き出し、既存再生機能は維持する。

再々補正コミット:

```text
d9bc98fcb03212f4b48a0f4e5303c08863c8b15c
```

再々補正で実装済みの内容:

- 通常練習画面の上部操作をアイコン中心の最小操作列へ変更
- 再生速度をrangeからselectへ変更し、`2 / 1.75 / 1.5 / 1.25 / 1 / 0.75 / 0.5` に対応
- 通常画面とメニュー内の再生速度を同期
- シークバーを低背の1行UIへ調整
- 練習メニューを画面内固定オーバーレイ化し、内部スクロールに限定
- 練習画面のみ `overscroll-behavior` とCanvas `touch-action` を調整
- メニュー閉状態で内容が表示されないよう明示修正
- 関連E2Eを新UI仕様へ更新

実装スレッドでの検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、14 files / 122 tests passed
- `npm.cmd run test:e2e`: 成功、76 passed
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

この時点ではAndroid Chrome実機確認へ進む予定だったが、追加確認で次の問題が分かったため、先に追加補正を行う。

再々補正後の追加確認で分かった問題:

- 表示調整モードで、スライダーのセクションが大きすぎてCanvasがほとんど見えない。
- Canvasを見ながら白鍵幅、譜面横位置、五線間隔、譜面縦位置を調整する目的に対して、現在の表示調整モードは実運用に合っていない。
- Canvasの位置と大きさは通常練習画面のまま固定し、表示調整UIだけを半透明のフローティングパネルとして重ねる方針にする。
- スマートフォンでは画面が狭く、テンポが速い課題で音符の移動を目で追いにくい。
- 再生速度やBPMを変えずに、Canvas上の時間方向だけを圧縮または拡大する「音価の幅」調整が必要である。

追加補正方針:

- 第10段階へ進まず、第9.5追加補正として扱う。
- 表示調整モードはCanvasを押し下げない。通常練習画面と同じCanvas配置の上に、半透明のフローティングパネルを表示する。
- フローティングパネルは、文字、スライダー、ボタン自体は読みやすくし、背景だけを半透明にする。
- 縦表示の表示調整には、白鍵幅、譜面横位置、音価の幅を含める。
- 横表示の表示調整には、五線間隔、譜面縦位置、音価の幅を含める。
- 音価の幅は表示専用の時間方向倍率であり、BPM、再生速度、`currentBeat`、メトロノーム、プリカウント、楽曲JSONを変更しない。
- 値を小さくすると、音符ブロックの時間方向の長さと移動量が圧縮され、速いテンポでも目で追いやすくなる。
- 表示調整値は可能であれば端末内設定として保存する。

追加補正コミット:

```text
a231808acb0c3d7eac2067da983c90a68a377fcb
```

追加補正で実装済みの内容:

- 表示調整パネルをCanvas上の半透明フローティング表示へ変更
- 縦表示・横表示に「音価の幅」50〜200%調整を追加
- 音価の幅を縦表示の縦方向、横表示の横方向の `pixelsPerBeat` に反映
- `currentBeat`、`displayBeat`、BPM、再生速度、メトロノーム、プリカウント、JSON形式は変更なし
- 音価の幅をlocalStorage設定へ保存・復元
- Canvasサイズが表示調整モードで変わらないこと、音価の幅が反映・復元されることをテスト追加

実装スレッドでの検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、14 files / 124 tests
- `npm.cmd run test:e2e`: 成功、76 tests
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

次は、PCとAndroid Chrome実機で、表示調整フロート、音価の幅、通常練習画面の使いやすさを確認する。

追加補正後のユーザー確認結果:

- PC縦表示の通常練習モードでCanvasが鍵盤側に大きく表示される: はい
- PC縦表示で表示調整モードを開閉し、白鍵幅・譜面横位置を調整しやすい: いいえ
- PC横表示の通常練習モードでCanvasが鍵盤側に大きく表示される: はい
- PC横表示で表示調整モードを開閉し、五線間隔・譜面縦位置を調整しやすい: いいえ
- PCで練習メニューが再生設定、表示設定、その他の順で分かりやすい: はい
- スマートフォンの最小操作、再生速度、現在位置シーク、通常練習モードは概ね改善した。
- スマートフォンで練習メニューが画面内に収まり、全項目へ到達できる: いいえ
- スマートフォンの表示調整モードでCanvasが押し下げられず、半透明フロート越しにCanvasを見られる: いいえ
- スマートフォン横表示で表示調整モードを使いやすい: いいえ
- スマートフォンで音価の幅を小さくすると、音符の動きを目で追いやすい: いいえ
- 画面移動時のプルダウン更新、横スクロール、端末回転は問題なし。
- Androidタブレット表示: 問題あり。

追加確認で分かった設計判断:

- 富士通のWindowsタブレットをレッスン用にしている教室があるため、PC、Windowsタブレット、Androidタブレット、スマートフォンを分けたレスポンシブ調整が必要である。
- 以前の「PCとスマートフォンで基本配置をそろえる」方針は緩める。
- 機能名、設定値、保存形式、操作優先順位は共通にしつつ、画面サイズと入力方式に応じてレイアウトは分けてよい。
- スマートフォンの練習メニューは、画面幅100%ではなく80%程度のドロワーにし、残りの背景部分をタップすると閉じる方式を検討する。
- メニューを閉じるために最上部へ戻らなくてよいようにする。
- 表示調整フロートはCanvas基準ではなく、端末表示領域または練習画面全体を基準に配置する。
- スマートフォン横向きでは、フロートを再生ボタン列付近まで上げ、Canvasの主要部分を残す。
- PCやタブレットでは、スマートフォン用の詰め込みUIをそのまま使わず、画面幅を活かしたサイドパネルまたは小さなフロートを検討する。
- 音価の幅は、フロートがCanvasを覆っていて効果を確認できなかったため、まずフロート位置と透明度を直して再確認する。

端末別レイアウト補正方針:

- 第10段階へ進まず、第9.5端末別レイアウト補正として扱う。
- OSやUser-Agentではなく、画面幅、高さ、向き、タッチ入力の有無を基準にレイアウトを分岐する。
- スマートフォン向け、短い横向き画面向け、タブレット/PC向けの代表レイアウトを分ける。
- 既存機能、データ形式、設定保存、再生タイムラインは変更しない。

端末別レイアウト補正コミット:

```text
4454a04b148aeeb129551b2e55b2f3c7e8ae8fb8
```

端末別レイアウト補正で実装済みの内容:

- スマートフォン幅の練習メニューを80%程度の右ドロワー化
- メニュー外背景タップで閉じられる処理を追加
- メニュー閉じるボタンをメニュー内スクロール中も到達しやすい固定表示へ調整
- 表示調整フロートをCanvas基準から画面基準の固定配置へ変更
- PC/タブレットでは右側サイド寄せの表示調整パネルに変更
- スマートフォン横向きでは上部寄せ・横並び3項目で、Canvas全域を覆わない配置に調整
- フロート背景の半透明度を強め、操作部の可読性は維持
- 既存の音価の幅50〜200%、保存形式、再生タイムライン、JSON形式は変更なし

実装スレッドでの検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、14 files / 124 tests
- `npm.cmd run test:e2e`: 成功、82 tests
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

端末別レイアウト補正後のユーザー確認結果:

- PC縦表示、PC横表示の通常練習モードと表示調整モード: 問題なし
- PCで練習メニューが再生設定、表示設定、その他の順で分かりやすい: はい
- PC/Windowsタブレット向けに、スマートフォンとは別の余裕ある配置になっている: はい
- スマートフォンの最小操作列、再生速度、現在位置シーク、練習メニュー、表示調整フロート: 問題なし
- スマートフォンで音価の幅を小さくすると、音符の動きを目で追いやすい: はい
- スマートフォンで横スクロールなし、端末回転後の表示: 問題なし
- Androidタブレット表示: 問題なし
- Windowsタブレット表示: 問題なし

追加で分かった改善点:

- スマートフォン横向きの縦表示では、画面下部の簡易鍵盤ガイドが高すぎ、音符が流れる有効領域を少し圧迫している。
- このアプリは画面上の鍵盤をタップして演奏する音楽ゲームではないため、簡易鍵盤ガイドは「実鍵盤との位置合わせの目安」として見えればよい。
- タブレットでは現状の高さでも大きな問題はなく、補正対象はスマートフォン横向きの縦表示に限定する。

追加補正方針:

- 第10段階へ進む前の小補正として扱う。
- スマートフォン横向きの縦表示だけ、簡易鍵盤ガイドの高さを現在の50%から60%程度へ圧縮する。
- 白鍵幅、譜面横位置、黒鍵と白鍵の相対位置、音符ブロックの横座標、再生タイムライン、音価の幅、JSON形式は変更しない。
- スマートフォン縦向き、Androidタブレット、Windowsタブレット、PCでは現状の見え方を維持する。
- 補正後、スマートフォン横向きの縦表示でCanvas領域が広がり、実鍵盤との位置合わせの目安としては十分に見えることを確認する。

スマートフォン横向き鍵盤ガイド高さ補正コミット:

```text
a117da01bd955c0e5a6bed9b900a849c6b5aa879
```

補正で実装済みの内容:

- スマートフォン横向き相当、幅900px以下、高さ480px以下、横向きの縦表示だけ、簡易鍵盤ガイド高さを `0.56` 倍へ圧縮
- 白鍵幅、譜面横位置、白鍵/黒鍵の横位置、音符X座標、再生タイムライン、音価の幅は維持
- Canvas dataset に鍵盤ガイド高さとスケールを出し、E2Eで確認可能化
- 単体テストで、圧縮時も横方向の鍵盤幾何と音符位置が変わらないことを確認
- E2Eで、844×390では圧縮、390×844と1024×768では通常高さを維持することを確認

実装スレッドでの検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、126件
- `npm.cmd run test:e2e`: 成功、84件
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

次は、スマートフォン横向きの縦表示で、薄い鍵盤ガイドとして十分見えるかを実機確認する。

スマートフォン横向き鍵盤ガイド高さ補正後のユーザー確認結果:

- スマートフォン横向き縦表示で鍵盤ガイドが低くなった: はい
- 鍵盤ガイドが低くても白鍵・黒鍵の位置関係が分かる: はい
- 音符が流れる領域が広くなって見やすい: はい
- 実鍵盤との白鍵幅合わせ: 問題なし
- 実鍵盤との横位置合わせ: 問題なし
- 音符ブロックの横位置: 問題なし
- 再生操作と練習メニュー: 問題なし
- 表示調整モードと音価の幅: 問題なし
- スマートフォン縦向き・タブレット・PCへの悪影響: なし
- 横スクロール: なし
- 使用端末: Android Chrome

第9.5段階は完了とする。

### 第10段階: 教室での手動検証とMVP仕上げ

状態: 実装開始待ち

主な内容:

- 手動テスト
- 教育実験記録
- 不具合修正
- 説明文の改善
- MVP完成条件の確認

第10段階の方針:

- 大きな新機能を追加しない。
- まず `spec.md` のMVP完成条件と実装済み機能を照合する。
- 自動テスト、ビルド、サブパス配置、主要E2Eを再確認する。
- 先生や生徒に見せる前に、明らかに分かりにくい文言、古い進捗説明、壊れたリンクを修正する。
- 教室試用で使う記録テンプレートと確認手順を整える。
- 音源再生、演奏判定、MIDI、マイク、クラウド同期は実装しない。

ユーザーが行う予定の内容:

- 先生と試用する
- 子供または初心者の反応を記録する
- 技術的な不具合と教育上の改善案を分けて報告する

## 7. 実装スレッドから報告を受け取ったとき

ユーザーは、実装スレッドの報告内容を完全に理解してから共有する必要はない。

次のような技術用語が含まれていても、そのまま設計スレッドへ貼り付けてよい。

- typecheck
- unit test
- E2E
- build
- dependency
- Vite base
- Playwright browser
- warning
- skipped

設計スレッドが内容を整理し、次を日本語で説明する。

- 予定どおり完了したか
- 問題があるか
- ユーザーによる確認が必要か
- 次に実装スレッドへ何を依頼するか

## 8. 各段階で設計スレッドが提供するもの

設計スレッドは、各段階の開始時に必ず次を提供する。

### 実装スレッド向け

- コピーして送れる実装指示
- 作業範囲
- 実装しない項目
- 完了条件
- 必要なテスト

### ユーザー向け

- 今回何が追加されるか
- ユーザーが今すること
- 作業中に質問された場合の対応
- 完了後に確認する内容
- 操作手順
- 正常な状態
- 問題が起きた場合に共有する情報

ユーザー向けの説明は、ソフトウェア開発の初心者を前提とし、コマンド名だけを示して終わらせない。

## 9. 状態の表記

各段階の状態は次のいずれかとする。

- 未着手
- 指示準備中
- 実装開始待ち
- 実装スレッドで作業中
- 設計スレッドで確認中
- ユーザー確認待ち
- 修正中
- 完了
- 保留

状態変更は設計スレッドがこの資料へ反映する。
