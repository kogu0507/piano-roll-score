# 第9.5段階 ユーザー確認手順

確認状態: 完了。端末別レイアウト補正はPC、スマートフォン、Androidタブレット、Windowsタブレットで確認済み。スマートフォン横向きの縦表示における簡易鍵盤ガイド高さ補正もAndroid Chrome実機で確認済み。

直近の再々補正実装コミット:

```text
d9bc98fcb03212f4b48a0f4e5303c08863c8b15c
```

追加補正実装コミット:

```text
a231808acb0c3d7eac2067da983c90a68a377fcb
```

端末別レイアウト補正実装コミット:

```text
4454a04b148aeeb129551b2e55b2f3c7e8ae8fb8
```

直近の追加補正:

- スマートフォン横向きの縦表示だけ、画面下部の簡易鍵盤ガイド高さを現在の50%から60%程度へ圧縮する。
- 画面上の鍵盤はタップ用ではなく、実鍵盤との位置合わせ用の目安として扱う。
- 白鍵幅、横位置、音符ブロックの横座標、再生タイムライン、音価の幅は変更しない。

スマートフォン横向き鍵盤ガイド高さ補正実装コミット:

```text
a117da01bd955c0e5a6bed9b900a849c6b5aa879
```

直近の再々補正に対する実装スレッドでの検証:

- 型検査: 成功
- 単体テスト: 14 files / 122 tests passed
- E2Eテスト: 76 passed
- ビルド: 成功
- `git diff --check`: 成功

追加補正に対する実装スレッドでの検証:

- 型検査: 成功
- 単体テスト: 14 files / 124 tests
- E2Eテスト: 76 tests
- ビルド: 成功
- `git diff --check`: 成功

端末別レイアウト補正に対する実装スレッドでの検証:

- 型検査: 成功
- 単体テスト: 14 files / 124 tests
- E2Eテスト: 82 tests
- ビルド: 成功
- `git diff --check`: 成功

スマートフォン横向き鍵盤ガイド高さ補正に対する実装スレッドでの検証:

- 型検査: 成功
- 単体テスト: 126件
- E2Eテスト: 84件
- ビルド: 成功
- `git diff --check`: 成功

設計スレッドでの資料更新:

- 再々補正、追加補正、端末別レイアウト補正の実装結果を `README.md`、`docs/project-roadmap.md`、`docs/implementation-plan.md`、本ファイルへ反映済み
- 端末別レイアウト補正後のユーザー確認結果を反映済み
- スマートフォン横向きの簡易鍵盤ガイド高さ補正の実装結果を資料へ反映済み
- スマートフォン横向きの簡易鍵盤ガイド高さ補正後のユーザー確認結果を反映済み

## 1. この確認の目的

練習画面で、譜面Canvasとよく使う操作が画面の主役になっているか確認する。

第9.5段階では、単に画面幅へ収まるだけでなく、スマートフォンやタブレットを鍵盤付近へ置いたときに、実鍵盤を見ながら白鍵幅や譜面位置を調整できるかを重視する。

端末別レイアウト補正後は、スマートフォン、Androidタブレット、Windowsタブレット、PCで、同じ機能を保ちながら画面サイズに合った配置になっているかを確認する。

追加補正後にも維持する通常練習モード:

```text
練習画面
├─ 最小操作列
│  ├─ ▶ 再生
│  ├─ ❚❚ 一時停止
│  ├─ ⏮ 先頭へ
│  └─ ☰ 練習メニュー
├─ 速度と現在位置
│  ├─ 再生速度セレクト
│  └─ 曲の現在位置シークバー
└─ Canvas
```

Canvasは、実際のピアノ鍵盤に近い画面下側へ大きく表示されることを重視する。ボタンや再生速度欄がCanvasを圧迫しないことを確認する。

追加補正後に期待する表示調整モード:

```text
表示調整モード
├─ Canvasは通常練習画面と同じ位置・大きさで固定
└─ Canvas上に半透明のフローティングパネル
   ├─ 閉じる
   ├─ 縦表示の場合
   │  ├─ 白鍵1鍵の幅
   │  ├─ 譜面の横位置
   │  └─ 音価の幅
   └─ 横表示の場合
      ├─ 五線の1間の幅
      ├─ 譜面の縦位置
      └─ 音価の幅
```

音価の幅は、再生速度やBPMではなく、Canvas上の時間方向の表示密度である。値を小さくすると、音符ブロックの時間方向が圧縮され、速いテンポでも目で追いやすくなる。

端末別レイアウト補正後に期待する表示調整モード:

- スマートフォン縦向きでは、Canvasを見ながら調整できる小さなフロートまたは同等のUIになっている。
- スマートフォン横向きでは、フロートがCanvas全域を覆わず、再生ボタン列付近を含む画面上部へ寄っている。
- PC、Windowsタブレット、Androidタブレットでは、スマートフォン用の詰め込み表示ではなく、サイド寄せまたは小型フロートでCanvasを見ながら調整しやすい。
- 表示調整フロートの背景は半透明で、背後のCanvasが見える。
- 文字、スライダー、ボタンは読める。
- 音価の幅が最初から見つけやすい。

追加補正後にも維持する練習メニュー:

```text
練習メニュー
├─ 再生設定
│  ├─ 再生速度
│  ├─ メトロノーム
│  ├─ メトロノーム音量
│  └─ プリカウント
├─ 表示設定
│  ├─ 表示調整モードを開く
│  ├─ 縦表示 / 横表示へ切り替え
│  └─ 曲情報
└─ その他
   ├─ 端末内に保存
   ├─ JSONを書き出す
   ├─ 保存一覧を開く
   └─ ロード画面へ戻る
```

端末別レイアウト補正後に期待する練習メニュー:

- スマートフォンでは、画面幅100%の全面メニューに固定されていない。
- 80%程度のドロワー、または同等に閉じやすいUIになっている。
- メニュー外の背景部分をタップする、固定された閉じるボタンを押す、などで下の方を操作した後でもすぐ閉じられる。
- PCとタブレットでは、画面幅を活かして読みやすく、押しやすい。

## 2. PCでの確認

通常は次のURLを開く。

```text
http://127.0.0.1:5173/app/?id=001
```

### 手順1: 縦表示の練習画面を確認する

縦表示を開く。

正常な状態:

- 最小操作列に、再生、一時停止、先頭、練習メニューの4つが1列で並んでいる。
- 4つのボタンが文字中心ではなく、アイコン中心でコンパクトに表示されている。
- 再生速度が大きなrangeではなく、プルダウンまたは同等のコンパクトUIになっている。
- 曲の現在位置シークバーが低背の1行UIで、Canvasを圧迫しない。
- Canvasが画面下側へ大きく表示されている。
- 曲名、縦/横切り替え、表示調整スライダーが通常練習画面を圧迫していない。
- ロード画面へ戻る操作が、練習中に誤って押しにくい位置にある。

確認すること:

- その曲を練習するために必要な操作が、探さずに使えるか。
- Canvasが実際のピアノ鍵盤に近い位置へ表示されているか。
- 画面を少し動かしても、ブラウザのプルダウン更新が起きにくいか。

### 手順2: 縦表示の表示調整モードを確認する

練習メニューから表示調整モードを開く。

正常な状態:

- 白鍵1鍵の幅と譜面横位置を、Canvasを見ながら調整できる。
- 表示調整パネルが半透明のフロートとしてCanvas上に重なり、Canvas自体を押し下げない。
- PCやタブレットでは、サイド寄せまたは小型フロートによりCanvasを広く見ながら調整できる。
- 音価の幅を変更すると、再生速度を変えずに音符ブロックの時間方向の長さと移動量が変わる。
- 調整後に閉じる操作で通常練習モードへ戻れる。
- 閉じても調整値が維持される。

### 手順3: 横表示の練習画面を確認する

横表示へ切り替える。

正常な状態:

- 最小操作列、再生速度、曲の現在位置シークバーが縦表示と同じ考え方でコンパクトに配置されている。
- Canvasが画面下側へ大きく表示されている。
- 縦表示へ戻る操作が練習メニュー内で見つかる。

確認すること:

- 横表示で五線風の見やすさが維持されているか。
- Canvasと操作欄の優先順位が縦表示と同じで分かりやすいか。

### 手順4: 横表示の表示調整モードを確認する

練習メニューから表示調整モードを開く。

正常な状態:

- 五線の1間の幅と譜面縦位置を、Canvasを見ながら調整できる。
- 表示調整パネルが半透明のフロートとしてCanvas上に重なり、Canvas自体を押し下げない。
- PCやタブレットでは、サイド寄せまたは小型フロートによりCanvasを広く見ながら調整できる。
- 音価の幅を変更すると、再生速度を変えずに音符ブロックの横方向の長さと移動量が変わる。
- 調整後に閉じる操作で通常練習モードへ戻れる。
- 閉じても調整値が維持される。

### 手順5: 練習メニューを確認する

メニュー、ハンバーガーメニュー、または折りたたみ領域を開く。

正常な状態:

- スマートフォンでもメニューが画面内に表示される。
- メニュー内部をスクロールすれば全項目へ到達できる。
- スマートフォンでは、背景タップ、固定閉じるボタン、または同等の操作で、下の方を操作した後でもすぐ閉じられる。
- 再生設定、表示設定、その他の順に並んでいる。
- 再生速度、メトロノーム、音量、プリカウントが深い階層なしに見つかる。
- 表示調整モード、縦/横切り替え、曲情報が表示設定にまとまっている。
- 保存、JSON書き出し、保存一覧、ロード画面へ戻る操作がその他にまとまっている。
- メニューを閉じられる。

確認すること:

- 再生設定の方が保存やJSON書き出しより先に見えるか。
- 階層が深すぎず、練習中に使いやすいか。

## 3. スマートフォンでの確認

スマートフォンで同じ画面を開く。

```text
http://192.168.0.162:5173/app/?id=001
```

確認すること:

1. 通常練習モードでCanvasが画面下側へ大きく表示される。
2. 再生、一時停止、先頭、練習メニューが1列に収まっている。
3. 4つの操作ボタンがアイコン中心で、Canvasを圧迫していない。
4. 再生速度がプルダウンまたは同等のコンパクトUIになっている。
5. 再生速度に 2倍、1.75倍、1.5倍、1.25倍、1倍、0.75倍、0.5倍 がある。
6. 曲の現在位置シークバーが低背で、隠れず操作しやすい。
7. 曲名や縦/横切り替えが通常練習画面を圧迫していない。
8. 練習メニューが画面内に表示され、全項目へ到達できる。
9. 練習メニューの下の方を操作した後でも、すぐ閉じられる。
10. 表示調整モードを開ける。
11. 表示調整モードでCanvasが押し下げられず、半透明フロート越しにCanvasを見られる。
12. スマートフォン横向きで、表示調整フロートがCanvas全域を覆っていない。
13. 音価の幅が見つけやすい。
14. 縦表示で白鍵幅と譜面横位置を調整しやすい。
15. 横表示で五線間隔と譜面縦位置を調整しやすい。
16. 音価の幅を小さくすると、音符の時間方向の移動が目で追いやすくなる。
17. 表示調整モードを閉じて通常練習モードへ戻れる。
18. 練習メニューが再生設定、表示設定、その他の順で分かりやすい。
19. 画面移動時にプルダウン更新が起きにくい。
20. 端末回転後も大きく崩れない。
21. ページ全体の横スクロールがない。

## 4. タブレットでの確認

タブレットがある場合は、縦向きと横向きで確認する。

タブレットがない場合は未確認でよい。

確認すること:

- Canvasが広く使えているか。
- 主要操作が遠すぎないか。
- 縦表示で1オクターブ前後の課題を鍵盤へ合わせやすそうか。
- 横表示で五線風表示と操作欄のバランスがよいか。
- スマートフォン用の詰め込み表示ではなく、タブレットの画面幅を活かせているか。
- WindowsタブレットやAndroidタブレットでも押しやすいか。

## 5. 確認結果の伝え方

次の形式で設計スレッドへ伝える。

```text
1. PC縦表示の通常練習モードでCanvasが鍵盤側に大きく表示される: はい / いいえ
2. PC縦表示で表示調整モードを開閉し、白鍵幅・譜面横位置を調整しやすい: はい / いいえ
3. PC横表示の通常練習モードでCanvasが鍵盤側に大きく表示される: はい / いいえ
4. PC横表示で表示調整モードを開閉し、五線間隔・譜面縦位置を調整しやすい: はい / いいえ
5. PCで練習メニューが再生設定、表示設定、その他の順で分かりやすい: はい / いいえ
6. PC/Windowsタブレット向けに、スマホとは別の余裕ある配置になっている: はい / いいえ
7. スマートフォンで4つのアイコン操作が1列に収まる: はい / いいえ
8. スマートフォン縦表示でCanvasと最小操作を扱いやすい: はい / いいえ
9. スマートフォンで再生速度をプルダウンまたはコンパクトUIで操作しやすい: はい / いいえ
10. スマートフォンで曲の現在位置シークバーが低背で操作しやすい: はい / いいえ
11. スマートフォンで練習メニューが画面内に収まり、全項目へ到達できる: はい / いいえ
12. スマートフォンで練習メニュー下部を操作した後でも、すぐ閉じられる: はい / いいえ
13. スマートフォンの表示調整モードでCanvasが押し下げられず、半透明フロート越しにCanvasを見られる: はい / いいえ
14. スマートフォン横表示で表示調整フロートがCanvas全域を覆っていない: はい / いいえ
15. スマートフォン縦表示で実鍵盤を見ながら表示調整できそう: はい / いいえ
16. スマートフォン横表示の通常練習モードでCanvasと最小操作を扱いやすい: はい / いいえ
17. スマートフォン横表示で表示調整モードを使いやすい: はい / いいえ
18. スマートフォンで音価の幅を小さくすると、音符の動きを目で追いやすい: はい / いいえ
19. スマートフォンで画面移動時にプルダウン更新が起きにくい: はい / いいえ
20. スマートフォンで横スクロールがない: はい / いいえ
21. スマートフォンの端末回転後の表示: 問題なし / 問題あり
22. Androidタブレット表示: 問題なし / 問題あり / 未確認
23. Windowsタブレット表示: 問題なし / 問題あり / 未確認

使用したPCブラウザ:
使用したスマートフォン:
使用したスマートフォンブラウザ:
使用したタブレット:
分かりにくかったところ:
改善してほしいところ:
```

「いいえ」または「問題あり」の場合は、どの画面で起きたか、端末の向き、期待した配置、実際の配置を分かる範囲で伝える。

## 6. スマートフォン横向き鍵盤ガイド高さ補正後の確認

確認結果:

- スマートフォン横向き縦表示で鍵盤ガイドが低くなった: はい
- 鍵盤ガイドが低くても白鍵・黒鍵の位置関係が分かる: はい
- 音符が流れる領域が広くなって見やすい: はい
- 実鍵盤との白鍵幅合わせ: 問題なし
- 実鍵盤との横位置合わせ: 問題なし
- 音符ブロックの横位置: 問題なし
- 再生操作と練習メニュー: 問題なし
- 表示調整モードと音価の幅: 問題なし
- スマートフォン縦向き・タブレット・PCへの悪影響: なし
- 横スクロール: なし
- 使用端末: Android
- 使用ブラウザ: Chrome
- 分かりにくかったところ: なし
- 改善してほしいところ: なし

追加補正後は、全項目を最初から確認し直す必要はない。特にスマートフォンを横向きにして、縦表示を確認する。

確認すること:

1. スマートフォン横向きの縦表示で、画面下部の簡易鍵盤ガイドが以前より低くなっている。
2. 簡易鍵盤ガイドが低くなっても、白鍵と黒鍵の位置関係は目安として分かる。
3. 音符が流れるCanvas領域が広くなり、以前より見やすい。
4. 白鍵1鍵の幅を調整して、実鍵盤との横幅合わせができる。
5. 譜面の横位置を調整して、実鍵盤との位置合わせができる。
6. 音符ブロックの横位置が鍵盤ガイドや実鍵盤とずれていない。
7. 再生、停止、先頭、再生速度、シーク、練習メニューが壊れていない。
8. 表示調整モード、音価の幅、練習メニューがこれまで通り使える。
9. スマートフォン縦向き、Androidタブレット、Windowsタブレット、PCで不自然に鍵盤ガイドが小さくなっていない。
10. ページ全体の横スクロールがない。

確認結果は次の形式で共有する。

```text
1. スマートフォン横向き縦表示で鍵盤ガイドが低くなった: はい / いいえ
2. 鍵盤ガイドが低くても白鍵・黒鍵の位置関係が分かる: はい / いいえ
3. 音符が流れる領域が広くなって見やすい: はい / いいえ
4. 実鍵盤との白鍵幅合わせ: 問題なし / 問題あり
5. 実鍵盤との横位置合わせ: 問題なし / 問題あり
6. 音符ブロックの横位置: 問題なし / 問題あり
7. 再生操作と練習メニュー: 問題なし / 問題あり
8. 表示調整モードと音価の幅: 問題なし / 問題あり
9. スマートフォン縦向き・タブレット・PCへの悪影響: なし / あり / 未確認
10. 横スクロール: なし / あり

使用したスマートフォン:
使用したブラウザ:
分かりにくかったところ:
改善してほしいところ:
```
