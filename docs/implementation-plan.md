# 実装計画

## 1. この資料の役割

この資料は、実装スレッドが現在取り組む作業範囲を定める。

製品仕様は `spec.md`、作業ルールは `AGENTS.md`、全体進行は `docs/project-roadmap.md` を参照する。この資料と `spec.md` が矛盾する場合は `spec.md` を優先し、矛盾を設計スレッドへ報告する。

作業開始前に次を読むこと。

1. `AGENTS.md`
2. `README.md`
3. `spec.md`
4. `docs/project-roadmap.md`
5. `docs/implementation-plan.md`
6. 作業対象に関連する既存コードとテスト

## 2. 現在の実装段階

現在は **第9.5段階: 練習画面レイアウト再設計の再々補正** に進む。

第6段階までで、縦表示と横表示の再生タイムライン共有、スタート、一時停止、先頭に戻す、シーク、再生速度変更、縦横切り替え時の現在位置維持まで完了した。PCとAndroid Chromeでのユーザー手動確認でも問題なしだった。

第7段階では、既存の再生タイムラインへWeb Audio APIによるメトロノーム音と、なし・1小節・2小節のプリカウントを追加した。

ユーザー手動確認で、Android Chromeでは問題が出ていない一方、PCでは再生速度を上げた後に画面スクロールがメトロノーム音より少し遅れることがあると報告された。第7段階の小補正として同期基準時刻をそろえる修正を行ったが、PCではなお違和感が残った。

原因は単純な実装バグだけではなく、赤い判定線と「1拍目から画面が動き出す」見え方が、音楽ゲーム的な完全同期を期待させていることにあった。第7.5段階では、MVPの教育目的に合わせて、判定線を弱い再生ガイドへ変更し、プリカウント中も譜面を助走表示で流した。PCとAndroid Chromeでのユーザー手動確認も完了し、問題なしだった。

第8段階では、利用者が明示的に保存した楽曲をIndexedDBへ保存し、表示・再生設定をlocalStorageへ保存する実装を行った。PCとAndroid Chromeでのユーザー手動確認も完了し、問題なしだった。

第9段階では、既存機能を大きく増やさず、ロード画面、縦表示、横表示、保存一覧、再生操作欄、設定調整UIを、スマートフォン、タブレット、PCで無理なく操作できるように調整した。

第9段階の実装により、画面幅への追従、タッチ領域、横スクロール抑制は改善された。一方で、スマートフォンやタブレットを鍵盤付近へ置いて使う実運用では、練習中の主役であるCanvasと、再生・表示調整操作が画面の主役になっていない。第9.5段階では、教室テスト前に練習画面の情報設計を見直し、Canvasと主要操作を常時使いやすい位置へ再配置する。

第9.5実装コミット `bf3e630e2ed30d6df23dece711561db600c054ef` は、主要操作をCanvas近くへ追加した点では前進したが、意図していた固定構造である「上部: 曲名 + 縦/横切替 + メニュー」「中央: Canvas最大表示」「下部: 再生・表示調整バー」にはなっていなかった。第9.5補正コミット `f4360d3c46149c6e94bb21d079f0f6383d0025f8` で、練習画面を `practice-screen`、`practice-topbar`、`practice-canvas-region`、`practice-bottom-bar`、`practice-menu` の固定構造へ変更した。

補正後のユーザー確認では、PC表示は概ね成立した一方、スマートフォンでは操作欄がCanvasを圧迫し、シークバーや表示調整が扱いにくく、端末回転後にも問題が出た。したがって、第10段階へ進まず、第9.5再補正として通常練習モードと表示調整モードを分ける方針にした。PCを小型ディスプレイとしてピアノ上へ置く運用も想定し、PCとスマートフォンで基本配置を分けない。

第9.5再補正コミット `89de9d69e2be5ae6d829e0f0101715990538ef04` で、通常練習モード、表示調整モード、練習メニューの再整理を実装した。PCでの手動確認は合格したが、スマートフォンでは操作ボタン、再生速度欄、シークバーがCanvasを圧迫し、練習メニューも画面外になったためテスト継続不能だった。したがって、第10段階へ進まず、第9.5再々補正としてスマートフォン実機で使える最小操作列へ補正する。

この段階では、音符に対応するピアノ音源再生、演奏判定、マイク入力、MIDI入力、クラウド同期、ユーザー登録、外部サーバー保存、新しい保存機能は実装しない。

## 3. 第6段階までの確定結果

第6段階の実装コミット:

```text
170ac5a5d91737e6df4a75c9e785acb9697e00c1
```

設計スレッドで再確認済み:

- 型検査: 成功
- 単体テスト: 103件成功
- E2Eテスト: 50件成功
- ビルド: 成功
- `git diff --check`: 成功
- 作業ツリー: クリーン
- 仕様上の未決事項: なし

ユーザー手動確認結果:

- 縦表示のスタート: 問題なし
- 縦表示の一時停止・再開: 問題なし
- 先頭に戻す: 問題なし
- シーク: 問題なし
- 再生速度変更: 問題なし
- 横表示への切り替えと現在位置維持: 問題なし
- 横表示の再生: 問題なし
- ロード画面へ戻る: 問題なし
- スマートフォン表示: 問題なし
- 使用スマートフォン: Android
- 使用ブラウザ: Chrome
- 分かりにくい箇所: なし
- 改善要望: なし

## 4. 第7段階の目的

第7段階の目的は、視覚的な再生に拍の音を加え、利用者が入るタイミングを取りやすくすることである。

具体的には次を実現する。

- メトロノームON/OFFを切り替えられる。
- 1拍目は高い音、その他の拍は低い音にする。
- メトロノーム音量を調整できる。
- 再生速度変更にメトロノーム音も追従する。
- プリカウントを「なし」「1小節」「2小節」から選べる。
- プリカウント中は譜面を進めず、カウント後に既存の再生を開始する。
- 縦表示と横表示のどちらでも同じメトロノーム・プリカウント設定を使う。

## 5. Web Audio APIの扱い

### 5.1 初期化

- AudioContextは、利用者の操作後に初期化または `resume()` する。
- ページ表示時に自動で音を鳴らそうとしない。
- ブラウザの自動再生制限によりAudioContextが `suspended` になる場合を想定する。
- 音を開始できない場合は、画面が壊れないようにし、可能なら「もう一度スタートを押してください」程度の回復可能な案内にする。

### 5.2 音の作り方

- 外部音源ファイルは使わず、Web Audio APIの `OscillatorNode` と `GainNode` などで短いクリック音を生成する。
- 1拍目は高い音、その他の拍は低い音にする。
- 音は短く、長く残らないようにする。
- 音量は0から100%相当の範囲で調整できるようにする。
- 音量の初期値は大きすぎない値にする。推奨は50%から60%程度である。

### 5.3 スケジューリング

- メトロノームは `requestAnimationFrame` だけに依存しない。
- Web Audio APIの時刻を使い、可能な範囲で少し先の拍をスケジュールする。
- 先読み幅、タイマー間隔などの実装値は、テストしやすい小さな関数として分離する。
- 再生、一時停止、先頭に戻す、シーク、再生速度変更、タブ非表示に追従して、不要な予約音が鳴り続けないようにする。

## 6. プリカウント仕様

### 6.1 選択肢

プリカウントは次の3択とする。

- なし
- 1小節
- 2小節

この段階では設定を永続保存しない。永続保存は第8段階で扱う。

### 6.2 小節数と拍数

- 1小節あたりの拍数は、楽曲データの `timeSignature.numerator` を基準にする。
- 現在のデータ形式では音符の `time` と `duration` は拍単位で扱っているため、4/4なら1小節は4拍、3/4なら1小節は3拍として扱う。
- `timeSignature.denominator` は表示上の拍子情報として保持するが、第7段階のプリカウント長計算では `numerator` を基準にする。

### 6.3 開始時の挙動

- 停止中または終了後に「スタート」を押したとき、プリカウントが「1小節」または「2小節」なら、まずプリカウント状態へ入る。
- プリカウント中は `currentBeat` を進めない。
- プリカウントが終わったら、プリカウント開始前の `currentBeat` から再生を開始する。
- 一時停止からの再開では、原則としてプリカウントを挟まず、その位置から再開する。停止中から始めるときだけプリカウントを使う。
- 「先頭に戻す」を押した後は停止状態なので、次のスタートではプリカウント対象になる。

### 6.4 プリカウント中のUI

- 状態表示に「プリカウント中」と分かる表示を出す。
- 可能であれば、現在のカウント拍を表示する。
- プリカウント中に一時停止または先頭に戻す操作をした場合は、プリカウントをキャンセルする。
- プリカウント中に縦横表示を切り替えても、アプリが壊れないようにする。可能な範囲でプリカウント状態を維持するが、実装上難しい場合は安全に停止してよい。その場合は完了報告に判断を記載する。

## 7. UI要件

既存の再生操作欄に、少なくとも次を追加する。

- メトロノームON/OFF
- メトロノーム音量
- プリカウント選択
- プリカウント中の状態表示

名称は初心者が意味を取りやすい日本語にする。

例:

- メトロノーム
- 音量
- プリカウント
- なし
- 1小節
- 2小節
- カウント中

スマートフォンでも押しにくくならないよう、既存の再生操作欄を詰め込みすぎない。必要に応じて折り返しやグルーピングで整理する。

## 8. 既存機能との関係

維持すること:

- 縦表示と横表示の共通 `currentBeat`
- スタート、一時停止、先頭に戻す、シーク、再生速度変更
- 縦表示で音符が上から下へ流れること
- 横表示で音符が右から左へ流れること
- 縦横切り替え時の現在位置維持
- タブ非表示時の自動一時停止
- 縦表示の白鍵幅調整と横位置調整
- 横表示の五線間隔調整と譜面縦位置調整
- ロード画面へ戻ったときのJSONと検証結果保持
- C♯とD♭の扱い
- 高DPI Canvas、リサイズ、端末回転対応
- ページ全体に横スクロールを発生させないこと

壊してはいけない分離:

- シークは曲の現在位置を変える操作である。
- 縦表示の白鍵幅・横位置調整は表示調整である。
- 横表示の五線間隔・譜面縦位置調整は表示調整である。
- メトロノームとプリカウントは再生補助であり、音符のピアノ音源再生ではない。

## 9. 第7段階で実装しないもの

- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- 設定の永続保存
- 端末内への楽曲保存
- ローカル操作ログ
- 本格的な楽譜組版
- 低音部譜表または大譜表風背景
- 音色の詳細な選択
- 教室向けの記録機能

## 10. 推奨ファイル

既存構成に合わせ、次を基準とする。

```text
src/
  audio/
    metronome.ts
  core/
    metronome-timing.ts
    timeline.ts
  playback/
    playback-controller.ts
  ui/
    playback-controls.ts
```

実際の責務に応じて調整してよい。Web Audio APIに直接依存する部分と、拍・プリカウント・スケジュール計算のピュアな部分は分離する。

## 11. 単体テスト

少なくとも次を確認する。

- `timeSignature.numerator` から1小節の拍数を計算できる。
- プリカウントなし、1小節、2小節のカウント拍数を計算できる。
- 4/4、3/4などでプリカウント拍数が変わる。
- 小節内の1拍目をアクセント拍として判定できる。
- 1拍目は高音、それ以外は低音として分類できる。
- BPMと再生速度からメトロノーム間隔を計算できる。
- 再生速度変更時に次の拍間隔が変わる。
- プリカウント開始、プリカウント終了、再生開始の状態遷移を確認できる。
- プリカウント中は `currentBeat` が進まない。
- プリカウント完了後は元の `currentBeat` から再生が始まる。
- 一時停止、先頭に戻す、シークで不要なプリカウントまたはメトロノーム予約が残らないように扱える。

実際に音が鳴ったかどうかは単体テストの主要判定にしない。Web Audio API部分は薄くし、スケジュール対象の時刻、音種別、音量などをテストできる形にする。

## 12. E2Eテスト

PC幅とスマートフォン幅で少なくとも次を確認する。

- メトロノームON/OFFを切り替えられる。
- メトロノーム音量を変更できる。
- プリカウントを「なし」「1小節」「2小節」から選べる。
- プリカウントありでスタートすると、まず「プリカウント中」などの状態になる。
- プリカウント中は現在位置が進まない。
- プリカウント後に再生が始まり、現在位置が進む。
- 一時停止からの再開が壊れない。
- 再生速度変更後も再生操作が壊れない。
- 縦表示と横表示を切り替えても操作欄が使える。
- スマートフォン幅でも主要操作ができる。
- ページ全体の横スクロールが発生しない。

E2Eでは実際の音声出力を厳密に判定しない。ブラウザやOSの音量、権限、再生制限の影響があるため、UI状態と再生状態を中心に確認する。

## 13. 第7段階の完了条件

1. メトロノームON/OFFを切り替えられる。
2. メトロノーム音量を変更できる。
3. 再生中にメトロノーム音が鳴る。
4. 1拍目が高い音、その他の拍が低い音になる。
5. 再生速度変更にメトロノーム音が追従する。
6. プリカウントを「なし」「1小節」「2小節」から選べる。
7. プリカウント中は譜面が進まない。
8. プリカウント完了後に再生が始まる。
9. 一時停止、先頭に戻す、シーク、タブ非表示で音や状態が不自然に残らない。
10. 縦表示と横表示のどちらでも同じ設定を使える。
11. 既存の第6段階までの再生操作を壊さない。
12. 縦表示と横表示の表示調整を壊さない。
13. ページ全体の横スクロールを発生させない。
14. 型検査が成功する。
15. 単体テストが成功する。
16. E2Eテストが成功する。
17. ビルドが成功する。
18. `git diff --check` が成功する。
19. 第7段階の変更がローカルコミットとして保存される。
20. コミットIDと検証結果が完了報告に記載される。

## 14. 検証コマンド

```text
npm run typecheck
npm run test
npm run test:e2e
npm run build
git diff --check
```

Windows PowerShellで実行ポリシーにより `npm` が失敗する場合は、`npm.cmd` を使用してよい。

## 15. 推奨コミット

```text
feat: add metronome and precount
```

GitHubリモートの追加、push、公開は行わない。

## 16. 現在の手動確認

第7段階は次のローカルコミットで実装済みである。

```text
a54efd484a1584df8ae2f7e206187f51f11b1d41
```

設計スレッドで再確認済み:

- 型検査: 成功
- 単体テスト: 111件成功
- E2Eテスト: 52件成功
- ビルド: 成功
- `git diff --check`: 成功
- 作業ツリー: クリーン
- 仕様上の未決事項: なし

ユーザー手動確認結果:

- メトロノームONで音が鳴る: 問題なし
- 1拍目とその他の拍の違い: 問題なし
- メトロノームOFF: 未確定。回答欄に「問題なし / 問題あり」が残っているため、補正後に再確認する。
- 音量調整: 問題なし
- 再生速度変更への追従: PCで問題あり。再生速度を上げると、画面のスクロールが少し遅れる時がある。毎回ではない。
- 1小節プリカウント: 問題なし
- 2小節プリカウント: 問題なし
- 横表示でのメトロノームとプリカウント: PCで問題あり。再生速度変更への追従で同様の現象。
- ロード画面へ戻る: 問題なし
- スマートフォン表示と音: 問題なし
- 使用スマートフォン: Android
- 使用ブラウザ: Chrome
- スマートフォンでは、メトロノームと譜面移動のズレは確認されなかった。
- 音量について気になったこと: なし
- 分かりにくい箇所: なし
- 改善要望: なし

補正コミット:

```text
c11b7b557a469dba513899a255b3f76da29294c1
```

補正内容:

- `PlaybackController.setPlaybackRate()` で、`tick`、`anchorBeat`、`anchorTimeMs`、`precountAnchorTimeMs`、メトロノーム再スケジュールが同じ `nowMs` を使うよう補正
- `start`、`pause`、`resetToStart`、`seek` も単一基準時刻へ整理
- メトロノームON/OFF・音量変更時も、再生中なら現在時刻まで進めてから再スケジュール
- 速度変更時の `currentBeat` とメトロノーム次拍計算が新速度で揃う単体テストを追加
- 縦表示・横表示のE2Eで、メトロノームON状態の再生中に速度を上げても `currentBeat` が進み続けることを確認

設計スレッドで再確認済み:

- 型検査: 成功
- 単体テスト: 112件成功
- E2Eテスト: 52件成功
- ビルド: 成功
- `git diff --check`: 成功
- 作業ツリー: クリーン
- 仕様上の未決事項: なし

第7段階補正の手動確認で残った違和感は、第7.5段階で再生ガイドと助走表示へ変更することで解消した。第7段階と第7.5段階は完了とする。

この段階の手動確認で重視すること:

- メトロノームの音量が大きすぎないか。
- 1拍目とその他の拍の違いが分かるか。
- 画面の動きとメトロノームのタイミングが自然か。
- プリカウントが「入る準備」として分かりやすいか。
- スマートフォンでも音が出て操作しやすいか。

## 17. 実装スレッドへの指示履歴

第7段階は実装済みである。以下は履歴として残すものであり、新しい実装スレッドへは送らない。

```text
第7段階「メトロノームとプリカウント」を実装してください。

作業開始前に AGENTS.md、README.md、spec.md、
docs/project-roadmap.md、docs/implementation-plan.md を読み、
関連する既存コードとテストを確認してください。

今回の目的は、既存の第6段階の再生タイムラインへ、
Web Audio APIによるメトロノーム音と、なし・1小節・2小節のプリカウントを追加することです。

作業範囲は docs/implementation-plan.md の第7段階です。

実装してください:

- Web Audio APIを使ったメトロノーム音
- AudioContextをユーザー操作後に初期化またはresumeする処理
- メトロノームON/OFF
- メトロノーム音量調整
- 1拍目は高い音、その他の拍は低い音にすること
- BPMと再生速度に追従するメトロノーム間隔
- requestAnimationFrameだけに依存しない、Web Audio時刻を使った可能な範囲の先行スケジューリング
- プリカウント選択: なし、1小節、2小節
- timeSignature.numerator を基準にしたプリカウント拍数計算
- プリカウント中は currentBeat を進めないこと
- プリカウント完了後に、プリカウント開始前の currentBeat から再生を開始すること
- プリカウント中の状態表示
- 一時停止、先頭に戻す、シーク、タブ非表示時に不要な予約音やプリカウント状態が残らないこと

維持してください:

- 縦表示と横表示の共通 currentBeat
- スタート、一時停止、先頭に戻す、シーク、再生速度変更
- 縦表示で音符が上から下へ流れること
- 横表示で音符が右から左へ流れること
- 縦横切り替え時の現在位置維持
- タブ非表示時の自動一時停止
- 縦表示の白鍵幅調整
- 縦表示の横位置調整
- 横表示の五線間隔調整
- 横表示の譜面縦位置調整
- ロード画面へ戻ったときのJSONと検証結果保持
- C♯とD♭の扱い
- 高DPI Canvas、リサイズ、端末回転対応
- ページ全体に横スクロールを発生させないこと

実装しないでください:

- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- 設定の永続保存
- 端末内への楽曲保存
- 本格的な楽譜組版
- 低音部譜表または大譜表風背景
- 音色の詳細な選択

設計上の注意:

- Web Audio APIに直接依存する部分と、拍・プリカウント・スケジュール計算のピュアな部分を分離してください。
- 実際に音が鳴ったかどうかは自動テストの主要判定にしないでください。
- 自動テストでは、拍数計算、アクセント判定、プリカウント状態遷移、スケジュール対象の時刻や音種別を中心に確認してください。

テスト:

- timeSignature.numerator から1小節の拍数を計算する単体テスト
- プリカウントなし、1小節、2小節の拍数計算の単体テスト
- 1拍目アクセントとその他拍の分類の単体テスト
- BPMと再生速度からメトロノーム間隔を計算する単体テスト
- プリカウント開始、完了、再生開始の状態遷移の単体テスト
- プリカウント中に currentBeat が進まない単体テスト
- E2Eでは、メトロノームON/OFF、音量、プリカウント選択、プリカウント中表示、プリカウント後の再生開始、スマートフォン幅、横スクロールなしを確認してください

実装後は次を実行してください。

- npm run typecheck
- npm run test
- npm run test:e2e
- npm run build
- git diff --check

Windows PowerShellで npm が動かない場合は npm.cmd を使って構いません。

すべて成功したらローカルコミットし、
コミットID、主要変更ファイル、検証結果、未決事項を報告してください。

GitHubリモートの追加、公開、pushは行わないでください。
```

## 18. 第8段階 端末内保存と設定保存

### 18.1 目的

第8段階の目的は、利用者が明示的に保存した楽曲を端末内へ保存し、表示・再生設定を次回起動時に復元できるようにすることである。

この段階の保存は、クラウド保存ではない。その端末の、そのブラウザ内だけで使える保存である。ユーザー登録、ログイン、クラウド同期、外部サーバー保存は扱わない。

### 18.2 保存対象

楽曲データ:

- 利用者が明示的に保存操作をした検証済み楽曲をIndexedDBへ保存する。
- 読み込んだだけの内蔵曲、URL曲、貼り付けJSON、JSONファイルを自動保存しない。
- 保存時には曲名、保存日時、更新日時、保存IDを保持する。
- 保存データは、既存の楽曲スキーマと同じ検証フローで読み込む。
- 保存データが壊れている場合は、アプリ全体を止めず、回復可能なエラーとして表示する。

設定:

- 小さな設定値は `localStorage` へ保存する。
- 縦表示の白鍵幅と横位置を保存する。
- 縦表示の調整値は、可能であれば端末の縦向きと横向きで分けて保存する。
- 横表示の五線間隔と譜面縦位置を保存する。
- 再生速度、メトロノームON/OFF、メトロノーム音量、プリカウント設定を保存する。
- 壊れた設定値、範囲外の設定値、古い形式の設定値があっても、初期値へ戻してアプリを継続する。

### 18.3 UI要件

- ロード画面または楽曲画面から、現在の検証済み楽曲を端末内へ保存できる。
- 保存一覧を表示できる。
- 保存一覧には、曲名、保存日時または更新日時を表示する。
- 保存一覧から読み込める。
- 保存一覧から削除できる。
- 削除時は確認を出し、キャンセルできるようにする。
- 保存データがない場合は、空の状態を分かりやすく表示する。
- 「この端末のこのブラウザ内だけに保存され、別端末には同期されない」ことを画面上で明示する。
- ブラウザのデータ削除で消える可能性があるため、重要なデータにはJSON書き出しを案内する。
- 既存のJSON書き出し機能を壊さない。

### 18.4 設計方針

- IndexedDB操作は、DOMやCanvasから分離する。
- `localStorage` 操作は、設定の正規化と分離する。
- 保存形式にはバージョンを持たせる。
- 将来のスキーマ移行に備えて、保存レコードの型を明確にする。
- JSON由来、保存データ由来の文字列を `innerHTML` へ直接入れない。
- 保存一覧の操作は、既存の共通検証フローへ接続する。
- 保存処理が失敗した場合は、ユーザーに分かるエラーを表示する。
- プライベートブラウズや容量制限などで保存できない場合を想定する。

保存レコードの例:

```text
SavedSongRecord
- storageVersion
- storageId
- songId
- title
- savedAt
- updatedAt
- song
```

実装上より適切な名前や構造があれば変更してよい。ただし、保存日時、曲名、保存ID、スキーマバージョン相当は保持する。

### 18.5 実装しないもの

- ユーザー登録
- ログイン
- クラウド同期
- 外部サーバー保存
- パスワードによる曲の解除
- 外部カタログ取得
- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- 本格的な楽譜組版

### 18.6 テスト

少なくとも次を確認する。

- 保存レコードの作成、正規化、表示用メタデータを単体テストする。
- 設定値の保存、読み込み、範囲外値の補正を単体テストする。
- 壊れた `localStorage` 値があっても初期値へ戻ることを単体テストする。
- 保存一覧、保存、読み込み、削除をE2Eテストする。
- ページ再読み込み後も保存曲が残ることをE2Eテストする。
- 表示・再生設定がページ再読み込み後に復元されることをE2Eテストする。
- 読み込んだだけの曲が自動保存されないことをE2Eまたは単体テストで確認する。
- スマートフォン幅で保存UIが横スクロールを発生させないことをE2Eテストする。

IndexedDBの単体テストが難しい場合は、保存ロジックを小さなインターフェースで分離し、純粋な変換・正規化部分を単体テストする。ブラウザ実体のIndexedDBはPlaywright E2Eで確認してよい。テスト用ライブラリを追加する場合は、必要性を説明する。

### 18.7 完了条件

1. 利用者が明示的に楽曲を端末内へ保存できる。
2. 保存一覧から読み込める。
3. 保存一覧から削除できる。
4. 保存と削除の結果が画面上で分かる。
5. 端末内だけの保存であることが画面上で分かる。
6. 重要なデータにはJSON書き出しを使う案内がある。
7. 縦表示、横表示、再生関連の主要設定が復元される。
8. 読み込んだだけのデータを無断で永続保存しない。
9. 既存の内蔵曲読み込み、JSON貼り付け、JSONファイル読み込み、JSON書き出しが壊れていない。
10. PC幅とスマートフォン幅のE2Eテストが成功する。
11. 型検査、単体テスト、E2E、ビルド、`git diff --check` が成功する。
12. 第8段階の変更がローカルコミットとして保存される。

### 18.8 実装結果

実装コミット:

```text
cf8fd81405f98979c519049bd9d0fdebf66a87ec
```

実装済みの内容:

- IndexedDBによる明示的な楽曲保存、一覧表示、読み込み、削除
- 削除確認とキャンセル対応
- 端末内・同一ブラウザ限定保存である旨とJSON書き出し案内の表示
- `localStorage` による次の設定復元
  - 縦表示: 白鍵幅、横位置。縦向き、横向き別。
  - 横表示: 五線間隔、譜面縦位置。
  - 再生: 再生速度、メトロノームON/OFF、音量、プリカウント。
- 壊れた設定値の初期値復帰、範囲外値の正規化
- 保存一覧と設定復元の単体テスト、E2Eテスト

設計判断:

- `song.id` がある楽曲は同じ保存IDとして再保存時に更新扱いにした。
- `song.id` がない楽曲は初回保存時に保存IDを発行する。
- 同じ曲から複数の別バージョンを保存する「複製保存」は、MVPでは扱わず将来検討とする。

実装スレッドでの検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、14 files / 122 tests
- `npm.cmd run test:e2e`: 成功、60 passed
- `npm.cmd run build`: 成功
- `git diff --check`: 成功
- `git diff --cached --check`: 成功

設計スレッドでの再検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、14 files / 122 tests
- `npm.cmd run test:e2e`: 成功、60 passed
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

残る確認:

- 端末内だけの保存である説明の分かりやすさ
- 保存、読み込み、削除操作の分かりやすさ
- 表示・再生設定の復元の体感
- スマートフォンでの保存UIの操作性

ユーザー手動確認結果:

- 楽曲を端末内へ保存できた: はい
- 保存後に曲名と保存日時が分かる: はい
- 端末内だけの保存だと理解できる: はい
- 保存一覧から読み込めた: はい
- ページ再読み込み後も保存曲が残った: はい
- 削除確認と削除操作: 問題なし
- 縦表示の白鍵幅・横位置設定の復元: 問題なし
- 横表示の五線間隔・譜面縦位置設定の復元: 問題なし
- 再生速度・メトロノーム・プリカウント設定の復元: 問題なし
- JSON書き出し案内の分かりやすさ: 問題なし
- スマートフォン表示: 問題なし
- 使用PCブラウザ: Windows Chrome
- 使用スマートフォン: Android
- 使用スマートフォンブラウザ: Chrome
- 分かりにくかったところ: なし
- 改善してほしいところ: なし

## 19. 第9段階 レスポンシブ対応と操作性調整

### 19.1 目的

第9段階の目的は、MVPの既存機能を、スマートフォン、タブレット、PCで無理なく操作できる状態へ整えることである。

この段階では、目立つ新機能を増やすより、既存機能の使いやすさ、画面崩れ、横スクロール、タッチしにくいボタン、端末回転後の違和感を減らす。

特に、このアプリはスマートフォンを鍵盤付近へ置いて使う想定があるため、縦表示のCanvas幅、操作欄の折り返し、タッチ領域、セーフエリア、端末回転後の再描画を重視する。

### 19.2 対象画面

- ロード画面
- JSON入力欄
- 保存一覧
- 縦表示画面
- 横表示画面
- 再生操作欄
- メトロノーム、プリカウント、再生速度の操作
- 縦表示の白鍵幅、横位置調整UI
- 横表示の五線間隔、譜面縦位置調整UI
- エラー、状態メッセージ、説明文

### 19.3 調整方針

- 主要操作のタッチ領域を十分に確保する。目安として、主要ボタンや入力操作は44 CSS px程度以上の高さまたは押しやすさを確保する。
- ボタンや入力欄が狭い画面で横にはみ出さないよう、折り返し、グルーピング、縦積みを調整する。
- ページ全体の横スクロールを発生させない。
- Canvasは既存のDPR対応、リサイズ対応、端末回転対応を維持する。
- 縦表示のCanvas左右端拡張と、実鍵盤へ合わせる教育的価値を壊さない。
- 横表示の五線間隔、譜面縦位置調整を壊さない。
- 保存一覧やJSON欄が長くても、画面全体が扱いにくくならないようにする。
- ボタン、入力、スライダー、チェックボックス、セレクトには、適切なラベルまたはアクセス可能な名前を付ける。
- フォーカス表示を消さない。
- 色だけで状態を判断させない。必要な状態は文言でも示す。
- ノッチやホームインジケーターの安全領域を極端に侵さない。

### 19.4 代表ビューポート

自動テストまたは手動確認では、少なくとも次の代表条件を意識する。

- スマートフォン縦: 360×740 付近
- スマートフォン横: 844×390 付近
- タブレット縦: 768×1024 付近
- タブレット横: 1024×768 付近
- PC: 1280×720 以上

すべてを厳密なピクセル一致で確認する必要はない。主要操作ができること、横スクロールがないこと、Canvasが再描画されることを確認する。

### 19.5 実装しないもの

- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- ユーザー登録
- ログイン
- クラウド同期
- 外部サーバー保存
- PWA化、オフライン対応、ホーム画面追加
- 新しい楽曲データ形式
- 本格的な楽譜組版
- 保存データの複製管理
- 大規模なデザイン刷新

第9段階は既存UIの仕上げであり、機能追加を広げない。

### 19.6 テスト

少なくとも次を確認する。

- ロード画面がスマートフォン幅で横スクロールしない。
- 保存一覧、JSON入力、JSON書き出し、読み込み操作がスマートフォン幅で使える。
- 縦表示で、白鍵幅、横位置、Canvasドラッグ、再生操作がスマートフォン縦横で使える。
- 横表示で、五線間隔、譜面縦位置、再生操作がスマートフォン縦横で使える。
- タブレット相当の幅で、縦表示、横表示、保存一覧が崩れない。
- PC幅で、既存操作が悪化していない。
- 端末回転またはviewport変更後にCanvas内部サイズとCSSサイズが更新される。
- ページ全体に横スクロールが発生しない。
- 主要ボタンやフォームにアクセス可能な名前がある。
- 既存の保存、再生、メトロノーム、プリカウント、設定復元のE2Eを壊さない。

Playwrightでは、既存のPC幅とスマートフォン幅に加えて、必要に応じてタブレット相当またはスマートフォン横向きの代表ケースを追加する。Canvasの厳密なピクセル一致へ過度に依存しない。

### 19.7 完了条件

1. ロード画面、保存一覧、縦表示、横表示がスマートフォン幅で操作できる。
2. タブレット相当の画面幅でも主要操作ができる。
3. PC幅で既存の操作性が悪化していない。
4. 端末回転またはviewport変更後に表示が破綻しない。
5. ページ全体の横スクロールが発生しない。
6. 主要操作のタッチ領域が小さすぎない。
7. 主要ボタンや入力に分かりやすいラベルまたはアクセス可能な名前がある。
8. 既存の縦表示、横表示、再生、メトロノーム、プリカウント、保存、設定復元が壊れていない。
9. 型検査、単体テスト、E2E、ビルド、`git diff --check` が成功する。
10. 第9段階の変更がローカルコミットとして保存される。

### 19.8 実装結果

実装コミット:

```text
3f480b2669e34379a807fe620e5010a472110107
```

実装済みの内容:

- スマートフォン幅向けにロード画面、保存一覧、JSON欄、ボタン列の余白と折り返しを調整した。
- 主要ボタン、range、file input、checkbox、select のタッチ領域を44 CSS px相当以上へ調整した。
- 縦表示・横表示の操作パネルを狭い画面で縦積みしやすく調整した。
- スマートフォン横向きなど高さが短い画面でCanvas高さを調整した。
- フォーカス表示対象を入力、セレクトにも拡張した。
- ページ全体の横スクロール抑制を追加した。
- 第9段階用E2Eを追加し、スマートフォン縦横、タブレット縦横、PC相当で確認した。

実装スレッドでの検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、122 tests
- `npm.cmd run test:e2e`: 成功、66 tests
- `npm.cmd run build`: 成功
- `git diff --check`: 成功
- `git diff --cached --check`: 成功

設計スレッドでの再検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、122 tests
- `npm.cmd run test:e2e`: 成功、66 tests
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

残る確認:

- PC、スマートフォン、可能であればタブレットでの実機操作感
- ボタン、スライダー、保存一覧、再生操作欄の押しやすさ
- 端末回転後の見え方
- 横スクロールが発生しないこと

第9段階の実装により、レスポンシブ基盤は改善された。ただし、練習画面の主役であるCanvasと主要操作の優先配置は不十分なため、第9.5段階で扱う。

## 20. 第9.5段階 練習画面レイアウト再設計

### 20.1 目的

第9.5段階の目的は、スマートフォンやタブレットを鍵盤付近へ置いて使う前提で、練習画面の情報設計をMVPに耐える形へ整えることである。

第9段階では、既存UIを画面幅へ収める調整を行った。しかし、練習中に頻繁に使う操作が画面上で散らばっていると、実際の鍵盤を見ながら白鍵幅や横位置を調整する運用が難しい。第9.5段階では、「その曲のその設定で練習する」ための操作を最優先にし、曲選択やデータ操作は主画面から一段下げる。

### 20.2 配置優先順位

最優先で常時使いやすくするもの:

- 譜面Canvasまたは再生プレビュー
- スタート
- 一時停止
- 先頭に戻す
- 曲の現在位置、シーク
- 縦表示と横表示の切り替え
- 縦表示の白鍵1鍵の幅
- 縦表示の譜面横位置
- 横表示の五線の1間の幅
- 横表示の譜面縦位置

練習画面内の設定パネルまたは折りたたみでよいもの:

- 再生速度
- メトロノームON/OFF
- メトロノーム音量
- プリカウント
- 表示補助の説明
- 曲情報

ハンバーガーメニューまたは補助メニューでよいもの:

- ロード画面へ戻る
- 端末内保存
- JSON書き出し
- 保存一覧
- ヘルプ、説明
- データ操作系

### 20.3 UI方針

- 練習画面ではCanvasを画面の主役にする。
- 主要操作バーをCanvasの近くに配置し、スマートフォンでもCanvasと同時に見えるようにする。
- 縦表示では、実鍵盤を見ながら白鍵幅と譜面横位置を調整できることを優先する。
- 横表示では、五線の1間の幅と譜面縦位置を調整しやすくする。
- ロード画面へ戻る操作は、練習中に誤って押しにくい位置へ移す。
- ただし、ロード画面へ戻る導線自体は失わない。
- メニュー内に移した機能は、見つけにくくなりすぎないよう、ボタン名と説明を明確にする。
- ハンバーガーメニューまたは折りたたみメニューは、キーボード操作とスクリーンリーダー上の名前も考慮する。
- PCでは広い画面を活かし、スマートフォンでは縦積みや下部操作バーを使って、同じ優先順位を保つ。
- タブレット横向きでは、Canvasを広く見せつつ、主要操作を片側または下部へまとめることを検討する。

### 20.4 実装しないもの

- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- ユーザー登録
- ログイン
- クラウド同期
- 外部サーバー保存
- PWA化、オフライン対応、ホーム画面追加
- 新しい楽曲データ形式
- 本格的な楽譜組版
- 保存データの複製管理
- 新しい設定項目の大量追加
- 大規模なビジュアル刷新

第9.5段階は練習画面の情報設計と配置の見直しであり、機能追加を広げない。

### 20.5 テスト

少なくとも次を確認する。

- スマートフォン縦表示で、Canvas、再生操作、白鍵幅、譜面横位置が同じ練習画面内で使える。
- スマートフォン横表示で、Canvas、再生操作、五線間隔、譜面縦位置が同じ練習画面内で使える。
- ロード画面へ戻る操作が主要操作から外れ、メニューまたは補助領域から実行できる。
- メニューを開閉でき、メニュー内の項目にアクセス可能な名前がある。
- スマートフォン縦横、タブレット縦横、PC幅でページ全体の横スクロールが発生しない。
- 端末回転またはviewport変更後にCanvas内部サイズとCSSサイズが更新される。
- 既存の再生、メトロノーム、プリカウント、表示調整、保存、設定復元が壊れていない。

E2Eでは、厳密なピクセル一致ではなく、主要操作が表示され操作できること、メニュー内に移した操作へ到達できること、横スクロールがないことを確認する。

### 20.6 完了条件

1. 練習画面でCanvasが画面の主役になっている。
2. スマートフォンで、Canvasと主要操作を同時に扱いやすい。
3. 縦表示で、白鍵幅と譜面横位置を実鍵盤を見ながら調整しやすい。
4. 横表示で、五線間隔と譜面縦位置を調整しやすい。
5. スタート、一時停止、先頭に戻す、現在位置、縦横切り替えが練習中に使いやすい。
6. ロード画面へ戻る操作が練習中の誤操作になりにくい。
7. 再生速度、メトロノーム、プリカウントは設定パネルまたは折りたたみで操作できる。
8. 保存、JSON書き出し、ロード画面へ戻るなどのデータ操作へ補助メニューから到達できる。
9. スマートフォン、タブレット、PCで横スクロールが発生しない。
10. 型検査、単体テスト、E2E、ビルド、`git diff --check` が成功する。
11. 第9.5段階の変更がローカルコミットとして保存される。

### 20.7 実装結果

実装コミット:

```text
bf3e630e2ed30d6df23dece711561db600c054ef
```

実装済みの内容:

- 練習画面に主要操作パネルを追加し、再生操作、シーク、表示調整をCanvas近くへ集約した。
- 「ロード画面へ戻る」を練習メニュー内へ移動した。
- 端末内保存、保存一覧、JSON書き出しはロード画面で操作する導線としてメニュー内に明示した。
- 再生速度、メトロノーム、音量、プリカウントを「再生補助設定」の折りたたみに整理した。
- 縦表示、横表示の切り替えを主要操作として目立つ位置に維持した。
- 第9.5段階用E2Eを追加し、メニュー開閉、主要操作、横スクロールなしを確認した。

設計判断:

- 保存、JSON書き出し、保存一覧は練習画面内に複製せず、補助メニューからロード画面へ戻って操作する設計にした。
- 練習中の誤操作防止と、画面の主役をCanvasと主要操作に集中させることを優先した。

実装スレッドでの検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、14 files / 122 tests
- `npm.cmd run test:e2e`: 成功、70 tests
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

設計スレッドでの再検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、14 files / 122 tests
- `npm.cmd run test:e2e`: 成功、70 tests
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

残る確認:

- PC、スマートフォン、可能であればタブレットでの練習画面の実機操作感
- Canvasと主要操作が同時に扱いやすいか
- 実鍵盤を見ながら白鍵幅と譜面横位置を調整できそうか
- メニュー内に移した操作が見つけにくすぎないか

設計スレッドでの再判断:

- 直近実装は、主要操作をCanvas近くへ追加した点では前進している。
- しかし、Canvasが中央で最大表示され、下部に再生・表示調整バーがある構造にはなっていない。
- 練習画面内で、ヘッダー、主要操作、説明、Canvasの優先順位がまだ曖昧である。
- この状態をMVP候補とはせず、第9.5補正として画面シェルを固定する。

補正で必ず実現する構造:

```text
練習画面
├─ 上部: 曲名 + 縦/横切替 + メニュー
├─ 中央: Canvasを最大限広く表示
├─ 下部: よく使う再生・表示調整バー
│  ├─ 再生/一時停止
│  ├─ 先頭
│  ├─ 曲の現在位置
│  ├─ 白鍵幅 / 五線間隔
│  └─ 横位置 / 縦位置
└─ メニュー内
   ├─ 再生速度
   ├─ メトロノーム
   ├─ プリカウント
   ├─ 保存
   ├─ JSON書き出し
   └─ ロード画面へ戻る
```

### 20.8 第9.5段階補正の実装結果

補正コミット:

```text
f4360d3c46149c6e94bb21d079f0f6383d0025f8
```

補正実装済みの内容:

- 練習画面を明確な固定構造へ変更した。
  - `practice-screen`
  - `practice-topbar`
  - `practice-canvas-region`
  - `practice-bottom-bar`
  - `practice-menu`
- 上部バーを「曲名 + 縦/横切替 + 練習メニュー」に限定した。
- 中央Canvas領域を最大化した。
- 下部バーへ常用操作を集約した。
  - スタート / 一時停止
  - 先頭に戻す
  - 曲の現在位置
  - 縦表示: 白鍵幅 / 譜面横位置
  - 横表示: 五線間隔 / 譜面縦位置
- メニュー内へ次を移動した。
  - 再生速度
  - メトロノーム
  - メトロノーム音量
  - プリカウント
  - 端末内保存
  - JSON書き出し
  - 保存一覧を開く
  - ロード画面へ戻る
- 練習画面から保存した後、ロード画面へ戻った際に保存一覧を再読み込みするよう補正した。
- 第9.5段階用E2Eを固定構造検証へ更新した。

実装スレッドでの検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、122 tests
- `npm.cmd run test:e2e`: 成功、72 tests
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

設計スレッドでの再検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、14 files / 122 tests
- `npm.cmd run test:e2e`: 成功、72 tests
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

残る確認:

- PCで、上部バー、中央Canvas、下部操作バーの優先順位が自然か。
- スマートフォンで、Canvasと下部操作バーを同時に扱いやすいか。
- 縦表示で、実鍵盤を見ながら白鍵幅と譜面横位置を調整できそうか。
- 横表示で、五線間隔と譜面縦位置を調整しやすいか。
- メニュー内へ移した保存、JSON書き出し、保存一覧、ロード画面へ戻る操作が見つけにくすぎないか。

### 20.9 第9.5段階補正のユーザー確認結果と再補正方針

補正後のユーザー手動確認結果:

- PC縦表示でCanvasが主役になっている: はい
- PC縦表示で白鍵幅・譜面横位置を調整しやすい: はい
- PC横表示でCanvasが主役になっている: はい
- PC横表示で五線間隔・譜面縦位置を調整しやすい: はい
- PCでメニュー内の操作が分かりやすい: いいえ
- スマートフォン縦表示でCanvasと主要操作を同時に扱いやすい: いいえ
- スマートフォン縦表示で実鍵盤を見ながら調整できそう: いいえ
- スマートフォン横表示でCanvasと主要操作を同時に扱いやすい: いいえ
- スマートフォンでロード画面へ戻る誤操作が起きにくい: はい
- スマートフォンでメニューを開閉しやすい: はい
- スマートフォンで横スクロールがない: はい
- スマートフォンの端末回転後の表示: 問題あり
- タブレット表示: 未確認

確認で分かった問題:

- メニュー内の再生補助設定が一段深く、再生速度やメトロノームへたどり着く操作が多い。
- 保存、JSON書き出し、保存一覧より、再生速度、メトロノーム、プリカウントの方が練習中に使う頻度が高い。
- スマートフォンでは、操作欄がCanvasより大きく見え、Canvasが練習画面の主役になっていない。
- シークバーや現在位置が狭くなり、練習中に扱いにくい。
- 曲名と縦/横切り替えが常時表示され、スマートフォンではCanvasを圧迫している。
- 白鍵幅、譜面横位置、五線間隔、譜面縦位置は、最初に調整した後は頻繁には触らないため、常時表示の優先度は低い。
- PCも、小型ディスプレイをピアノ上へ置く運用が想定されるため、スマートフォンやタブレットと同じ配置方針にする必要がある。

再補正の基本方針:

- 第10段階へ進まず、第9.5再補正として扱う。
- PC、スマートフォン、タブレットで基本配置と操作体系をそろえる。
- 通常練習モードと表示調整モードを分ける。
- 通常練習モードでは、Canvasをピアノ鍵盤に近い画面下側へ大きく配置する。
- 通常練習モードで常時表示する操作は、スタート/一時停止、先頭、再生速度、練習メニュー、曲の現在位置シークに絞る。
- 曲名、縦/横切り替え、表示調整、保存、JSON書き出し、保存一覧、ロード画面へ戻る操作は練習メニュー内へ移す。
- 表示調整は、練習メニューから「表示調整モード」を開いたときだけ表示する。
- 表示調整モードでは、Canvasと表示調整スライダーを同時に見ながら操作できるようにする。
- 表示調整モードは、閉じる操作で通常練習モードへ戻れるようにする。
- 練習メニューは深い階層にせず、再生設定、表示設定、その他の順に並べる。
- 再生速度は使用頻度が高いため、通常練習モードにも常時表示する。

通常練習モードの想定構造:

```text
練習画面
├─ 上部の最小操作列
│  ├─ スタート / 一時停止
│  ├─ 先頭
│  ├─ 再生速度
│  └─ 練習メニュー
├─ 曲の現在位置シークバー
└─ Canvas
```

Canvasは、画面下側、つまり実際のピアノ鍵盤に近い位置へ置く。

表示調整モードの想定構造:

```text
表示調整モード
├─ 閉じる
├─ 縦表示の場合
│  ├─ 白鍵1鍵の幅
│  └─ 譜面の横位置
├─ 横表示の場合
│  ├─ 五線の1間の幅
│  └─ 譜面の縦位置
└─ Canvas
```

練習メニューの想定構造:

```text
練習メニュー
├─ 再生設定
│  ├─ 再生速度
│  ├─ メトロノーム
│  ├─ メトロノーム音量
│  └─ プリカウント
├─ 表示設定
│  ├─ 表示調整モードを開く
│  ├─ 縦表示 / 横表示へ切り替え
│  └─ 曲情報
└─ その他
   ├─ 端末内に保存
   ├─ JSONを書き出す
   ├─ 保存一覧を開く
   └─ ロード画面へ戻る
```

### 20.10 第9.5段階再補正 実装スレッドへの指示

```text
第9.5段階再補正「通常練習モードと表示調整モードの分離」を実装してください。

作業開始前に AGENTS.md、README.md、spec.md、
docs/project-roadmap.md、docs/implementation-plan.md を読み、
関連する既存コードとテストを確認してください。

背景:

第9.5補正コミット f4360d3c46149c6e94bb21d079f0f6383d0025f8 で、
練習画面は practice-screen / practice-topbar / practice-canvas-region /
practice-bottom-bar / practice-menu の固定構造になりました。

しかしユーザー手動確認では、PCは概ね成立した一方、
スマートフォンでは操作欄がCanvasを圧迫し、
実鍵盤を見ながら調整する運用に合わないことが分かりました。
また、PCも小型ディスプレイとしてピアノ上へ置く使い方が想定されるため、
PCとスマートフォンで配置方針を分けないでください。

今回の目的:

- 通常練習モードと表示調整モードを分ける
- 通常練習中はCanvasをピアノ鍵盤に近い画面下側へ大きく表示する
- 常時表示する操作を最小限にする
- 再生速度は常時表示する
- 表示調整は必要なときだけ開く
- 練習メニューを「再生設定」「表示設定」「その他」の順に整理する

通常練習モードの構造:

練習画面
├─ 上部の最小操作列
│  ├─ スタート / 一時停止
│  ├─ 先頭
│  ├─ 再生速度
│  └─ 練習メニュー
├─ 曲の現在位置シークバー
└─ Canvas

重要:

- Canvasは、画面下側、つまり実際のピアノ鍵盤に近い位置へ置いてください。
- 曲名は通常練習画面に常時表示しなくてよいです。練習メニュー内の曲情報へ移してください。
- 縦表示/横表示の切り替えは、通常練習画面に常時表示しなくてよいです。練習メニュー内の表示設定へ移してください。
- 白鍵幅、譜面横位置、五線間隔、譜面縦位置は、通常練習画面に常時表示しないでください。
- シークバーは、曲の現在位置を扱う操作として通常練習画面に残してください。

表示調整モード:

練習メニューから「表示調整モード」を開けるようにしてください。

縦表示の表示調整モード:

- 閉じる操作
- 白鍵1鍵の幅
- 譜面の横位置
- Canvas

横表示の表示調整モード:

- 閉じる操作
- 五線の1間の幅
- 譜面の縦位置
- Canvas

表示調整モードでは、Canvasを見ながら調整できることを優先してください。
調整が終わったら、閉じる操作で通常練習モードへ戻れるようにしてください。
閉じても調整値は維持し、既存のlocalStorage保存も壊さないでください。

練習メニュー:

深い階層にしないでください。
メニュー内は、見出しまたは区切りで次の順に整理してください。

1. 再生設定
2. 表示設定
3. その他

再生設定:

- 再生速度
- メトロノーム
- メトロノーム音量
- プリカウント

表示設定:

- 表示調整モードを開く
- 縦表示 / 横表示へ切り替え
- 曲情報

その他:

- 端末内に保存
- JSONを書き出す
- 保存一覧を開く
- ロード画面へ戻る

再生速度は通常練習モードにも常時表示してください。
メニュー内にも置く場合は、同じ設定値を操作していることが分かるようにし、状態の食い違いが起きないようにしてください。
通常練習モード側とメニュー側のどちらで変更しても、同じ再生速度設定へ即時反映してください。

維持してください:

- 縦表示でCanvasだけを左右端まで使えること
- 縦表示で実鍵盤へ合わせるための白鍵幅と横位置調整
- 横表示の五線間隔と譜面縦位置調整
- 共通タイムライン、再生、シーク、速度変更
- メトロノーム、音量、プリカウント
- 再生ガイドと助走表示
- 端末内保存、保存一覧、読み込み、削除
- localStorageによる設定復元
- JSON貼り付け、JSONファイル読み込み、JSON書き出し
- /app/ サブパス配置
- ページ全体に横スクロールを出さないこと

実装しないでください:

- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- ユーザー登録
- ログイン
- クラウド同期
- 外部サーバー保存
- PWA化、オフライン対応、ホーム画面追加
- 新しい楽曲データ形式
- 本格的な楽譜組版
- 保存データの複製管理
- 大規模なビジュアル刷新

テストしてください:

- PC幅、スマートフォン幅で通常練習モードが同じ基本配置になること
- 通常練習モードで、スタート/一時停止、先頭、再生速度、練習メニュー、曲の現在位置シーク、Canvasが操作できること
- 通常練習モードで、曲名、縦/横切り替え、表示調整スライダーがCanvasを圧迫しないこと
- メニューが「再生設定」「表示設定」「その他」の順に見えること
- メニュー内で再生速度、メトロノーム、音量、プリカウントへ深い階層なしに到達できること
- 表示調整モードを開閉できること
- 縦表示の表示調整モードで、白鍵幅と譜面横位置を調整できること
- 横表示の表示調整モードで、五線間隔と譜面縦位置を調整できること
- 表示調整モードを閉じても調整値が維持されること
- スマートフォン縦横、タブレット相当、PC幅でページ全体の横スクロールが発生しないこと
- 端末回転またはviewport変更後にCanvas内部サイズとCSSサイズが更新されること
- 既存の再生、メトロノーム、プリカウント、表示調整、保存、設定復元のE2Eを壊さないこと

実装後は次を実行してください。

- npm run typecheck
- npm run test
- npm run test:e2e
- npm run build
- git diff --check

Windows PowerShellで npm が動かない場合は npm.cmd を使って構いません。

すべて成功したらローカルコミットし、
コミットID、主要変更ファイル、検証結果、未決事項を報告してください。

GitHubリモートの追加、公開、pushは行わないでください。
```

### 20.11 第9.5段階再補正の実装結果

再補正コミット:

```text
89de9d69e2be5ae6d829e0f0101715990538ef04
```

再補正実装済みの内容:

- 通常練習モードを「上部の最小操作列 + シークバー + 下側Canvas」に再構成した。
- 曲名、縦横切替、表示調整、保存/書き出し系を練習メニュー内へ移動した。
- 表示調整モードを追加し、縦表示/横表示それぞれで調整UIとCanvasを表示できるようにした。
- 再生速度を通常画面とメニュー内の両方に配置し、即時同期するようにした。
- 練習メニューを「再生設定」「表示設定」「その他」の順に整理した。
- スマートフォン横向きなど短い画面でCanvas高さを確保するため、上部速度入力とシークバーを調整した。
- 関連E2Eを新構造へ更新した。

主要変更ファイル:

- `src/ui/playback-controls.ts`
- `src/ui/practice-menu.ts`
- `src/ui/vertical-screen.ts`
- `src/ui/horizontal-screen.ts`
- `src/styles/main.css`
- `tests/e2e/practice-layout-stage9-5.spec.ts`

実装スレッドでの検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、122件
- `npm.cmd run test:e2e`: 成功、76件
- `npm.cmd run build`: 成功
- `git diff --check`: 成功
- `git diff --cached --check`: 成功

設計スレッドでの再検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、14 files / 122 tests
- `npm.cmd run test:e2e`: 成功、76 tests
- `npm.cmd run build`: 成功
- `git diff --check`: 成功

残る確認:

- PC縦表示と横表示で、通常練習モードのCanvasが鍵盤側に大きく表示されるか。
- PCで、表示調整モードを開閉して各調整値を扱いやすいか。
- PCで、練習メニューが再生設定、表示設定、その他の順で分かりやすいか。
- スマートフォン縦表示と横表示で、Canvasと最小操作を扱いやすいか。
- スマートフォンで、再生速度を常時操作しやすいか。
- スマートフォンの端末回転後に大きく崩れないか。
- タブレットがあれば、PCやスマートフォンと同じ操作体系として理解できるか。

### 20.12 第9.5段階再補正のユーザー確認結果と再々補正方針

再補正後のユーザー手動確認結果:

- PC縦表示の通常練習モードでCanvasが鍵盤側に大きく表示される: はい
- PC縦表示で表示調整モードを開閉し、白鍵幅・譜面横位置を調整しやすい: はい
- PC横表示の通常練習モードでCanvasが鍵盤側に大きく表示される: はい
- PC横表示で表示調整モードを開閉し、五線間隔・譜面縦位置を調整しやすい: はい
- PCで練習メニューが再生設定、表示設定、その他の順で分かりやすい: はい
- スマートフォン縦表示の通常練習モードでCanvasと最小操作を扱いやすい: いいえ
- スマートフォン縦表示で実鍵盤を見ながら表示調整できそう: テスト不可
- スマートフォン横表示の通常練習モードでCanvasと最小操作を扱いやすい: テスト不可
- スマートフォン横表示で表示調整モードを使いやすい: テスト不可
- スマートフォンで再生速度を常時操作しやすい: テスト不可
- スマートフォンでロード画面へ戻る誤操作が起きにくい: テスト不可
- スマートフォンでメニューを開閉しやすい: テスト不可
- スマートフォンで横スクロールがない: テスト不可
- スマートフォンの端末回転後の表示: テスト不可
- タブレット表示: 未確認

確認で分かった問題:

- PCは合格している。
- スマートフォンでは、ボタンと再生速度欄が大きすぎてCanvasを圧迫している。
- 練習メニューの多くが画面外に出て、以後のテストを続けられない。
- 画面を移動しているとAndroid Chromeのプルダウン更新が発生し、練習画面として使いにくい。
- 再生速度のrange表示は、余白が大きくスマートフォン練習画面に向いていない。
- 文字ボタンは横幅と高さを消費するため、スマートフォンではアイコン中心にする必要がある。
- シークバーと再生速度は上下余白を削り、Canvasを優先する必要がある。

再々補正の基本方針:

- 第10段階へ進まず、第9.5再々補正として扱う。
- スマートフォン実機で使えることを最優先にする。PCは今回の再補正で合格しているため、PCの見た目を多少コンパクトにしてもよい。
- PCとスマートフォンで、通常練習画面の基本構造と操作優先順位はそろえる。
- 通常練習モードの最上段は、再生、一時停止、先頭に戻る、練習メニューの4ボタン1列にする。
- 4ボタンは文字ではなく、アイコン中心にする。アクセシビリティのため `aria-label` などの名前は残す。
- 再生速度はrangeではなく、候補選択式のコンパクトUIにする。
- 再生速度の候補は、`2倍`、`1.75倍`、`1.5倍`、`1.25倍`、`1倍`、`0.75倍`、`0.5倍` を基本とする。
- 曲の現在位置シークバーは低背の1行UIにし、大きなラベルや余白でCanvasを削らない。
- 練習メニューは画面外に展開されないよう、固定オーバーレイ、ドロワー、またはダイアログとして表示し、メニュー内部だけをスクロール可能にする。
- 練習画面では、ブラウザのプルダウン更新が発生しにくいよう、ページ全体の縦スクロールとoverscrollを可能な範囲で抑制する。
- ロード画面や保存一覧など、通常スクロールが必要な画面まで固定しない。

通常練習モードの再々補正後の想定構造:

```text
練習画面
├─ 最小操作列
│  ├─ ▶ 再生
│  ├─ ❚❚ 一時停止
│  ├─ ⏮ 先頭へ
│  └─ ☰ 練習メニュー
├─ 速度と現在位置
│  ├─ 再生速度セレクト
│  └─ 曲の現在位置シークバー
└─ Canvas
```

### 20.13 第9.5段階再々補正 実装スレッドへの指示

```text
第9.5段階再々補正「スマートフォン用の最小操作列とメニュー画面内表示」を実装してください。

作業開始前に AGENTS.md、README.md、spec.md、
docs/project-roadmap.md、docs/implementation-plan.md を読み、
関連する既存コードとテストを確認してください。

背景:

第9.5再補正コミット 89de9d69e2be5ae6d829e0f0101715990538ef04 で、
通常練習モードと表示調整モードを分離しました。
PC確認は合格しました。

しかしAndroid Chromeの手動確認では、
操作ボタン、再生速度欄、シークバーが大きすぎてCanvasを圧迫し、
練習メニューも画面外に出たため、以後のスマートフォン確認を続けられませんでした。
また、画面を動かすとブラウザのプルダウン更新が発生して使いにくい状態です。

今回の目的:

- スマートフォン実機で通常練習画面を使えるようにする
- PCとスマートフォンで通常練習画面の基本構造と操作優先順位をそろえる
- Canvasの表示領域を最大限確保する
- 文字ボタンをアイコン中心の最小操作列へ置き換える
- 再生速度をコンパクトな選択式UIにする
- シークバーの高さと余白を減らす
- 練習メニューを画面内に収める
- 練習画面でプルダウン更新が起きにくいようにする

通常練習モードの構造:

練習画面
├─ 最小操作列
│  ├─ ▶ 再生
│  ├─ ❚❚ 一時停止
│  ├─ ⏮ 先頭へ
│  └─ ☰ 練習メニュー
├─ 速度と現在位置
│  ├─ 再生速度セレクト
│  └─ 曲の現在位置シークバー
└─ Canvas

実装してください:

- スタート、停止、先頭、練習メニューは1列に並べる
- これらのボタンは文字中心ではなく、アイコン中心にする
- 視覚上のラベルは短く、または非表示でよい
- `aria-label` や `title` などで、E2Eとアクセシビリティ上の名前は維持する
- ボタンの高さ、幅、gap、paddingをスマートフォンで過剰に大きくしない
- 再生速度はrangeではなくselectまたは同等のコンパクトな選択UIにする
- 再生速度の候補は、2倍、1.75倍、1.5倍、1.25倍、1倍、0.75倍、0.5倍を基本にする
- 再生速度は通常練習モードとメニュー内のどちらで変更しても同期する
- 曲の現在位置シークバーは低背の1行UIにする
- 「曲の現在位置」などの長いラベルは、スマートフォンでは視覚的に短くするか、アクセシブル名だけにしてよい
- Canvasを最優先し、速度欄とシークバーの上下余白を削る

練習メニュー:

- スマートフォンで画面外に展開されないようにする
- detailsの単純展開で画面外に出る場合は、固定オーバーレイ、ドロワー、またはdialog相当のUIへ変更する
- メニュー内部だけをスクロール可能にする
- メニューを閉じる操作を明確にする
- メニュー内の順番は、再生設定、表示設定、その他を維持する
- 再生設定、表示設定、その他の見出しは画面内で見つけやすくする

プルダウン更新対策:

- 練習画面では、ページ全体の縦スクロールとoverscrollを可能な範囲で抑制する
- 例: practice screenに限定した `overscroll-behavior: none` または `contain`、必要に応じた高さ固定、内部スクロール分離
- ロード画面、保存一覧、JSON編集欄など、通常スクロールが必要な画面まで不必要に固定しない
- Canvasやスライダー操作中にページ全体が引っ張られて更新されにくいことを重視する

維持してください:

- 通常練習モードと表示調整モードの分離
- 表示調整モードの開閉
- 縦表示の白鍵幅と譜面横位置調整
- 横表示の五線間隔と譜面縦位置調整
- Canvasの左右端利用
- 共通タイムライン、再生、シーク、速度変更
- メトロノーム、音量、プリカウント
- 再生ガイドと助走表示
- 端末内保存、保存一覧、読み込み、削除
- localStorageによる設定復元
- JSON貼り付け、JSONファイル読み込み、JSON書き出し
- /app/ サブパス配置
- ページ全体に横スクロールを出さないこと

実装しないでください:

- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- ユーザー登録
- ログイン
- クラウド同期
- 外部サーバー保存
- PWA化、オフライン対応、ホーム画面追加
- 新しい楽曲データ形式
- 本格的な楽譜組版
- 大規模なビジュアル刷新

テストしてください:

- スマートフォン幅で、再生、一時停止、先頭、練習メニューが1列に収まること
- スマートフォン幅で、上記4ボタンがCanvasを過度に圧迫しないこと
- スマートフォン幅で、再生速度がselectまたはコンパクトUIとして表示されること
- 再生速度の候補に 2倍、1.75倍、1.5倍、1.25倍、1倍、0.75倍、0.5倍 があること
- 再生速度を通常練習モードとメニュー内のどちらで変更しても同期すること
- スマートフォン幅で、曲の現在位置シークバーが低背で操作できること
- スマートフォン幅で、練習メニューが画面内に収まり、内部スクロールで全項目へ到達できること
- スマートフォン幅で、ページ全体の横スクロールが発生しないこと
- スマートフォン縦横、タブレット相当、PC幅でCanvas内部サイズとCSSサイズが更新されること
- 既存の再生、メトロノーム、プリカウント、表示調整、保存、設定復元のE2Eを壊さないこと

可能であれば、練習画面のoverscroll抑制をE2EまたはDOM/CSS検査で確認してください。
実機のAndroid Chromeでのプルダウン更新そのものは自動テストで厳密判定しなくて構いません。

実装後は次を実行してください。

- npm run typecheck
- npm run test
- npm run test:e2e
- npm run build
- git diff --check

Windows PowerShellで npm が動かない場合は npm.cmd を使って構いません。

すべて成功したらローカルコミットし、
コミットID、主要変更ファイル、検証結果、未決事項を報告してください。

GitHubリモートの追加、公開、pushは行わないでください。
```

## 21. 履歴: 第9.5段階補正 実装スレッドへの指示

第9.5段階補正は実装済みである。以下は補正実装時に使った指示の履歴であり、次の実装スレッドへそのまま送らない。

```text
第9.5段階補正「練習画面シェルの固定」を実装してください。

作業開始前に AGENTS.md、README.md、spec.md、
docs/project-roadmap.md、docs/implementation-plan.md を読み、
関連する既存コードとテストを確認してください。

重要:

直近の第9.5実装コミット bf3e630e2ed30d6df23dece711561db600c054ef は、
主要操作をCanvas近くへ追加した点では前進しました。
しかし、意図していた画面構造が実現されていません。

今回の補正では、抽象的に「Canvasを主役にする」のではなく、
次の画面構造を実装してください。

練習画面
├─ 上部: 曲名 + 縦/横切替 + メニュー
├─ 中央: Canvasを最大限広く表示
├─ 下部: よく使う再生・表示調整バー
│  ├─ 再生/一時停止
│  ├─ 先頭
│  ├─ 曲の現在位置
│  ├─ 白鍵幅 / 五線間隔
│  └─ 横位置 / 縦位置
└─ メニュー内
   ├─ 再生速度
   ├─ メトロノーム
   ├─ プリカウント
   ├─ 保存
   ├─ JSON書き出し
   └─ ロード画面へ戻る

実装方針:

- 練習画面全体を、上部バー、中央Canvas領域、下部操作バー、メニューに分けてください。
- DOM上でもCSS上でも、この構造が分かる名前にしてください。
  例: practice-screen / practice-topbar / practice-canvas-region / practice-bottom-bar / practice-menu
- 上部バーには、曲名、縦表示/横表示切り替え、メニューだけを置いてください。
- 中央Canvas領域は、利用可能な高さを最大限使ってください。
- 下部操作バーには、練習中に頻繁に使う操作だけを置いてください。
- 白鍵幅/五線間隔、譜面横位置/譜面縦位置は、Canvasを見ながら調整できる下部操作バーに置いてください。
- 再生速度、メトロノーム、音量、プリカウントは、下部バーの常時表示ではなく、メニュー内または明確な設定パネル内へ移してください。
- ロード画面へ戻る、保存、JSON書き出し、保存一覧は、メニュー内に置いてください。
- 保存、JSON書き出し、保存一覧を練習画面内に直接複製しない場合でも、メニュー内から「ロード画面へ戻って操作する」ことが明確に分かる導線にしてください。
- 既存の説明文は、練習画面の主役を邪魔しない位置へ移すか、メニュー/補助説明に格納してください。

必要なら、bf3e630e2ed30d6df23dece711561db600c054ef の変更を git revert してから作り直して構いません。
ただし git reset --hard は使わないでください。
revertする場合も、その後に今回の補正実装を行い、最終的に作業ツリーをクリーンにしてください。

維持してください:

- 縦表示でCanvasだけを左右端まで使えること
- 縦表示で実鍵盤へ合わせるための白鍵幅と横位置調整
- 横表示の五線間隔と譜面縦位置調整
- 共通タイムライン、再生、シーク、速度変更
- メトロノーム、音量、プリカウント
- 再生ガイドと助走表示
- 端末内保存、保存一覧、読み込み、削除
- localStorageによる設定復元
- JSON貼り付け、JSONファイル読み込み、JSON書き出し
- /app/ サブパス配置

実装しないでください:

- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- ユーザー登録
- ログイン
- クラウド同期
- 外部サーバー保存
- PWA化、オフライン対応、ホーム画面追加
- 新しい楽曲データ形式
- 本格的な楽譜組版
- 保存データの複製管理
- 新しい設定項目の大量追加
- 大規模なビジュアル刷新

テストしてください:

- スマートフォン縦表示で、上部バー、中央Canvas、下部操作バーの構造になっていること
- スマートフォン横表示で、上部バー、中央Canvas、下部操作バーの構造になっていること
- 下部操作バーから、再生/一時停止、先頭、曲の現在位置、白鍵幅/五線間隔、横位置/縦位置を操作できること
- メニュー内から、再生速度、メトロノーム、プリカウント、ロード画面へ戻る操作へ到達できること
- 保存、JSON書き出し、保存一覧の導線がメニュー内で分かること
- スマートフォン縦横、タブレット縦横、PC幅でページ全体の横スクロールが発生しないこと
- viewport変更後にCanvas内部サイズとCSSサイズが更新されること
- 既存の再生、メトロノーム、プリカウント、表示調整、保存、設定復元のE2Eを壊さないこと

実装後は次を実行してください。

- npm run typecheck
- npm run test
- npm run test:e2e
- npm run build
- git diff --check

Windows PowerShellで npm が動かない場合は npm.cmd を使って構いません。

すべて成功したらローカルコミットし、
コミットID、主要変更ファイル、検証結果、未決事項を報告してください。

GitHubリモートの追加、公開、pushは行わないでください。
```

## 22. 履歴: 第9.5段階 実装スレッドへの指示

第9.5段階は実装済みである。以下は実装時に使った指示の履歴であり、次の実装スレッドへそのまま送らない。

```text
第9.5段階「練習画面レイアウト再設計」を実装してください。

作業開始前に AGENTS.md、README.md、spec.md、
docs/project-roadmap.md、docs/implementation-plan.md を読み、
関連する既存コードとテストを確認してください。

背景:

第9段階では、スマートフォン、タブレット、PC相当の画面幅で、
横スクロール抑制、タッチ領域、折り返し、フォーカス表示などを改善しました。

ただし、現在の練習画面は、このままMVPにするには弱いです。
スマートフォンやタブレットを鍵盤付近へ置いて使う場合、
譜面Canvasと、練習中によく使う操作が画面の主役になっている必要があります。

今回の目的は、練習画面の情報設計を見直し、
「その曲のその設定で練習する」ための操作を最優先に配置することです。

最優先で常時使いやすくしてください:

- 譜面Canvasまたは再生プレビュー
- スタート
- 一時停止
- 先頭に戻す
- 曲の現在位置、シーク
- 縦表示と横表示の切り替え
- 縦表示の白鍵1鍵の幅
- 縦表示の譜面横位置
- 横表示の五線の1間の幅
- 横表示の譜面縦位置

練習画面内の設定パネル、折りたたみ、または補助領域でよいもの:

- 再生速度
- メトロノームON/OFF
- メトロノーム音量
- プリカウント
- 表示補助の説明
- 曲情報

ハンバーガーメニューまたは補助メニューでよいもの:

- ロード画面へ戻る
- 端末内保存
- JSON書き出し
- 保存一覧
- ヘルプ、説明
- データ操作系

実装方針:

- 練習画面ではCanvasを画面の主役にしてください。
- 主要操作バーをCanvasの近くに配置し、スマートフォンでもCanvasと同時に見えるようにしてください。
- 縦表示では、実鍵盤を見ながら白鍵幅と譜面横位置を調整できることを優先してください。
- 横表示では、五線の1間の幅と譜面縦位置を調整しやすくしてください。
- ロード画面へ戻る操作は、練習中に誤って押しにくい位置へ移してください。
- ただし、ロード画面へ戻る導線自体は失わないでください。
- メニュー内に移した機能は、見つけにくくなりすぎないよう、ボタン名と説明を明確にしてください。
- メニューや折りたたみは、キーボード操作とスクリーンリーダー上の名前も考慮してください。
- PCでは広い画面を活かし、スマートフォンでは縦積みや下部操作バーを使って、同じ優先順位を保ってください。

維持してください:

- 縦表示でCanvasだけを左右端まで使えること
- 縦表示で実鍵盤へ合わせるための白鍵幅と横位置調整
- 横表示の五線間隔と譜面縦位置調整
- 共通タイムライン、再生、シーク、速度変更
- メトロノーム、音量、プリカウント
- 再生ガイドと助走表示
- 端末内保存、保存一覧、読み込み、削除
- localStorageによる設定復元
- JSON貼り付け、JSONファイル読み込み、JSON書き出し
- /app/ サブパス配置

実装しないでください:

- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- ユーザー登録
- ログイン
- クラウド同期
- 外部サーバー保存
- PWA化、オフライン対応、ホーム画面追加
- 新しい楽曲データ形式
- 本格的な楽譜組版
- 保存データの複製管理
- 新しい設定項目の大量追加
- 大規模なビジュアル刷新

テストしてください:

- スマートフォン縦表示で、Canvas、再生操作、白鍵幅、譜面横位置が同じ練習画面内で使える
- スマートフォン横表示で、Canvas、再生操作、五線間隔、譜面縦位置が同じ練習画面内で使える
- ロード画面へ戻る操作が主要操作から外れ、メニューまたは補助領域から実行できる
- メニューを開閉でき、メニュー内の項目にアクセス可能な名前がある
- スマートフォン縦横、タブレット縦横、PC幅でページ全体の横スクロールが発生しない
- viewport変更後にCanvas内部サイズとCSSサイズが更新される
- 既存の再生、メトロノーム、プリカウント、表示調整、保存、設定復元のE2Eを壊さない

実装後は次を実行してください。

- npm run typecheck
- npm run test
- npm run test:e2e
- npm run build
- git diff --check

Windows PowerShellで npm が動かない場合は npm.cmd を使って構いません。

すべて成功したらローカルコミットし、
コミットID、主要変更ファイル、検証結果、未決事項を報告してください。

GitHubリモートの追加、公開、pushは行わないでください。
```

## 23. 履歴: 第9段階 実装スレッドへの指示

第9段階は実装済みである。以下は実装時に使った指示の履歴であり、次の実装スレッドへそのまま送らない。

```text
第9段階「レスポンシブ対応と操作性調整」を実装してください。

作業開始前に AGENTS.md、README.md、spec.md、
docs/project-roadmap.md、docs/implementation-plan.md を読み、
関連する既存コードとテストを確認してください。

背景:

第8段階までで、ロード画面、縦表示、横表示、共通タイムライン、再生、メトロノーム、プリカウント、
再生ガイド、助走表示、端末内保存、設定保存まで実装済みです。
PCとAndroid Chromeでの第8段階手動確認も問題なしでした。

今回の目的は、既存機能をスマートフォン、タブレット、PCで無理なく操作できるように整えることです。
新機能を広げる段階ではありません。

重点的に確認・調整してください:

- ロード画面、JSON入力欄、保存一覧がスマートフォン幅で横スクロールしない
- 保存、読み込み、削除、JSON書き出しの操作が狭い画面でも押しやすい
- 縦表示の再生操作、白鍵幅、横位置調整、Canvasドラッグがスマートフォン縦横で使いやすい
- 横表示の再生操作、五線間隔、譜面縦位置調整がスマートフォン縦横で使いやすい
- タブレット相当の画面幅で、縦表示、横表示、保存一覧が崩れない
- PC幅で既存の操作性が悪化していない
- 端末回転またはviewport変更後にCanvas内部サイズとCSSサイズが更新される
- ページ全体に横スクロールが発生しない
- 主要操作のタッチ領域が小さすぎない
- 主要ボタン、入力、スライダー、セレクトに分かりやすいラベルまたはアクセス可能な名前がある
- フォーカス表示を消さない
- 色だけで状態を判断させない

維持してください:

- 縦表示でCanvasだけを左右端まで使えること
- 縦表示で実鍵盤へ合わせるための白鍵幅と横位置調整
- 横表示の五線間隔と譜面縦位置調整
- 共通タイムライン、再生、シーク、速度変更
- メトロノーム、音量、プリカウント
- 再生ガイドと助走表示
- 端末内保存、保存一覧、読み込み、削除
- localStorageによる設定復元
- JSON貼り付け、JSONファイル読み込み、JSON書き出し
- /app/ サブパス配置

実装しないでください:

- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- ユーザー登録
- ログイン
- クラウド同期
- 外部サーバー保存
- PWA化、オフライン対応、ホーム画面追加
- 新しい楽曲データ形式
- 本格的な楽譜組版
- 保存データの複製管理
- 大規模なデザイン刷新

テストしてください:

- スマートフォン縦幅、スマートフォン横幅、タブレット相当幅、PC幅の代表ケースで主要画面を確認する
- ロード画面、保存一覧、縦表示、横表示で横スクロールが発生しないことをE2Eで確認する
- viewport変更後にCanvas内部サイズとCSSサイズが更新されることを確認する
- 既存の保存、再生、メトロノーム、プリカウント、設定復元のE2Eを壊さない
- 可能な範囲で、主要ボタンや入力にアクセス可能な名前があることを確認する

実装後は次を実行してください。

- npm run typecheck
- npm run test
- npm run test:e2e
- npm run build
- git diff --check

Windows PowerShellで npm が動かない場合は npm.cmd を使って構いません。

すべて成功したらローカルコミットし、
コミットID、主要変更ファイル、検証結果、未決事項を報告してください。

GitHubリモートの追加、公開、pushは行わないでください。
```

## 24. 履歴: 第8段階 実装スレッドへの指示

第8段階は実装済みである。以下は実装時に使った指示の履歴であり、次の実装スレッドへそのまま送らない。

```text
第8段階「端末内保存と設定保存」を実装してください。

作業開始前に AGENTS.md、README.md、spec.md、
docs/project-roadmap.md、docs/implementation-plan.md を読み、
関連する既存コードとテストを確認してください。

背景:

第7.5段階までで、縦表示・横表示、共通タイムライン、再生、メトロノーム、プリカウント、再生ガイド、助走表示まで実装済みです。
PCとAndroid Chromeでの手動確認も問題なしでした。

今回の目的は、利用者が明示的に保存した楽曲を端末内へ保存し、
表示・再生設定を次回起動時に復元できるようにすることです。

この保存はクラウド保存ではありません。
その端末の、そのブラウザ内だけで使える保存です。

実装してください:

- 検証済み楽曲を、利用者の明示的な操作でIndexedDBへ保存する
- 保存一覧を表示する
- 保存一覧に曲名、保存日時または更新日時を表示する
- 保存一覧から楽曲を読み込めるようにする
- 保存一覧から楽曲を削除できるようにする
- 削除時は確認を出し、キャンセルできるようにする
- 保存データがない場合は、空の状態を分かりやすく表示する
- 「この端末のこのブラウザ内だけに保存され、別端末には同期されない」ことを画面上で明示する
- ブラウザのデータ削除で消える可能性があるため、重要なデータにはJSON書き出しを案内する
- 縦表示の白鍵幅と横位置をlocalStorageへ保存し、次回起動時に復元する
- 縦表示の調整値は、可能であれば端末の縦向きと横向きで分けて保存する
- 横表示の五線間隔と譜面縦位置をlocalStorageへ保存し、次回起動時に復元する
- 再生速度、メトロノームON/OFF、メトロノーム音量、プリカウント設定をlocalStorageへ保存し、次回起動時に復元する
- 壊れた保存データや設定値があっても、アプリ全体を止めず、回復可能なエラーまたは初期値復元として扱う

守ってください:

- 読み込んだだけの内蔵曲、URL曲、貼り付けJSON、JSONファイルを自動保存しない
- IndexedDB操作は、DOMやCanvasから分離する
- localStorage操作は、設定の正規化と分離する
- 保存形式にはバージョンを持たせる
- JSON由来、保存データ由来の文字列をinnerHTMLへ直接入れない
- 保存一覧から読み込む場合も、既存の共通検証フローを通す
- 既存のJSON書き出し機能を壊さない
- /app/ サブパス配置を壊さない

実装しないでください:

- ユーザー登録
- ログイン
- クラウド同期
- 外部サーバー保存
- パスワードによる曲の解除
- 外部カタログ取得
- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- 本格的な楽譜組版

テストしてください:

- 保存レコードの作成、正規化、表示用メタデータを単体テストする
- 設定値の保存、読み込み、範囲外値の補正を単体テストする
- 壊れたlocalStorage値があっても初期値へ戻ることを単体テストする
- 保存一覧、保存、読み込み、削除をE2Eテストする
- ページ再読み込み後も保存曲が残ることをE2Eテストする
- 表示・再生設定がページ再読み込み後に復元されることをE2Eテストする
- 読み込んだだけの曲が自動保存されないことをE2Eまたは単体テストで確認する
- スマートフォン幅で保存UIが横スクロールを発生させないことをE2Eテストする

IndexedDBの単体テストが難しい場合は、保存ロジックを小さなインターフェースで分離し、
純粋な変換・正規化部分を単体テストしてください。
ブラウザ実体のIndexedDBはPlaywright E2Eで確認して構いません。
テスト用ライブラリを追加する場合は、必要性を説明してください。

実装後は次を実行してください。

- npm run typecheck
- npm run test
- npm run test:e2e
- npm run build
- git diff --check

Windows PowerShellで npm が動かない場合は npm.cmd を使って構いません。

すべて成功したらローカルコミットし、
コミットID、主要変更ファイル、検証結果、未決事項を報告してください。

GitHubリモートの追加、公開、pushは行わないでください。
```

## 25. 履歴: 第7.5段階 再生ガイドと助走表示

### 25.1 目的

第7.5段階の目的は、アプリの見え方を音楽ゲーム的な「判定線へ完全一致させる」ものから、教育用の「テンポと譜面の流れをつかむ」ものへ寄せることである。

ユーザー手動確認では、PCで再生速度変更時に画面とメトロノームの同期が少しずれることがあり、赤い線があるためにそのズレが強く気になると報告された。MVPでは演奏判定を行わないため、赤い判定線を強く見せる必要はない。

### 25.2 実装方針

- 「判定ライン」というUI文言を「再生ガイド」へ変更する。
- 赤い強い線ではなく、薄い線または半透明の帯として表示する。
- 縦表示と横表示の両方で同じ方針にする。
- プリカウント中も、表示用の助走位置で譜面を流す。
- 実際の `currentBeat` はプリカウント中に進めない。
- Canvas描画には、実再生位置とは別の `displayBeat`、`renderBeat`、または同等の表示用拍位置を渡せるようにする。
- プリカウント終了後は、表示用拍位置と実際の `currentBeat` が自然につながるようにする。
- シーク、現在位置表示、保存対象の再生位置は、実際の `currentBeat` を基準にする。

### 25.3 助走表示の考え方

プリカウント中の表示用拍位置は、次のような考え方を基準とする。

```text
displayBeat = currentBeat - precountRemainingBeats
```

たとえば、4/4で1小節プリカウントの場合は、開始時に `displayBeat = -4` として描画し、カウントが進むにつれて `displayBeat` が0へ近づく。これにより、1拍目から突然画面が動き出すのではなく、テンポに合わせて譜面が先に流れ始める。

実装上より自然な計算があれば調整してよい。ただし、プリカウント中に利用者へ表示する「曲の現在位置」は、0拍または開始前であることが分かる表示にする。

### 25.4 実装しないもの

- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- 設定の永続保存
- 端末内への楽曲保存
- 本格的な楽譜組版
- アウフタクトのデータ形式変更

アウフタクト対応に役立つ設計ではあるが、この段階ではデータスキーマや既存曲データを変更しない。

### 25.5 テスト

少なくとも次を確認する。

- プリカウント中の表示用拍位置が、負の値または開始前の値として進む。
- プリカウント中も実際の `currentBeat` は進まない。
- プリカウント終了後に、表示用拍位置と `currentBeat` が自然につながる。
- 縦表示で、プリカウント中に音符ブロックが流れる。
- 横表示で、プリカウント中に音符ブロックが流れる。
- 再生ガイドが赤い判定線として強く見えすぎない。
- UI文言に「判定ライン」が残らず、「再生ガイド」へ置き換わっている。
- 既存のメトロノームON/OFF、音量、プリカウント、再生速度、シークを壊さない。
- スマートフォン幅で横スクロールが発生しない。

### 25.6 実装結果

実装コミット:

```text
a90280d962282d51c4942757397c1d9f154229f2
```

実装済みの内容:

- UI文言を「判定ライン」から「再生ガイド」へ変更した。
- 縦表示・横表示のヘルプ文言を再生ガイドの説明へ変更した。
- Canvas上の強い赤い線を、薄い青緑系の再生ガイド帯と細線へ変更した。
- `currentBeat` とは別に `displayBeat` を導入した。
- プリカウント中は `currentBeat` を進めず、`displayBeat` を負の助走位置から0へ近づけるようにした。
- 縦表示・横表示の両方で `displayBeat` による助走描画に対応した。
- Canvas dataset に `data-display-beat` と `data-playback-guide-x` を追加した。
- 単体テストとE2Eで、助走表示、実 `currentBeat` 維持、UI文言を検証した。

実装スレッドでの検証:

- `npm.cmd run typecheck`: 成功
- `npm.cmd run test`: 成功、115件
- `npm.cmd run test:e2e`: 成功、54件
- `npm.cmd run build`: 成功
- `git diff --check`: 成功
- `git diff --cached --check`: 成功
- コミット後の作業ツリー: クリーン

残る確認:

- 再生ガイドの見え方
- PCでの体感同期
- プリカウント中の助走表示の自然さ

ユーザー手動確認結果:

- 縦表示の再生ガイドの見え方: 問題なし
- 「判定ライン」ではなく「再生ガイド」として理解できるか: はい
- 1小節プリカウント中の助走表示: 問題なし
- プリカウント後の再生開始: 問題なし
- 再生速度変更時の違和感: 問題なし
- 横表示の再生ガイドと助走表示: 問題なし
- ロード画面へ戻る: 問題なし
- スマートフォン表示: 問題なし
- 使用PCブラウザ: Chrome
- 使用スマートフォン: Android
- 使用スマートフォンブラウザ: Chrome
- まだ気になるズレ: なし
- 再生ガイドについて気になったこと: なし
- プリカウント中の動きについて気になったこと: なし
- その他気になったところ: なし

## 26. 履歴: 第7.5段階 実装スレッドへの指示

第7.5段階は実装済みである。以下は実装時に使った指示の履歴であり、次の実装スレッドへそのまま送らない。

```text
第7.5段階「再生ガイドと助走表示」を実装してください。

作業開始前に AGENTS.md、README.md、spec.md、
docs/project-roadmap.md、docs/implementation-plan.md を読み、
関連する既存コードとテストを確認してください。

背景:

第7段階ではメトロノームとプリカウントを実装し、
さらに c11b7b557a469dba513899a255b3f76da29294c1 で
再生速度変更時の基準時刻補正も行いました。

しかしPCの手動確認では、再生速度を上げた後に、
画面スクロールがメトロノーム音より少し遅れるように感じることが残りました。
また、赤い判定線があるため、音楽ゲームのように完全同期していないと気になりやすいことが分かりました。

このアプリは演奏判定を行う音楽ゲームではなく、
ピアノ練習用の流れる譜面です。
今回の目的は、見え方を「判定線にぴったり合わせる」ものから、
「テンポと譜面の流れを自然につかむ」ものへ寄せることです。

実装してください:

- UI文言の「判定ライン」を「再生ガイド」へ変更する
- 縦表示と横表示のヘルプ文言も「赤い線」や「判定ライン」から、再生ガイドの説明へ変更する
- Canvas上の強い赤い判定線を、薄い再生ガイドラインまたは半透明の再生ガイド帯へ変更する
- 再生ガイドは演奏判定点ではなく、譜面の流れの目安として見えるようにする
- プリカウント中も、表示用の助走位置で譜面を流す
- プリカウント中は実際の currentBeat を進めない
- Canvas描画には、実再生位置とは別の displayBeat、renderBeat、または同等の表示用拍位置を渡せるようにする
- プリカウント終了後に、表示用拍位置と実際の currentBeat が自然につながるようにする
- 縦表示と横表示の両方で同じ考え方にする

助走表示の基本方針:

4/4で1小節プリカウントなら、プリカウント開始時の表示用拍位置はおおむね -4 拍から始まり、
カウントが進むにつれて 0 拍へ近づくようにしてください。
考え方としては次です。

displayBeat = currentBeat - precountRemainingBeats

実装上より自然な値があれば調整して構いません。
ただし、シーク、現在位置表示、保存対象の再生位置は、実際の currentBeat を基準にしてください。

維持してください:

- メトロノームON/OFF
- メトロノーム音量調整
- 1拍目高音、その他低音
- なし、1小節、2小節のプリカウント
- プリカウント後に元の位置から再生すること
- スタート、一時停止、先頭に戻す、シーク、再生速度変更
- 縦表示の白鍵幅調整
- 横表示の五線間隔と譜面縦位置調整
- ロード画面へ戻ったときのJSONと検証結果保持
- C♯とD♭の扱い
- スマートフォン幅と横スクロールなし

実装しないでください:

- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- 設定の永続保存
- 端末内への楽曲保存
- 本格的な楽譜組版
- アウフタクトのデータ形式変更

テスト:

- プリカウント中の表示用拍位置が開始前の値として進むことを単体テストしてください
- プリカウント中も実際の currentBeat が進まないことを単体テストしてください
- プリカウント終了後に表示用拍位置と currentBeat が自然につながることを単体テストしてください
- 縦表示と横表示で、プリカウント中にCanvasの表示位置が変わることをE2Eまたは単体テストで確認してください
- 「判定ライン」という表示文言がUI上に残らないことを確認してください
- 既存のメトロノーム、プリカウント、再生速度、シークのE2Eを壊さないでください

実装後は次を実行してください。

- npm run typecheck
- npm run test
- npm run test:e2e
- npm run build
- git diff --check

Windows PowerShellで npm が動かない場合は npm.cmd を使って構いません。

すべて成功したらローカルコミットし、
コミットID、主要変更ファイル、検証結果、未決事項を報告してください。

GitHubリモートの追加、公開、pushは行わないでください。
```

## 27. 履歴: 第7段階補正の実装スレッドへの指示

第7段階補正は実装済みである。以下は履歴として残すものであり、新しい実装スレッドへは送らない。

```text
第7段階補正「再生速度変更時の画面とメトロノーム同期」を実装してください。

作業開始前に AGENTS.md、README.md、spec.md、
docs/project-roadmap.md、docs/implementation-plan.md を読み、
関連する既存コードとテストを確認してください。

第7段階の実装コミット a54efd484a1584df8ae2f7e206187f51f11b1d41 は、
型検査、単体テスト、E2E、ビルドがすべて成功しています。
ただしユーザー手動確認で、PCのみ次の現象が報告されました。

- 再生速度を上げると、画面のスクロールがメトロノーム音より少し遅れる時がある
- 毎回ではない
- 縦表示と横表示の両方で、再生速度変更への追従時に発生する
- Android Chromeでは同じズレは確認されていない

今回の目的は、第7段階の小補正として、
再生速度変更時の画面描画とメトロノーム予約の基準時刻をできるだけそろえ、
PCでもズレを起こしにくくすることです。

重点的に確認してください:

- PlaybackController.setPlaybackRate が、tick、anchorBeat、anchorTimeMs、precountAnchorTimeMs、metronome再スケジュールで同じ基準時刻を使えているか
- 再生速度変更直後に、Canvas側の requestAnimationFrame 更新がメトロノーム再予約より不自然に遅れないか
- Web Audio側の先行スケジュール済み音が、速度変更後に古い速度で残らないか
- 縦表示と横表示の両方で、速度変更後の currentBeat とメトロノームの次拍が同じ考え方で計算されているか

実装の方向性:

- 大きな設計変更は避けてください。
- まず setPlaybackRate、seek、pause、start などで now を複数回取得している箇所を確認し、必要なら単一の nowMs を使う形へ補正してください。
- 必要なら、速度変更時に描画側へ即時再描画を促す処理を追加してください。ただし描画コードと音声スケジューラを密結合させないでください。
- メトロノームの先行スケジューリング幅や停止処理を調整する場合は、音切れや二重発音が起きないようにしてください。

維持してください:

- メトロノームON/OFF
- メトロノーム音量調整
- 1拍目高音、その他低音
- なし、1小節、2小節のプリカウント
- プリカウント中は currentBeat を進めないこと
- プリカウント後に元の位置から再生すること
- 縦表示と横表示の共通 currentBeat
- スタート、一時停止、先頭に戻す、シーク、再生速度変更
- ロード画面へ戻ったときのJSONと検証結果保持
- スマートフォン幅と横スクロールなし

実装しないでください:

- 音符に対応するピアノ音源再生
- 演奏判定
- マイク入力
- MIDI入力
- 設定の永続保存
- 端末内への楽曲保存
- 本格的な楽譜組版
- 音色の詳細な選択

テスト:

- 再生速度変更時に currentBeat の進行が遅れないことを単体テストしてください。
- 速度変更時にメトロノームの次拍計算が新しい速度へ切り替わることを単体テストしてください。
- 可能であれば、setPlaybackRate が同一基準時刻で状態更新することをテストしてください。
- E2Eでは、縦表示と横表示で再生中に速度を上げても currentBeat が進み続け、UI状態が壊れないことを確認してください。
- 音の実聴感は自動テストでは厳密判定しなくて構いません。

実装後は次を実行してください。

- npm run typecheck
- npm run test
- npm run test:e2e
- npm run build
- git diff --check

Windows PowerShellで npm が動かない場合は npm.cmd を使って構いません。

すべて成功したらローカルコミットし、
コミットID、主要変更ファイル、検証結果、未決事項を報告してください。

GitHubリモートの追加、公開、pushは行わないでください。
```
