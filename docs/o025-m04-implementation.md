# O-025 M04 実装・レビュー記録

実施日: 2026-09-07

Execution Package: [GitHub Issue #1](https://github.com/kogu0507/piano-roll-score/issues/1)

review_status=pending

## 開始状態と範囲

- branch: `main`
- HEAD: `25651b7b27c90ced490877b28afa8c489e0f3368`
- 変更16 / 削除7 / 未追跡15、全38件が未ステージ。O-025棚卸しと一致。
- Git管理対象と非ignored未追跡の146パスについて、パス・SHA-256・削除状態の一覧から得た集約ハッシュも棚卸しと一致: `086e790b10c3965f45f6c621fe06f78db6fa574e49a069311665b3c7d5208efc`。
- 既存差分のID再編・伴奏教材追加を引き継ぎ、Issueで確定した運指修正、現行資料・テストの整合を実施。commit・push・merge・公開・production mirror更新は実施しない。
- 終了時: 変更21 / 削除7 / 未追跡16、全44件が未ステージ。HEADとindexは開始時と一致。開始時から内容が変わったのは運指9曲、テスト2ファイル、spec・README、履歴への現行追記5ファイル、追加した本記録だけであり、既存パスの追加削除はない。

## 採用した現行仕様と修正

旧6曲は `001→000`、`002→040`、`003→030`、`004→050`、`005→010`、`006→020`。旧`001`〜`007`のURL互換性は終了し、旧`007`は廃止する。`041`は旧`007`の改名ではなく、固定した右手メロディ`040`に合わせる左手伴奏シリーズのLv.1である。

`041`〜`049`の全284音符を確認し、C3＝5、D3＝4、E3＝3、F3＝2、G3＝1に整合させた。280音符の指番号を変更し、E3の4音符は3を維持した。音高、綴り、時刻、音価、音符数、曲名など指番号以外は維持した。アプリ実装や汎用スキーマへの指番号自動補正は追加していない。

`041`〜`045`は和音中心、`046`以降は一部に単音の分散和音、`048`は簡単な音階的進行を含む。左手伴奏シリーズは`041`のみホーム表示し、`042`〜`049`はカタログ限定。内蔵曲全体は17曲、ホーム表示は `000,010,020,030,040,041,050` の7曲。

index・デモの参照先と表示範囲は既存差分が確定仕様に合致しているため維持。catalogは正式な生成コマンドで再生成し、既存内容と一致した。spec・READMEに教材意図と運指・URL廃止方針を明記し、実装計画・roadmap・future-todos・第21/22段階手動確認には現行追記を追加した。旧段階本文、mock、汎用JSON例、教室カタログの別ID例は保持する。

## 検証

運指修正前に `npm.cmd run test -- tests/unit/builtin-song-files.test.ts` を実行し、041の既存期待値更新と全9曲の追加検証が失敗することを確認した（10失敗・11成功）。修正後は以下がすべて成功した。

| コマンド・確認 | 結果 |
|---|---|
| `npm.cmd run typecheck` | 成功 |
| `npm.cmd run test` | 24ファイル・222テスト成功。CLIテストも含め除外なし |
| `npm.cmd run build` | catalog生成、型検査、Viteビルド成功 |
| `npm.cmd run test:e2e` | desktop/mobile Chromiumの152テスト成功（29.7秒） |
| `git -c core.safecrlf=false diff --check` | 成功 |
| catalog生成関数とのメモリ内比較 | public/catalog.htmlと完全一致 |
| publicとdistの比較 | 曲JSON・index・catalog・デモの内容が一致 |
| 旧ID実参照の検索 | publicに残存なし。E2Eには廃止URLの回復検証だけを意図的に追加 |

E2Eには旧`001`〜`007`がエラーになり、現行`000`の選択で回復するテストを追加した。既存E2Eで新17曲の直接URL、カタログリンク、041の両表示、デモ導線、ホーム表示範囲、保存・設定・再生操作を確認した。

警告: E2Eで`NO_COLOR`と`FORCE_COLOR`の同時指定に関するNode警告が出た。テスト失敗はない。Gitは既存の改行設定により通常のdiffでLF→CRLF警告を出すため、diff-checkではコマンド単位で警告を抑制した。Git設定自体は変更していない。

未確認: 実機での演奏・聴感・iPhone Safari、および本番・ミラーの配布状態。これらは今回の自動検証では保証しない。実装上の未解決エラーはない。

## 手動確認

未実施。実機での教育的な見やすさ・弾きやすさ・音の聞こえ方は人間の確認対象。

1. `npm.cmd run dev` で起動し、`http://localhost:5173/app/piano-roll-score/` を開く。
2. ホームで左手伴奏はLv.1だけ、`catalog.html`ではLv.1〜9が表示され、各曲を直接開けることを確認する。
3. `?id=041`をピアノ表示とスコア表示で開き、C3が5、G3が1になることを確認する。白鍵幅・譜面横位置の調整を実鍵盤に合わせる。
4. `?id=048`と`?id=049`でC3・D3・E3・F3・G3が5・4・3・2・1であること、音階的進行と各伴奏リズムを確認する。
5. `?classroom=demo`の左手カードから両表示へ進めることを確認する。
6. 旧`?id=001`〜`?id=007`はエラーになり、現行サンプル選択で回復できることを確認する。
7. PCとスマートフォンでカタログ・練習画面が崩れないこと、メトロノームと表示調整が使えることを確認する。

## commit構成と次の操作

推奨はB: 全変更を1つのatomic migration commitにまとめる。今回検証する状態は、旧ID廃止と伴奏導入、共有index・デモ・生成catalog・期待曲一覧・資料を一体で整合させた状態である。Aの2commit案は中間index・catalog・テスト・資料の組み立てと独立検証を別途必要とし、今回その中間状態は検証していない。生成物やテストだけを種類別に分割しない。

次はこの記録と差分の人間レビュー・手動確認。新たな仕様判断待ちはなく、Issueで採用済みの教材を実機評価する段階である。commit等の操作は別途依頼を受けるまで実施しない。

## 公開経路（今回は実行しない）

将来の公開を別途依頼された場合の経路:

- ビルド元: `C:\Users\kogu0\Documents\seegmund-music-labo-repositorys\projects\piano-roll-score\dist\` の内容
- ローカルミラー: `C:\Users\kogu0\Documents\seegmund-music-labo-repositorys\public_html\app\piano-roll-score\`
- ユーザーのFTPアップロード先: `/public_html/app/piano-roll-score/`

今回ミラーにはコピーしていない。将来のコピー時、既存のコピー用スクリプトは古いファイルを削除しないため、ミラー・サーバー上に旧IDのJSONが残るかは別途確認する。旧ファイルの撤去は対象を確定してから扱い、今回の作業では実施しない。
