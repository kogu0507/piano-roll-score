# AGENTS.md

このファイルは、このリポジトリで作業するCodexおよびその他の実装担当者向けの共通ルールである。

## 1. 作業開始時に読む資料

コードの調査、設計、実装、レビューを始める前に、次の資料を読むこと。

1. `AGENTS.md`
2. `README.md`
3. `spec.md`
4. `docs/project-roadmap.md`
5. `docs/implementation-plan.md`
6. 作業対象に関連する既存コードとテスト

`_spec.md` は旧仕様書であり、参照用に残している。正式な仕様として使用しない。

資料間に矛盾がある場合や、仕様だけでは判断できない重要事項がある場合は、推測で製品仕様を変更しない。問題点、影響、推奨案を報告し、設計判断を求めること。

## 2. 資料の役割

- `spec.md`
  - 製品目的、教育的価値、機能要件、完成条件を定める正式な仕様書。
- `docs/implementation-plan.md`
  - 現在の実装段階、作業範囲、完了条件を定める。
- `docs/project-roadmap.md`
  - 全体の実装順序、進捗、ユーザーが次に行うことを定める。
- `README.md`
  - 人間向けの概要、開発方法、資料への入口を提供する。
- `AGENTS.md`
  - Codexが守る作業手順と品質基準を定める。

製品仕様を変更する場合は、コードだけでなく `spec.md` も更新する。実装計画を変更する場合は `docs/implementation-plan.md`、段階の進捗を変更する場合は `docs/project-roadmap.md` を更新する。

ただし、実装スレッドは明示的に依頼されていない仕様変更を行わないこと。仕様変更が必要に見える場合は、実装を止める必要がある範囲だけ止め、設計スレッドへ判断材料を返す。

## 3. プロジェクトの優先事項

このアプリは、一般的な動画ゲームや本格的な楽譜アプリではなく、ピアノ教育上の仮説を検証するための実験的教材である。

特に次を簡略化、削除、別機能へ置き換えないこと。

- 縦表示で白鍵1鍵あたりの表示幅を調整できること
- 縦表示で譜面全体の横位置を微調整できること
- 狭い課題音域だけを表示し、不要な余白を抑えること
- 実鍵盤に近い白鍵と黒鍵の相対位置を使用すること
- 横表示を五線風にし、太い5本線と控えめな補助グリッドを使うこと
- 同じ実音高でも、ド♯とレ♭などの表記上の違いを保持すること
- 縦表示では実音高を基準にし、横表示では音名の綴りを基準にすること
- 縦表示と横表示で同じ楽曲データと再生タイムラインを共有すること

演奏判定、本格的な楽譜組版、ユーザー管理、クラウド同期など、`spec.md` でMVP対象外とした機能を先回りして実装しないこと。

## 4. 技術方針

- ViteとTypeScriptを使用する。
- UIは通常のHTML、CSS、DOM APIを基本とし、React、Vue、SvelteなどのUIフレームワークは導入しない。
- TypeScriptのstrictモードを有効にする。
- `any` の常用を避ける。
- Canvas描画にはCanvas 2D APIを使用する。
- メトロノームにはWeb Audio APIを使用する。
- 単体テストにはVitestを使用する。
- E2EテストにはPlaywrightを使用する。
- 外部依存は、必要性と保守上の利点を説明できるものに限定する。
- 静的ホスティングで動作する構成を維持する。
- `/app/` のようなサブパス配置を壊さない。
- URLやアセットパスをドメイン直下へ固定しない。

ライブラリを追加する場合は、既存のWeb APIや小さな自作コードでは不十分な理由を確認する。仕様へ影響する選択は独断で決めない。

## 5. 設計とコード構成

- データ検証、時間計算、音高計算、鍵盤座標、五線座標、設定正規化は、DOM、Canvas、Web Audioから分離する。
- 描画、再生状態、データ取得、端末保存、画面UIの責務を分離する。
- ピュア関数として表現できるロジックを、UIイベント内へ直接埋め込まない。
- 同じ計算を縦表示と横表示へ重複実装しない。
- ファイルを細分化すること自体を目的にしない。一つのファイルが独立した複数責務を持つ場合に分割する。
- 公開API、データ境界、複雑な計算には、意図が分かる型名と関数名を付ける。
- コメントは、コードから分かる処理内容ではなく、教育上または技術上の理由を説明するために使う。

## 6. データと安全性

- JSONは信頼できない入力として検証する。
- `pitch` と `spelling` の音楽的整合性を検証する。
- JSON由来の文字列を `innerHTML` で直接挿入しない。
- 読み込んだ楽曲を、利用者の操作なしに永続保存しない。
- 利用者のデータを外部へ自動送信しない。
- 極端なデータサイズや音符数で画面が停止しないよう、妥当な上限を設ける。
- データ形式を変更する場合は `schemaVersion` と既存データへの影響を検討する。

## 7. テストと検証

変更内容に応じて、少なくとも次を実行する。

```text
npm run typecheck
npm run test
npm run test:e2e
npm run build
```

Windows PowerShellで実行ポリシーにより `npm` が動かない場合は、`npm.cmd` を使用してよい。

テストの基本方針は次のとおり。

- 計算、検証、変換、状態遷移は自動テストする。
- 主要なユーザー操作はE2Eテストする。
- Canvas全体の厳密なピクセル一致へ過度に依存しない。
- Canvas座標は可能な限り単体テストする。
- 音の聞こえ方、実鍵盤との一致、教育的な見やすさは手動確認項目として残す。
- バグ修正時は、可能であれば再現テストを先に追加する。

実装完了報告には、実行したコマンド、成功・失敗、未実施項目と理由を含めること。

## 8. 作業範囲と変更管理

- 現在の作業範囲は `docs/implementation-plan.md` に従う。
- 指示された段階より先の機能をまとめて実装しない。
- 関係のないファイルを整形、移動、改名しない。
- ユーザーまたは別スレッドによる既存変更を上書きしない。
- 破壊的なGit操作を行わない。
- 生成物や依存ディレクトリを、必要なくリポジトリへ追加しない。
- 実装中に見つけた将来課題は、現在の作業に混ぜず、完了報告で分離して示す。

## 9. 完了報告

実装スレッドは、作業終了時に簡潔に次を報告する。

1. 実装した内容
2. 変更した主要ファイル
3. 実行した検証と結果
4. 仕様上の未決事項またはリスク
5. 次の実装段階として推奨する内容

仕様の解釈を伴う判断を行った場合は、その判断と根拠を明示すること。
