# piano-roll-score 仕様書

## 1. 文書の目的

本書は、ピアノ教室で試用するピアノロール譜アプリ `piano-roll-score` のMVP仕様を定義する。

このアプリは、完成された楽譜アプリや演奏採点アプリを目指すものではない。先生と生徒が実際に利用し、次の教育的仮説を検証するための実験的な教材とする。

1. 楽譜をまだ読めない初心者でも、縦表示を使うことで鍵盤と音の位置関係を理解しやすくなるか。
2. ゲームに近い見た目によって、子供が自発的にピアノへ触れる時間を増やせるか。
3. 縦表示で練習した同じ課題を横表示でも演奏することで、五線譜への移行を支援できるか。

対象は個人利用および知り合いの個人ピアノ教室での試用とする。大規模な商用サービス、ユーザー管理、演奏採点はMVPの対象外とする。

## 2. MVPの基本方針

- 楽曲データはJSONで扱う。
- 共有サンプルはアプリの配布物に同梱する。
- 共有サンプルは `?id=001` のようなURLで直接開ける。
- JSONの貼り付け、JSONファイルの読み込み、JSONファイルへの書き出しを可能にする。
- 読み込んだデータや編集中のデータを、利用者の端末内へ明示的に保存できるようにする。
- 表示は縦表示と横表示の2種類を提供する。
- 自動化できるロジックと主要操作は自動テストし、見た目、実鍵盤との位置合わせ、音、教育的な使いやすさは人間が確認する。
- 実験段階では、変更しやすさ、検証しやすさ、Codexが安全に修正しやすい構成を優先する。

## 3. MVPに含める機能

### 3.1 楽曲データ

- 内蔵サンプル一覧の表示
- URLの `id` パラメーターによる内蔵サンプルの読み込み
- JSONテキストの貼り付けと編集
- JSONファイルのインポート
- JSONファイルのエクスポート
- 端末内への保存、再読み込み、削除
- データ検証と理解しやすいエラー表示

内蔵サンプル、URLの `id`、JSON貼り付け、JSONファイル読み込みは、すべて同じ編集可能なJSON欄と共通の検証処理へ接続する。入力元ごとに異なる楽曲解釈や検証を持たせない。

### 3.2 譜面表示

- 縦表示
- 横表示
- 表示方向の切り替え
- 拍線と小節線の表示
- 音名表示のON/OFF
- 指番号表示のON/OFF
- 臨時記号付き音符の自動強調
- 表示密度の調整
- 横表示の五線間隔の調整
- 横表示の譜面縦位置の調整
- 画面サイズや端末向きの変更時の再描画

### 3.3 再生操作

- スタート
- 一時停止
- 先頭に戻す
- シーク
- 再生速度変更
- なし、1小節、2小節のプリカウント
- メトロノームON/OFF

### 3.4 設定保存

- 一般表示設定
- 再生設定
- 縦表示の鍵盤幅調整値
- 縦表示の横位置調整値

設定は端末内へ保存し、次回起動時に復元する。

## 4. MVPに含めない機能

次の機能は、実際のユーザーと先生の意見を確認してから再検討する。

- マイクまたはMIDI入力による演奏判定
- 正誤判定、採点、ランキング
- 本格的な楽譜組版
- グランドスタッフを使った通常のピアノ譜
- 音符に対応するピアノ音源の再生
- ユーザー登録、ログイン、クラウド同期
- 課金
- 先生用の生徒管理画面
- 強いアクセス制御を伴うパスワード配信
- 複数端末間の進捗同期
- 外部データサーバーを必須とする構成

## 5. 技術要件

### 5.1 採用技術

- Vite
- TypeScript
- HTML
- CSS
- Canvas 2D API
- Web Audio API
- Vitest
- Playwright

UIフレームワークは必須としない。MVPでは、TypeScriptと通常のDOM APIを基本とする。

BootstrapなどのCSSフレームワークも必須としない。導入する場合は、実装量を実際に減らせること、表示調整を妨げないこと、依存を増やす価値があることを確認する。

楽曲JSONの実行時検証にはZodを使用し、可能な限りスキーマからTypeScript型を推論する。型定義と実行時検証を別々に二重管理しない。

### 5.2 実装上の原則

- TypeScriptのstrictモードを有効にする。
- `any` の常用を避ける。
- データ検証、時間計算、座標計算、保存形式変換をDOMやCanvasから分離する。
- 描画処理と再生状態管理を分離する。
- 縦表示と横表示は、同じ楽曲データと再生タイムラインを共有する。
- 外部依存は必要最小限にする。
- npmスクリプトから開発、ビルド、型検査、自動テストを実行できるようにする。

### 5.3 配置先

アプリは静的ファイルとして、次のようなサブパスへの配置を想定する。

```text
https://example.com/app/piano-roll-score/
```

アプリ内のURLをドメイン直下へ固定しない。Viteの `base` または `import.meta.env.BASE_URL` を利用し、サブパスへ配置してもデータとアセットを読み込めるようにする。

MVPは静的ホスティングだけで動作し、専用サーバーを必要としない。

## 6. データの配布と保存

### 6.1 内蔵サンプル

内蔵サンプルはアプリの配布物へ含める。

```text
public/
  catalog.html
  data/
    songs/
      index.json
      001.json
      002.json
      003.json
      004.json
      005.json
      901.json
      902.json
```

`index.json` は、利用可能な内蔵サンプルのID、タイトル、説明、対象レベルなどを持つ一覧ファイルとする。

内蔵曲JSONでは `id` を必須とし、ファイル名および要求されたIDと一致させる。たとえば `001.json` の `id` は `"001"` とする。一致しないデータは壊れた内蔵データとして扱う。

`catalog.html` は、内蔵サンプルのID、曲名、説明、level、直接開く相対リンク、公開URL例を確認するための静的ページとする。アプリ本体のロード画面を複雑にしないための補助資料であり、`index.json` から生成する。

URL例:

```text
https://example.com/app/piano-roll-score/?id=001
```

`id` が指定された場合は該当曲を読み込む。IDが存在しない、またはデータが壊れている場合はエラーを表示し、ロード画面から別のデータを選べるようにする。

### 6.2 外部データ

公開された静的JSONを `https://example.com/data/...` に置くだけであれば、専用のアプリケーションサーバーは不要である。

ただし、別ドメインから取得する場合はCORS設定が必要になる。同じドメイン配下の `/app/piano-roll-score/` と `/data/` などであれば、通常はCORSなしで読み込める。

MVPでは外部データを必須としないが、将来追加しやすいように、データ取得処理をUIや描画処理へ直接埋め込まない。

想定するデータ取得元は次のとおり。

- 内蔵サンプル
- JSON貼り付け
- ローカルJSONファイル
- 端末内保存
- 将来の外部URLまたは外部カタログ

### 6.3 端末内保存

- 一般設定と小さな状態は `localStorage` に保存してよい。
- 利用者が保存した楽曲はIndexedDBへ保存する。
- 保存時には、曲名と保存日時を表示する。
- 読み込んだだけのデータを無断で永続保存しない。
- 保存データの一覧、読み込み、削除を提供する。
- 端末内保存であり、別端末には同期されないことを画面上で明示する。
- ブラウザのデータ削除やプライベートブラウズによって消える可能性があるため、重要なデータにはJSONエクスポートを案内する。

### 6.4 JSON入力とファイル入出力

- JSON欄は利用者が直接編集できる。
- 内蔵サンプルまたはURLの `id` から読み込んだデータもJSON欄へ整形して表示する。
- JSON欄の最大入力サイズはUTF-8換算で2 MiBとする。
- 読み込めるファイルは1ファイルだけとし、最大2 MiBとする。
- ファイル拡張子は原則として `.json` とする。ブラウザのMIME判定だけには依存しない。
- UTF-8のJSONを扱い、先頭のBOMは許容する。
- ファイル内容を外部へ送信せず、ブラウザ内で読み込む。
- エクスポート前に現在のJSONを検証する。不正なJSONは書き出さない。
- エクスポートするJSONは検証後のデータを2スペースで整形し、UTF-8で書き出す。
- ファイル名には検証済みの `id` または安全化した曲名を使用し、利用できない場合は `piano-roll-score-song.json` とする。
- 書き出しファイルのベース名は80文字以下とし、Windowsの予約名である `CON`、`PRN`、`AUX`、`NUL`、`COM1` から `COM9`、`LPT1` から `LPT9` をそのまま使用しない。
- 編集中の内容を内蔵サンプル、ファイル読み込み、クリア操作で置き換える場合は、必要に応じて破棄確認を行う。

### 6.5 合言葉によるアンロック

子供向けのゲーム的な仕組みとして、課題終了後に先生から次の「合言葉」を渡す案は有効な可能性がある。ただし、MVPには含めず、利用者の反応を見て追加する。

合言葉を単なる進行演出として使い、データを秘密にする必要がない場合はサーバー不要で実装できる。解除済みIDを端末内へ保存すればよい。

短い合言葉で静的ファイルを暗号化しても、強いアクセス制御にはならない。本当に未許可の利用者へデータを渡したくない場合は、認証を行うサーバーまたはサーバーレスAPIが必要になる。

仕様上は、将来次の3段階から選べるようにする。

1. 演出のみの合言葉。サーバー不要。
2. 暗号化した静的JSONの解除。サーバー不要だが、機密保護用途には使わない。
3. サーバーで認証して配信。強いアクセス制御が必要になった場合のみ採用する。

画面上の名称は、セキュリティ機能と誤解されやすい「パスワード」ではなく「合言葉」を推奨する。

## 7. 楽曲データ形式

### 7.1 基本方針

鍵盤上の実音高と、楽譜上の音名表記を分離して保持する。

- `pitch` は鍵盤位置と実音高を示すMIDIノート番号とする。
- `spelling` はド♯とレ♭のような表記上の違いを保持する。
- 縦表示の鍵盤位置は `pitch` を基準にする。
- 横表示の五線上の位置は `spelling` を基準にする。
- `pitch` と `spelling` が音楽的に一致することをデータ検証で確認する。

これにより、MIDI番号が同じ `C#4` と `Db4` を次のように使い分けられる。

- 縦表示では同じ黒鍵の位置へ表示する。
- 横表示ではCの位置またはDの位置へ表示する。
- 横表示ではそれぞれ♯または♭を表示する。

これは、鍵盤位置を学ぶ段階から五線上の音名表記へ移行するための重要な仕様とする。

### 7.2 JSON例

```json
{
  "schemaVersion": 1,
  "id": "001",
  "title": "ドからラまで",
  "description": "右手の導入課題",
  "bpm": 80,
  "timeSignature": {
    "numerator": 4,
    "denominator": 4
  },
  "clef": "treble",
  "displayRange": {
    "mode": "fixed",
    "minPitch": 60,
    "maxPitch": 69
  },
  "notes": [
    {
      "id": "n1",
      "pitch": 61,
      "spelling": {
        "step": "C",
        "accidental": "sharp",
        "octave": 4
      },
      "hand": "right",
      "finger": 2,
      "time": 0,
      "duration": 1
    },
    {
      "id": "n2",
      "pitch": 61,
      "spelling": {
        "step": "D",
        "accidental": "flat",
        "octave": 4
      },
      "hand": "right",
      "finger": 3,
      "time": 1,
      "duration": 1
    }
  ]
}
```

### 7.3 曲全体の項目

`schemaVersion`
: データ形式のバージョン。MVPは `1` とする。

`id`
: 内蔵曲または保存曲を識別するID。貼り付けやインポートでは省略可能とする。

`title`
: 曲名または課題名。

`description`
: 先生向けまたは生徒向けの短い説明。省略可能。

`bpm`
: 基準テンポ。20以上300以下の有限数とする。

`timeSignature`
: 拍子。`numerator` と `denominator` を持つ。`numerator` は1以上32以下の整数、`denominator` は `1`、`2`、`4`、`8`、`16`、`32` のいずれかとする。

`clef`
: 横表示で使用する音部記号。MVPでは `treble` または `bass` の単一譜表を扱う。

`displayRange`
: 縦表示で使用する音域。`auto` または `fixed` とする。教育実験で同じ条件を再現できるよう、内蔵課題では原則として `fixed` を使用する。

`notes`
: 音符データの配列。

### 7.4 音符の項目

`id`
: 曲内で一意な音符ID。

`pitch`
: MIDIノート番号。中央のドを `60` とする。MVPでは88鍵ピアノの範囲である `21` から `108` を受け付ける。

`spelling.step`
: `A` から `G` の音名。

`spelling.accidental`
: `natural`、`sharp`、`flat` のいずれか。

`spelling.octave`
: オクターブ番号。

`hand`
: `right`、`left`、`unspecified` のいずれか。省略時は `unspecified`。

`finger`
: 1から5の指番号。省略可能。

`time`
: 開始位置。単位は拍。0以上の有限数とする。

`duration`
: 音の長さ。単位は拍。0より大きい有限数とする。

### 7.5 データ検証

少なくとも次を検証する。

- JSONとして解析できる
- 対応する `schemaVersion` である
- 必須項目が存在する
- BPMと拍子が有効である
- 内蔵曲では、要求されたIDと曲データ内の `id` が一致する
- 音符配列が空ではない
- 音符IDが重複していない
- `time` と `duration` が有効である
- 指番号が1から5の範囲である
- MIDIノート番号が有効範囲にある
- `pitch` と `spelling` が同じ実音高を示す
- 固定表示範囲にすべての音符が収まっている
- 同時刻の複数音符を許可する
- 1曲の音符数は最大5,000件とする

エラーには、可能な限り対象フィールドと音符IDを含める。

## 8. 画面構成

### 8.1 ロード画面

ロード画面は、利用者にとってのホーム画面として扱う。初期表示では「曲を選んで、表示を選んで始める」導線を主役にする。

- 曲選択
  - サンプル曲
  - 端末内保存曲
- ピアノ表示で開始
- スコア表示で開始
- 曲の詳細
- データ管理
  - JSON入力欄
  - JSONの内容を検証する
  - JSONファイルを読み込む
  - 現在のJSONをファイルへ書き出す
  - 端末内へ保存
  - 端末内保存データの一覧
  - 検証エラー表示

ユーザーに見える表示名は、縦表示を「ピアノ表示」、横表示を「スコア表示」とする。内部の実装名や設計説明では、必要に応じて縦表示／横表示を使ってよい。

JSON貼り付け、ファイル入出力、端末内保存、保存一覧、詳細な検証結果は、初期表示では目立たせすぎず、データ管理の中にまとめてよい。

URLに有効な `id` がある場合は、そのデータを自動で読み込んで譜面表示へ進んでよい。ただし読み込み中であることを表示する。

Canvasによる譜面画面がまだ利用できない実装段階では、有効な `id` のデータをJSON欄へ読み込み、検証済みの楽曲概要を表示する。譜面表示実装後は、同じ検証済みデータから譜面画面へ進む。

検証エラーは、全体の説明に加えてフィールドパス、音符ID、原因を可能な範囲で表示する。大量のエラーで画面が埋まらないよう、画面に表示する詳細は先頭20件までとし、残件数を示してよい。

### 8.2 譜面表示画面

- ホームへ戻る
- 再生／一時停止
- 最初から
- ピアノ表示／スコア表示の切り替え
- シーク
- 現在位置と曲全体の時間または拍数
- 設定パネル
- 譜面Canvas
- エラーまたは状態メッセージ

ホームへ戻る際、未保存の編集がある場合は破棄確認を行う。

### 8.3 練習画面の配置方針

練習画面は、スマートフォン、タブレット、PC外部ディスプレイをピアノ鍵盤付近へ置いて使う前提で設計する。機能名、設定値、保存形式、操作の優先順位は端末種別を問わずそろえる。一方で、画面サイズ、向き、入力方式に応じて、PC、タブレット、スマートフォンのレイアウトは分けてよい。

特に、教室用のWindowsタブレットや小型PCディスプレイも対象に含める。端末判定はOS名やUser-Agentへ過度に依存せず、画面幅、高さ、向き、タッチ入力の有無、利用可能領域を基準にする。スマートフォン向けの狭い表示を、PCやタブレットへ無理に適用しない。

通常練習中は、Canvasをピアノ鍵盤に近い画面下側へ大きく配置する。練習中に常時表示する操作は最小限にする。

通常練習モードで常時表示する主な操作は次とする。

- ホームへ戻る
- 再生／一時停止
- 最初から
- 再生速度
- 練習メニュー
- 曲の現在位置シーク
- 譜面Canvas

スマートフォンでは、上記のうちホーム、再生、一時停止、最初から、練習メニューを1列のアイコンボタンとして表示する。ボタン内の長い文字ラベルはCanvasを圧迫するため避ける。ただし、アクセシビリティとE2Eテストのために `aria-label` などの名前は保持する。

再生速度は、スマートフォンの通常練習画面では横長のrangeではなく、原則として選択式のコンパクトなUIを使う。候補は `2倍`、`1.75倍`、`1.5倍`、`1.25倍`、`1倍`、`0.75倍`、`0.5倍` を基本とする。

曲の現在位置シークはCanvasを圧迫しない低背の1行UIとする。長い説明ラベル、大きな余白、複数行のラベルでCanvas高さを削らない。

曲名、ピアノ表示／スコア表示の切り替え、保存、JSON書き出し、ホームへ戻る操作は、通常練習中のCanvas表示を圧迫しないよう、練習メニュー内へ移してよい。ただし、ホームへ戻る操作は戻り方を見失わないよう、通常練習画面の上部操作列にも常時表示する。

白鍵幅、譜面横位置、五線間隔、譜面縦位置などの表示調整は、通常練習画面へ常時表示せず、練習メニューから入る表示調整モードで扱う。表示調整モードでは、Canvasを見ながら調整でき、完了後に通常練習モードへ戻れるようにする。

表示調整モードは、Canvas領域を押し下げたり縮めたりしない。通常練習画面と同じCanvas配置を維持し、その上に半透明のフローティングパネルとして表示する。パネル背景は半透明でも、文字、スライダー、ボタン自体は読める不透明度を保つ。スマートフォンでは、表示調整パネルがCanvas全体を覆って調整対象を見失う状態を避ける。

表示調整パネルの配置は、Canvasの左上ではなく、練習画面または端末表示領域を基準に決める。スマートフォン横向きなど高さが短い画面では、再生ボタン列付近を含む画面上部へ重ねてよく、Canvasの主要部分をできるだけ残す。PCやタブレットでは、画面幅を活かして右側または左側のフローティングパネル、サイドパネル、または小さなドック表示にしてよい。

表示調整モードでは、音域方向の調整に加えて、時間方向の表示倍率を調整できるようにする。UI上の名称は「音価の幅」または「時間幅」とする。これは再生速度ではなく、Canvas上で1拍または1音価を何pxとして描くかの表示専用倍率である。値を小さくすると音符ブロックの時間方向の長さと移動量が圧縮され、テンポが速い課題でも目で追いやすくなる。値を大きくすると音符ブロックの時間方向が広がる。初期値は100%とし、MVPでは50%から200%程度の範囲を目安にする。

時間方向の表示倍率は、BPM、再生速度、`currentBeat`、メトロノーム、プリカウント、シーク位置、楽曲JSONの `time` と `duration` を変更しない。あくまで縦表示と横表示のCanvas描画に使う表示密度である。

練習メニューは階層を深くしすぎない。メニュー内は原則として次の順に並べる。

1. 再生設定
2. 表示設定
3. その他

再生設定には、再生速度、メトロノーム、メトロノーム音量、プリカウントを含める。再生速度は使用頻度が高いため、通常練習画面にも常時表示してよい。

表示設定には、表示調整モード、ピアノ表示／スコア表示の切り替え、曲情報などを含める。

その他には、端末内保存、JSON書き出し、保存一覧、ホームへ戻る操作を含める。

スマートフォンでは、練習メニューが画面外に展開されて操作不能にならないようにする。必要に応じて、固定配置のオーバーレイ、ドロワー、またはダイアログとして表示し、メニュー内部だけをスクロール可能にする。メニューが長くなる場合でも、閉じる操作のために最上部まで戻る必要がないようにする。たとえば、メニューを画面幅の80%程度にし、残りの背景部分をタップすると閉じる、閉じるボタンを固定表示する、などの方法を使ってよい。

練習画面では、譜面操作中にブラウザのプルダウン更新が発生して使いにくくならないよう、可能な範囲でページ全体の縦スクロールとoverscrollを抑制する。ただし、ロード画面や保存一覧など、通常のページスクロールが必要な画面まで不必要に固定しない。

## 9. 縦表示

### 9.1 教育上の目的

縦表示は、ピアノを始めたばかりの人、楽譜をまだ読めない人、ゲーム感覚で鍵盤へ触れたい子供を主対象とする。

音符ブロックが上から下へ流れ、画面下部の再生ガイド付近を通過する。演奏の正誤判定は行わない。

### 9.2 座標

- 横軸は実際の鍵盤位置とする。
- 横位置はMIDIノート番号から白鍵と黒鍵の物理的な並びを計算する。
- 縦軸は時間とする。
- 音符ブロックの縦方向の長さは `duration` に対応する。
- 白鍵と黒鍵を均等な半音グリッドとして扱わず、実鍵盤に近い相対位置を使う。
- 白鍵を基準単位とし、黒鍵の中心は隣接する白鍵の境界上に置く。
- 黒鍵および黒鍵音符ブロックの幅は、白鍵幅の `0.62` を基準とする。
- 固定音域または自動音域の端が黒鍵の場合は、その黒鍵を配置するために必要な隣接白鍵まで描画範囲へ含める。
- Canvas内のローカル座標は、描画対象となる最初の白鍵の左端を `0` とする。

再生実装前の静止プレビューでは、再生位置を0拍として描画する。

- 再生ガイドへ音符の開始位置が到達する時刻を `time` とする。
- 音符ブロックは再生ガイドから上方向へ `duration` に応じた長さを持つ。
- 1拍あたりの初期表示密度は64 CSS pxとする。
- 音価の幅または時間方向の表示倍率が指定された場合は、1拍あたりの表示密度へ倍率を掛け、音符ブロックの縦方向の位置、長さ、移動量へ反映する。
- Canvas外の音符は描画を省略またはクリップしてよい。

### 9.3 鍵盤幅調整

縦表示における最重要機能として、画面上の音符位置を実際のピアノ鍵盤へ合わせられるようにする。

- 白鍵1鍵あたりの表示幅を調整できる。
- 白鍵幅は16から320 CSS pxの範囲で、1 CSS px単位に調整できる。
- 調整結果を即時にCanvasへ反映する。
- 再生位置を変更するシークとは、見た目も操作も明確に分離する。
- 誤操作を防ぐため、通常再生中は調整UIを折りたたんでよい。
- 調整値は端末内へ保存する。
- 端末の縦向きと横向きで別の調整値を保存できる。

幅だけでは実鍵盤へ合わせられない場合があるため、譜面全体の横位置もスライダーまたはドラッグで微調整できるようにする。

- 「画面幅に合わせる」は、対象白鍵がCanvas幅へ収まる白鍵幅を計算し、許容範囲内へ正規化して中央配置する。
- 「中央に戻す」は、現在の白鍵幅を維持したまま対象音域をCanvas中央へ戻す。
- 白鍵幅の変更時は、可能な限り対象音域の中心位置を画面上で維持する。
- Canvasを横方向へドラッグした場合は、横位置調整値と同期する。

### 9.4 表示音域と余白

- 課題の表示音域だけを描画対象とする。
- 内蔵課題では、先生が意図した固定音域を指定できる。
- `auto` の場合は音符から必要音域を計算する。
- 外側の境界は実鍵盤上の鍵の境界へ合わせる。
- 課題と無関係な広い音域を表示しない。
- 画面幅を単純に88鍵へ割り当てない。
- 必要以上の左右余白を作らない。
- 対象音域がCanvasより広い場合はCanvas内でクリップし、ページ全体の横スクロールを発生させない。

狭い音域の課題では、スマートフォンの横幅と実鍵盤の幅を合わせて使用できることを重視する。

### 9.5 補助表示

- 画面下部に再生ガイドラインまたは再生ガイド帯を表示する。
- 再生ガイドは、演奏の正誤判定点ではなく、譜面の流れを把握するための目安とする。
- 赤などの強い表示で厳密な判定線に見えすぎる場合は、薄い線、帯、または非表示にできる表示を優先する。
- 拍線を薄い横線として表示する。
- `timeSignature.numerator` の拍数ごとに、小節線を拍線より少し太く濃い横線として表示する。
- 拍線・小節線は音価の幅、再生位置、助走表示に追従して流れる。
- `timeSignature.denominator` は現時点では拍線・小節線の見た目計算には使わない。
- 必要に応じて白鍵と黒鍵の位置を示す簡易鍵盤ガイドを表示する。
- 音名と指番号を音符ブロック内または近くへ表示できる。
- 音名は `spelling` に従って、ド♯とレ♭を区別する。
- 静止プレビューでは簡易鍵盤ガイドを表示する。再生ガイドはガイドの上端付近とする。
- 簡易鍵盤ガイドの白鍵部分は72 CSS px、黒鍵部分は42 CSS pxを基準とする。
- 簡易鍵盤ガイドは、画面上でタップして演奏するための鍵盤ではなく、実鍵盤との位置合わせを助ける目安である。スマートフォン横向きなど高さが短い画面では、音符が流れる領域を優先し、鍵盤ガイドの高さを基準値の50%から60%程度へ圧縮してよい。この圧縮は白鍵幅、黒鍵の相対位置、音符ブロックの横座標には影響させない。
- 音符ブロック内の音名は「ド」「レ」「ド♯」「レ♭」のような日本語音名とし、通常はオクターブ番号を表示しない。
- 指番号がある場合は音名と区別して表示する。
- 右手、左手、指定なしは、色だけに依存せず必要に応じてラベルまたは凡例でも区別できるようにする。
- `spelling.accidental` が `sharp` または `flat` の音符は、細い色帯や枠線で軽く強調する。`sharp` はオレンジ/アンバー系の暖色、`flat` は青/青紫系の寒色を使い、鍵盤位置の理解を妨げないよう、音符ブロック全体を派手に塗り分けすぎない。
- 臨時記号の強調は音名表示や指番号表示のON/OFFとは独立して常時表示する。

## 10. 横表示

### 10.1 教育上の目的

横表示は、縦表示で覚えた鍵盤上の動きを、五線に近い上下位置と結び付けるために使用する。

同じ課題を縦表示と横表示で切り替えて練習できるようにし、将来の通常楽譜への移行を支援する。

### 10.2 座標

- 横軸は時間とする。
- 音符ブロックは右から左へ流れる。
- 音符ブロックの横幅は `duration` に対応する。
- 音価の幅または時間方向の表示倍率が指定された場合は、1拍あたりの表示密度へ倍率を掛け、音符ブロックの横方向の位置、長さ、移動量へ反映する。
- 左側に再生ガイドラインまたは再生ガイド帯を表示する。
- 縦位置はMIDI番号だけではなく `spelling` の音名段階を基準に計算する。

たとえば、同じ鍵盤を表すド♯とレ♭でも、ド♯はドの段、レ♭はレの段へ表示する。これにより、五線上での異名同音の違いを保持する。

### 10.3 五線風背景

- 対象の音部記号に対応する5本の線を太めに表示する。
- その他の補助グリッドは5本線より細く、目立ちすぎない表示にする。
- 拍線を薄い縦線として表示する。
- `timeSignature.numerator` の拍数ごとに、小節線を拍線より少し太く濃い縦線として表示する。
- 拍線・小節線は音価の幅、再生位置、助走表示に追従して流れる。
- `timeSignature.denominator` は現時点では拍線・小節線の見た目計算には使わない。
- 線と線の間も音の位置として認識できるレイアウトにする。
- 五線外の音には、必要に応じて補助線を表示する。
- MVPは本格的な楽譜組版ではなく、音符ブロックと五線位置の関係を学ぶ表示とする。
- 音名、指番号、♯、♭を必要に応じて表示する。
- スコア表示の音符ブロック内ラベルは、流れてくる音名を早く読めるよう、時間方向の左端側へ寄せて表示する。
- ラベル文字列は音名、指番号の順を基本とし、音名表示OFF、指番号表示OFFの設定をそれぞれ反映する。右手・左手は既存の色分けと凡例で区別する。
- `spelling.accidental` が `sharp` または `flat` の音符は、細い色帯や枠線で軽く強調する。`sharp` はオレンジ/アンバー系の暖色、`flat` は青/青紫系の寒色を使う。スコア表示では左寄せラベルと干渉しない位置に強調を置き、♯・♭の文字を読みやすく保つ。
- 臨時記号の強調は音名表示や指番号表示のON/OFFとは独立して常時表示する。

### 10.4 横表示の表示調整

横表示では、五線への移行を支援するため、利用者の年齢、視力、端末サイズ、課題音域に応じて見やすさを調整できるようにする。

- 五線の線間隔を調整できる。
- 線間隔の変更に合わせて、音符ブロック、補助グリッド、補助線、音名表示を再計算する。
- 譜面全体の縦位置を調整できる。
- 縦位置調整によって、右手だけ、左手だけ、特定音域だけを見やすい位置へ移動できるようにする。
- 横表示の調整値は、再生位置を変更するシークとは明確に分離する。
- 調整値は第8段階の設定保存で端末内へ保存する。

五線間隔と縦位置の調整は、通常楽譜の正確な組版を目的としない。初心者や子供、高齢者が五線上の上下関係を見やすくするための表示補助とする。

## 11. 再生仕様

### 11.1 再生状態

内部状態は少なくとも次を区別する。

- 停止
- プリカウント
- 再生中
- 一時停止
- 終了

### 11.2 タイミング

- 描画更新には `requestAnimationFrame` を使用する。
- 再生位置はフレーム数ではなく、経過時間から計算する。
- タブがバックグラウンドになった場合は、自動で一時停止するか、復帰時に不自然なジャンプを起こさない処理を行う。
- 表示方向を切り替えても、可能な限り現在の再生位置を維持する。
- このアプリは音楽ゲームではないため、再生ガイドとメトロノームの完全なミリ秒単位同期を主要目的にしない。利用者がテンポと譜面の流れを自然に把握できることを優先する。
- プリカウントまたは開始直前には、実際の `currentBeat` とは別に表示用の助走位置を使って譜面を流してよい。これにより、1拍目から急に画面が動き出す違和感を避ける。
- 表示用の助走位置を使う場合でも、曲の実再生位置、シーク、保存対象の現在位置を混同しない。

### 11.3 メトロノーム

- AudioContextはユーザー操作後に初期化またはresumeする。
- 1拍目は高い音、その他の拍は低い音とする。
- メトロノームON/OFFを変更できる。
- 再生速度変更にメトロノームも追従する。
- メトロノームは描画フレームだけに依存せず、可能な範囲で先行スケジュールする。
- 画面表示とメトロノームは、教育用の練習補助として自然に合って見えることを目標とする。赤い判定線に音を完全一致させるような音楽ゲーム的な精度はMVPの完成条件にしない。

### 11.4 シーク

- 曲の任意位置へ移動できる。
- ドラッグ中は現在位置のプレビューを表示する。
- 縦表示の鍵盤幅調整と混同しない名称、位置、外観にする。

## 12. 設定

### 12.1 表示設定

- 音名表示
- 指番号表示
- 表示密度
- 簡易鍵盤ガイド
- 縦表示の白鍵幅
- 縦表示の横位置
- 横表示の五線間隔
- 横表示の縦位置

### 12.2 再生設定

- 再生速度
- プリカウント
- メトロノーム
- メトロノーム音量

### 12.3 設定の扱い

- 設定変更は可能な限り即時反映する。
- 設定値は許容範囲へ正規化する。
- 初期値へ戻す操作を提供する。
- 鍵盤調整値と一般表示設定を別グループとして扱う。

## 13. エラー処理

次の場合は、画面上に日本語で理解しやすいエラーを表示する。

- JSONが空
- JSONの解析に失敗
- スキーマバージョンが未対応
- 楽曲データの項目が不正
- 音符データが空または不正
- `pitch` と `spelling` が一致しない
- URLのIDが存在しない
- 内蔵一覧または内蔵データを読み込めない
- JSONファイルを読み込めない
- 端末内保存に失敗
- AudioContextを開始できない

エラーが起きても、可能な限りロード画面へ戻り、別のデータを試せる状態を維持する。

開発者向けの詳細をブラウザコンソールへ出してよいが、画面には利用者が次に行うべき操作を示す。

## 14. ファイル構成

初期構成は次を基準とする。実装中に責務が明確になる場合は調整してよい。

```text
piano-roll-score/
  index.html
  package.json
  tsconfig.json
  vite.config.ts
  src/
    main.ts
    app.ts
    styles/
      main.css
    types/
      song.ts
    schema/
      song-schema.ts
    core/
      timeline.ts
      pitch.ts
      staff-position.ts
      keyboard-geometry.ts
      settings.ts
    data/
      song-repository.ts
      builtin-song-repository.ts
      local-song-repository.ts
      import-export.ts
    playback/
      playback-controller.ts
      metronome.ts
    renderers/
      vertical-renderer.ts
      horizontal-renderer.ts
    ui/
      load-screen.ts
      score-screen.ts
      settings-panel.ts
  public/
    data/
      songs/
        index.json
        001.json
  tests/
    unit/
    e2e/
    manual/
      checklist.md
      experiment-notes.md
  docs/
  spec.md
```

ファイル数を増やすこと自体を目的にしない。1ファイルが複数の独立した責務を持ち始めたときに分割する。

## 15. テスト方針

### 15.1 自動テスト

Vitestで少なくとも次を確認する。

- 正常な楽曲データの検証
- 各種不正データの検出
- MIDI番号と音名表記の整合性
- ド♯とレ♭の鍵盤位置が同じになること
- ド♯とレ♭の五線位置が異なること
- 拍と秒の変換
- 曲全体の終了位置
- 再生速度を含むタイムライン計算
- 固定音域と自動音域の計算
- 白鍵、黒鍵の横位置計算
- 五線上の縦位置計算
- 設定値の正規化
- URLパラメーターの解析
- 保存データの変換とスキーマ移行

Playwrightで少なくとも次を確認する。

- ロード画面が表示される
- `?id=001` で内蔵サンプルを開ける
- 不正なIDでエラーを表示できる
- JSON貼り付けから譜面を生成できる
- JSONファイルをインポートできる
- JSONをエクスポートできる
- 端末内へ保存して再読み込みできる
- 縦表示と横表示を切り替えられる
- スタート、一時停止、先頭に戻す、シークが動作する
- 表示設定を変更できる
- 主要なスマートフォン、タブレット、PC相当の画面幅で操作不能にならない

Canvas全体の厳密なピクセル一致を主要テストにはしない。座標計算を単体テストし、必要な代表画面だけ限定的にスクリーンショット確認を使用する。

### 15.2 手動テスト

人間が次を確認する。

- 実際の鍵盤へ白鍵幅を合わせられる
- 横位置を調整して音符と実鍵盤を合わせられる
- 狭い課題音域で不要な余白が目立たない
- スマートフォンを鍵盤付近へ置いて無理なく使用できる
- 音符ブロックの流れが見やすい
- 横表示の5本線が五線として認識しやすい
- 補助グリッドが五線を邪魔しない
- ド♯とレ♭の違いが理解できる表示になっている
- メトロノームの音量、音質、タイミングが適切である
- タッチ操作が誤操作を起こしにくい
- 縦表示から横表示へ移行する教材として有効そうか
- 子供が自発的に触りたくなる見た目と操作感か

手動テスト結果はMarkdownテンプレートへ記録する。

## 16. 教育実験の記録支援

MVPでは利用データを外部サーバーへ自動送信しない。

試用時には、少なくとも次を手動で記録できるテンプレートを用意する。

- 年齢層とピアノ経験
- 使用した課題ID
- 縦表示または横表示
- 使用時間
- 鍵盤幅を合わせられたか
- 一人で操作できたか
- 同じ課題を横表示でも演奏できたか
- 子供が再度使いたがったか
- 先生の所感
- 改善要望

将来、操作ログが必要になった場合は、個人を識別しないローカル記録とJSON／CSVエクスポートを先に検討する。クラウド収集は、目的、同意、保存期間、削除方法を決めてから導入する。

## 17. 対応環境と操作性

- スマートフォン、タブレット、PCの現行主要ブラウザを対象とする。
- 特にiPhone Safari、Android Chrome、デスクトップChromeまたはEdgeで確認する。
- タッチ操作とマウス操作の両方へ対応する。
- 主要操作のタッチ領域は十分な大きさを確保する。
- ノッチやホームインジケーターの安全領域を考慮する。
- CanvasはdevicePixelRatioを考慮し、高DPI画面でぼやけにくくする。
- 端末回転、ウィンドウサイズ変更、表示方向切り替え時に再レイアウトと再描画を行う。
- 色だけに依存せず、ラベルや形でも状態を区別できるようにする。
- 操作ボタンには適切なHTML要素とアクセス可能な名前を使用する。

## 18. プライバシーと安全性

- MVPでは氏名、メールアドレスなどの個人情報を扱わない。
- 読み込んだJSONを外部へ自動送信しない。
- 端末内保存データは、その端末のブラウザ内だけで使用する。
- 外部URL読み込みを将来追加する場合は、許可するURL、通信失敗、意図しないデータ送信を検討する。
- JSON内の文字列をHTMLとして直接挿入しない。
- データサイズと音符数に上限を設け、極端な入力で画面が停止しないようにする。

## 19. 完成条件

次をすべて満たした時点でMVP完成とする。

1. `npm install` 後に開発サーバーを起動できる。
2. 型検査、単体テスト、E2Eテスト、ビルドをnpmスクリプトから実行できる。
3. 静的ビルドを `/app/piano-roll-score/` 相当のサブパスへ配置して動作する。
4. ロード画面から内蔵サンプルを選択できる。
5. `?id=001` で内蔵サンプルを直接開ける。
6. JSONの貼り付け、編集、検証ができる。
7. JSONファイルのインポートとエクスポートができる。
8. 楽曲を端末内へ保存し、再読み込みと削除ができる。
9. 縦表示で音符ブロックが上から下へ流れる。
10. 縦表示で白鍵幅を調整できる。
11. 縦表示で横位置を微調整できる。
12. 狭い課題音域だけを表示し、不要な広い余白を作らない。
13. 白鍵と黒鍵が実鍵盤に近い相対位置で描画される。
14. 横表示で音符ブロックが右から左へ流れる。
15. 横表示で太めの5本線と補助グリッドが表示される。
16. ド♯とレ♭が縦表示では同じ鍵盤位置、横表示では異なる五線位置に表示される。
17. 音名と指番号の表示を切り替えられる。
18. スタート、一時停止、先頭に戻す、シークが動作する。
19. 再生速度、プリカウント、メトロノームが動作する。
20. 表示方向を切り替えても同じ楽曲とタイムラインを使用する。
21. 不正なデータや不正なIDに対して、回復可能なエラー表示を行う。
22. スマートフォン、タブレット、PCで主要操作が可能である。
23. 自動テストが成功する。
24. 実鍵盤との位置合わせ、音、操作感、教育的な見やすさについて手動確認を完了する。

## 20. 将来検討

MVPの試用結果をもとに、次を優先順位付けする。

- 合言葉による課題アンロック
- 外部データカタログ
- ローカル操作ログのエクスポート
- 先生向け課題作成UI
- 低音部譜表ガイドまたは大譜表風背景
- 左右両手や複数譜表への対応
- MIDI入力
- マイク入力
- 演奏判定
- 音源再生
- クラウド保存
- 生徒ごとの進捗管理

将来機能の追加時も、縦表示による鍵盤導入と横表示による五線への移行という教育上の中心目的を損なわないことを優先する。
