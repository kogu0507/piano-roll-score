
# piano-roll-score-4 仕様書

## 1. 目的

ピアノ教室での練習補助に使う、シンプルなピアノロール譜アプリを作成する。

対象は、個人使用および知り合いの個人ピアノ教室での試用である。
大規模な商用サービスではなく、まずは先生や生徒が実際に触って、使い方を研究できる試作版を目指す。

## 2. 基本方針

このアプリでは、楽曲データをJSONで扱う。

共有データを選んでJSON欄に挿入し、必要に応じて先生がその場で編集してから譜面を生成する。

表示方法は2種類用意する。

1. 縦表示
   音符が上から下へ落ちてくる表示。
   実際の鍵盤に合わせて使うことを想定する。

2. 横表示
   音符が右から左へ流れてくる表示。
   五線紙に近い見た目で、音の流れを横方向に確認することを想定する。

## 3. 技術要件

使用技術は以下とする。

* HTML
* CSS
* JavaScript
* Bootstrap 5
* Bootstrap Icons

フレームワークは使用しない。
React、Vue、Svelteなどは使わない。

ビルド環境は使わない。
npm、Vite、Webpackなどは使わない。

ブラウザで `index.html` を開けば動く、静的HTMLアプリとして作成する。

外部ライブラリは、Bootstrap 5 と Bootstrap Icons のCDN読み込みのみ使用してよい。

## 4. ファイル構成

基本構成は以下。

```text
piano-roll-score-3/
  index.html
  data/
    shared-songs.js
  tests/
    manual-test.html
    manual-test.js
  docs/
```

`index.html` には、HTML、CSS、JavaScriptをまとめて書く。
ただし、共有教材データだけは `data/shared-songs.js` に分離する。

`tests/` には、完全自動テストではなく、ブラウザで開いて確認する半自動テストを置く。

## 5. 楽曲データ形式

共有データは `window.PRS_SHARED_SONGS` に配列として定義する。

基本形式は以下。

```json
{
  "title": "曲名",
  "bpm": 80,
  "timeSignature": [4, 4],
  "startCoord": 1,
  "endCoord": 6,
  "startNote": "ド",
  "notes": [
    {
      "coord": 1,
      "keyOffset": 0,
      "accidental": "none",
      "label": "ド",
      "finger": 1,
      "time": 0,
      "duration": 1
    }
  ]
}
```

### 曲全体

`title`
曲名または練習課題名。

`bpm`
基準テンポ。

`timeSignature`
拍子。例: `[4, 4]`。

`startCoord`
表示範囲の左端または下端になる基準座標。

`endCoord`
表示範囲の右端または上端になる基準座標。

`startNote`
`startCoord` に対応する音名。例: `"ド"`。

`notes`
音符データの配列。

### notes の各項目

`coord`
音の高さを表す座標。
白鍵の位置を整数で扱う。

`keyOffset`
黒鍵や半音位置の横方向・縦方向のずれ。
例: `0`, `0.5`, `-0.5`。

`accidental`
臨時記号の種類。
`"none"`, `"sharp"`, `"flat"` を想定する。

`label`
表示する音名。

`finger`
指番号。
表示しない場合でもデータとして持てる。

`time`
開始位置。単位は拍。

`duration`
音の長さ。単位は拍。

## 6. 画面構成

画面は大きく2つに分ける。

1. ロード画面
2. 譜面表示画面

ロード画面には以下を置く。

* 共有データ選択
* 共有データを挿入ボタン
* JSON入力欄
* 楽譜を生成するボタン

譜面表示画面には以下を置く。

* ロード画面に戻る
* スタート / 一時停止
* 先頭に戻す
* 表示方向切り替え
* 設定メニュー
* 譜面エリア
* シークUI
* 縦表示用の鍵盤幅調整

## 7. 縦表示仕様

縦表示では、音符が上から下へ落ちてくるように見せる。

* 横軸: 音の高さ
* 縦軸: 時間
* 音符の縦方向の長さ: `duration`
* 音符の横位置: `coord + keyOffset`

画面下部に判定ラインを表示する。

縦表示では、実際のピアノ鍵盤と画面上の音符位置を合わせるために、鍵盤幅調整スライダーを用意する。

鍵盤幅調整は、再生位置を動かすシークバーとは別機能である。

## 8. 横表示仕様

横表示では、音符が右から左へ流れてくるように見せる。

* 横軸: 時間
* 縦軸: 音の高さ
* 音符の横幅: `duration`
* 音符の縦位置: `coord + keyOffset`

左側に判定ラインを表示する。
横表示の背景は五線紙風にする。

## 9. 再生仕様

スタートボタンを押すと再生開始。

再生中はボタン表示を一時停止にする。

一時停止を押すと、現在位置で停止する。
再度スタートすると、同じ位置から再開する。

先頭に戻すボタンを押すと、再生位置を最初に戻す。

プリカウントは以下を選べるようにする。

* なし
* 1小節
* 2小節

メトロノームON/OFFを切り替えられるようにする。
1拍目は高めの音、それ以外は低めの音にする。

AudioContextは、原則として「スタート」ボタン押下時に初期化またはresumeする。

## 10. 表示設定

以下の表示設定を用意する。

* 音名表示 ON / OFF
* 指番号表示 ON / OFF
* 表示密度
* 再生速度
* プリカウント
* メトロノーム ON / OFF

## 11. エラー処理

以下の場合は、ユーザーにわかるように表示する。

* JSONが空
* JSONのパースに失敗
* `notes` が配列ではない
* `notes` が空
* `bpm` が不正
* `timeSignature` が不正
* `startCoord / endCoord` が不正
* `notes` 内の項目が不正
* `shared-songs.js` が読み込めない

エラー表示は、可能なら画面上のBootstrap alertで表示する。

## 12. ピュア関数候補

以下は、できるだけDOM、Canvas、AudioContextに依存しない小さな関数または小さなクラスとして扱う。

* 楽曲データ検証
* 拍数と秒数の変換
* 楽曲全体の終了拍の計算
* 表示範囲の計算
* 縦表示の座標計算
* 横表示の座標計算
* 設定値の正規化
* 半自動テスト結果のMarkdown整形

これらは可能な範囲で、アプリ本体から切り離してテストしやすくする。

## 13. 半自動テスト方針

完全自動テストではなく、ブラウザで開く半自動テストを用意する。

`tests/manual-test.html` を開き、ボタン操作または表示結果を確認する。

テストページは、以下を支援する。

* ロジック確認
* 手動確認チェックリスト
* テスト結果のMarkdown出力
* ChatGPTへ渡すための結果コピー

Canvasの見た目、音が鳴るか、操作感、教育的使いやすさは、人間が確認する。

## 14. 完成条件

以下を満たせば完成とする。

1. `index.html` と `data/shared-songs.js` で基本動作する
2. ブラウザで `index.html` を開くとロード画面が表示される
3. 共有データのプルダウンが表示される
4. 「共有データを挿入」を押すとJSON欄にデータが入る
5. JSON欄を手動編集できる
6. 「楽譜を生成する」を押すと譜面表示画面に移動する
7. 縦表示で音符が上から下へ流れる
8. 横表示で音符が右から左へ流れる
9. 縦表示と横表示を切り替えられる
10. スタート / 一時停止が動作する
11. 先頭に戻すが動作する
12. メトロノームON/OFFが動作する
13. プリカウントが動作する
14. 再生速度変更が動作する
15. 音名表示ON/OFFが動作する
16. 指番号表示ON/OFFが動作する
17. 表示密度調整が動作する
18. 縦表示の鍵盤幅調整スライダーが動作する
19. 鍵盤幅調整はシークバーとは別機能として実装されている
20. JSONが不正な場合にエラー表示される
21. スマホ、タブレット、PCで大きく崩れない
22. 譜面エリアの音符・背景・判定ラインはCanvas APIで描画されている
23. 画面サイズ変更や表示方向切り替え時にCanvasが再描画される
24. `tests/manual-test.html` で半自動テスト結果を確認できる
